# ============================================================================
# Catalog 阶段规约 — profile 中立骨架（无 toolchain 细则）
# ============================================================================
# 模块 format 枚举、导出入口与打包形态相关的宿主说明在叠加层：
#   framework/profiles/hmos-app/phase-rules-overlays/catalog-rules.overlay.yaml
#
# 说明：ALLOWED_FORMATS 等运行时枚举仍可由 harness 脚本与 profile 能力共同演进；
# 本骨架保证无 profile overlay 的工程仍能加载 phase spec（或由 profile 补齐细则）。
#
# 消费者：
#   - framework/harness/scripts/check-catalog.ts
# ============================================================================

phase: catalog
version: "1.0"
applies_to: "doc/module-catalog.yaml"

structure_checks: {}

semantic_checks: {}

traceability_checks: {}
