---
name: 修复环裁决化 — 候选真伪裁决、收敛不变式与增量修复环
version: 3.0.0
todos:
  - id: t1-signal-identity-convergence
    status: completed
    content: M1 止血——信号级身份 + 累计 one-shot 收敛 + no-op 短路(最小机制,零新账本/零新事件/零新状态机)。①信号级候选:`collectActionableDefects` 从「每屏一条」改「每信号一条」,identity = `sha256(computeDefectFingerprint(screen, defect))`(visual-diff-check.ts:652 既有单条稳定指纹直用);must_fix→defect 关联**复用既有 `defect.must_fix_refs`**(visual-diff-check.ts:113,门禁已强制 must_fix 必须被 refs 引用)反向解析,不新增任何平行 identity/关联结构;`RepairCandidate` 增 `identity_schema: 'signal@1'`(legacy 无字段=仅诊断不入收敛判定)。②累计 one-shot 收敛公式(v4 修正 attempted 判定——v3 直接从 requested 累计会把「只 request 未执行」误判已修:现有实现 request→started→目标 phase 才真执行(goal-runner:7822/:7854),且 crash/resume 契约明确要求 request-only 候选必须恢复执行(repair-candidates.unit:692),v3 语义下 request(A)→崩溃→resume 会 eligible 空、A 从未修过即 repair_not_converging):**候选身份从 `phase_backtrack_requested.candidates[]` 取,但仅当同一回退窗口之后出现目标 phase 的 `agent_process_settled` 或 `phase_verdict` 事件,该批身份才计入 `attempted`**——全用既有事件,零新状态机;`eligible = current_open − attempted`;eligible 非空 → 只回退 eligible(注入按 eligible 过滤);eligible 为空 → 停 `repair_not_converging`(operator/WAITING-human,guidance 列每条 attempted∩open 身份的跨轮证据差异+人工恢复通道指引)。累计集防 A/C 交替重获资格(v2 漏洞)。术语澄清:文中 `identity` 只是概念名,**唯一存储字段仍是 `item_fingerprint`**;累计与 no-op 判定仅作用于 `identity_schema='signal@1'` 的候选,不触碰既有 check-domain candidates。整轮全等指纹熔断保留为兜底;`repeated_round` 接入 assess(现成声明位)。③no-op 短路:目标 phase agent settled 后比对 `product-source-snapshot` pre/post(pre-existing dirty 合法);相等 → 不重跑下游 + 本次回退候选并入 attempted → 走②停等;快照 unverifiable → fail-closed 现行全量。④事件语义(裁掉 v2 的 requested→started→executed→completed/noop 新状态机):保留既有 `phase_backtrack_requested/started/completed` 三事件;目标 phase 是否执行复用既有 `agent_process_settled`/`phase_verdict`;**把 :7592 提前发出的 completed 移到回退链真正完成之后**,repair/scope 路径补发;no-op 记为 `phase_backtrack_completed.result='noop'`,不新增事件类型;crash/resume 由既有事件回放语义覆盖,不设新恢复协议。回归:60bcd1 events 反演(i16 轮 expanded 身份 ∈ attempted → eligible 空 → 停;i17 快照相等 → noop);A/C 交替 case;**crash 两场景**(request 后目标 phase 执行前崩溃 → 仍 eligible,resume 后必须收到候选;目标 phase settled 后 completed 前崩溃 → 已 attempted,不得再自动修);既有 backtrack 单测全绿。
  - id: t2-producer-adjudication
    status: completed
    content: M2 裁决——producer 根治(主体)+ 最小两态裁决管道(v3 删自动 refuted 与第二套代码 verifier)+ 恢复复用既有通道。①producer 根治(第一道也是主要防线):visual-diff-check 产信号时 OCR 识别文本与候选串集合编辑距离 ≤1 → **uncertain**,不产 FAIL 级 text_placement 信号(编辑距离 1 只能证明两源冲突、不能证明 OCR 错——冲突即不确定,归 uncertain 交人裁,不自动判谁对);整页拼接参考图高度>视口高度时纵序二部匹配自动降级 uncertain 并注明口径缺口;OCR/viewport/geometry 的机械判定**只在 producer 一处实现**,裁决层不重复检测算法(否则第二套规则=第二真源)。**uncertain 生产链路与判停时序冻结(v5 接线+v6 时序)**:载体=既有 producer-owned `VisualDiffStructuredPayload`(visual-diff-check.ts:791)新增**可选 `uncertain_signals[]`**(逐条携带 `item_fingerprint`+原因+必要证据引用),经既有 `checks[].structured` 随 `script-report.json` 落盘(report-generator.ts:134/:147——无需新文件或新 IPC);读取点=goal-runner 读 fresh summary + 本轮 script-report 时。**判停时序(v6 冻结,必须早于 PASS closure 而不只早于 merge)**:`读 fresh summary+script-report → 读取 checks[].structured.uncertain_signals[] 形成局部 pending 标志 → 禁止 receipt validation 与 finalizePhaseClosure(:6319/:6335 不得执行) → 仍完成既有 visual_round 事件投影(:6393)与完整性处理 → halt repair_adjudication_pending,不进入普通 verdict/candidate merge`——杜绝「唯一 uncertain 其余全 PASS 时先完成 phase closure 再停等」的成功态与停等并存;**不回写 visual-diff.json、零新状态/事件/产物**。生产接线测试断言三条:走真实载体(check 产 payload→script-report→runner 停等);uncertain-only 时不得调用/完成 closure finalization;存在 visual_round 回执时既有事件投影不因提前停等丢失。②物化前裁决(v4 修正——v3 的「uncertain 不产候选只留 WARN」使其从两态管道掉出:既非 confirmed 也非 needs_human,而 harness 只把 FAIL 级纳入 violations(harness-runner:847),WARN 不保证停等,与「交人裁/fail-closed」自相矛盾):裁决发生在 **merge 之前**,`repair_candidates[]` 维持现行「只承载 trusted actionable defects」契约(unified delta:5)——**actionable + agent 复核同向 → confirmed,才物化为现有 RepairCandidate 并可回退**(v23 F1 保留,可信=harness 合成裁决非 agent 自报);**actionable 被 agent 反对、actionable 未复核、或 producer uncertain → merge 前直接停 `repair_adjudication_pending`**(operator/WAITING-human;fail-closed,agent 不写复核块无得利路径;无自动 refuted,反对理由与证据原样呈给人)。**needs_human 对象不写入 `repair_candidates[]`,不新增 adjudication schema/MIGRATION 面**——证据复用 `script-report.json` 的 `checks[].structured.uncertain_signals[]` 与 defect-review 块本身(v6 订正:v5 曾误写「visual-diff.json 的 uncertain 注记」,与不回写裁决矛盾);`summary.json` 唯一 schema 增项=可选 `identity_schema` 字段(向后兼容,非 breaking;payload 的 `uncertain_signals[]` 是结构化载体形状的可选扩展,同为非 breaking、无迁移要求)。③人工恢复单一真源=既有 visual-confirm 人签通道(`repair_not_converging`/`repair_adjudication_pending` 的恢复=visual-diff.json 屏条目 `confirmed_by` 真人人签,isHumanVerified 谓词——人签=人工确认该屏无需修复);**收敛 halt 后人工 `--resume` 本身即一次显式放行**(attempted 重新计入后须再次人工动作,不自动循环);不引入 manual driver/confirmation-receipts 作为本 change 恢复输入、不新增 receipt 体系;halt guidance 写明通道入口与 resume 命令(WAITING 可接受未来输入)。④retry 提示模板(review FAIL 路径)删除「apply a minimal fix」类代改诱导,改为「登记候选附复核证据交裁决路由」。回归:OCR 混淆在 producer 归 uncertain;**唯一信号为 uncertain、其余全 PASS 时仍必须停等**(不得静默 WARN 放行);agent 反对 → 停等不回退;无复核块 → 停等;confirmed 才物化为候选;人工通道恢复链路;v23 F1 修订版(PASS+harness-adjudicated-confirmed 仍回退)。
  - id: t3-regressions-closure
    status: completed
    content: 回归与收口——①60bcd1 全链反演回归(真实 events 片段):expanded OCR 信号 producer 侧归 uncertain(根治后不产候选)+ 若 actionable 且 agent 反对则 needs_human 停(第二道防线分列单测);collapsed 真缺陷同向 confirmed 正常回退一次;i16 eligible 空停 repair_not_converging;i17 noop。②既有契约不回归断言:goal-runner 禁直调 classifyPhaseVerdict、phase_verdict 仅 boundary 发、halt_reason 全注册、supervisor 只读 run_disposition、IncidentFacts 不产授权、「宁缺毋滥」。③单测契约修订:item_fingerprint 断言改 `sha256(computeDefectFingerprint)`+identity_schema;整轮全等降为兜底、累计 one-shot 为主判据;v23 F1 陈述改「PASS+harness-adjudicated-confirmed 仍回退」。④openspec delta 随返修同步(M1 收敛=ADDED、M2 裁决=ADDED;v2 的 MODIFIED :527 删除随 M4 移出),strict validate 过门;与在飞 unified-responsible-phase-routing 合并关系维持 proposal 注明。⑤验证节奏:窄返修=目标测试;里程碑收口=typecheck+`cd harness && npm test`+`openspec validate --strict`;`release:verify` 只留正式发版门禁。
overview: >
  08-20 宿主(SimulatedWalletForHmos bc-openCard-1,run 20260819T155559Z-60bcd1)实锤:
  小修复需求活跃 584m 被 budget_wall_clock 熔断。三根因:①候选无真伪裁决——OCR 误识+整页
  参考图 vs 单视口口径错配产出假信号,testing agent 复核结论无人消费,actionableDefectsToCandidates
  恒 category:'coding'(review 侧须 verifier 逐条 confirmed,testing 侧零验真直通);②候选按屏
  聚合、身份随 defects 集合漂移(goal-runner.ts:1875 拼接 hash;单条稳定指纹
  computeDefectFingerprint 已存在未被候选层用),五族收敛机制全部脱靶,真伪回退不可区分;
  ③回退全量级联无 no-op 检测(i17 零改动后 review 仍全量重启至熔断),review 走私改码
  (retry 模板「apply a minimal fix」教唆)→closure 缺口→stale 传染。本 change 收口两支柱
  (v3 最小机制):M1 信号级身份(computeDefectFingerprint+must_fix_refs 既有真源直用)+
  累计 one-shot 收敛(attempted 从 events 派生;eligible=open−attempted;空即停)+no-op 快照
  短路(completed.result='noop',零新事件);M2 producer 根治(OCR 编辑距离≤1/视口口径冲突
  源头归 uncertain——冲突≠可自动判错)+物化前两态裁决(同向 confirmed 才物化为候选可回退;
  反对/未复核/uncertain → merge 前停等,candidates 维持 trusted-actionable 契约、零 schema
  breaking,无自动 refuted、无第二套代码 verifier)+恢复复用既有人工通道。M4(receipt
  runner-owned 机械化/closure_wall_repeated 恢复/预算显式化)与 M3(增量修复环/设备证据复用)
  均另立 change。归因经本侧+codex 双路交叉验证;v2 吸收一轮 review 8 阻断,v3 吸收二轮
  review 3 必修,v4 吸收三轮 review 2 P1(attempted 加目标 phase settled/phase_verdict
  执行门槛防 request-only 误计;uncertain 收进 merge 前停等路径)。
isProject: false
---

# 修复环裁决化:候选真伪裁决、收敛不变式与增量修复环(e2b7c4a9)

状态:**已批准(v6),实施中**(2026-08-21 用户终审通过,五轮 review 收口;下游 AI 按本 plan todos 开发,提交遵循「提交与集成顺序」节,每里程碑独立可 pick;实施产物回本侧 review——验收基线=各 todo 的回归清单 + specs delta 的 Scenario + 既有契约不回归断言)

## 背景与根因链(08-20 bc-openCard-1 · run 60bcd1)

manifest 需求原文:「修复 TC-018 未执行以及三组视觉差异,完整重跑 coding/review/ut/testing,不得降低 P0 标准或签发 waiver」。全 run 18 attempts、活跃 584m:

```
前夜 23:56–0:29  spec-i1 33m FAIL(3 条 fidelity 门禁互斥)→ await_human 停机 6.3h
06:48–09:20      spec-i2/i3/i4(91m + harness closure 超时 54m);i4 PASS 却被
                 closure_wall_repeated 判 TERMINAL + supervisor never_restart → 等人 3.5h
12:52–13:22      spec-i5(22m 纯填回执占位符 FAIL)+ i6 PASS——6 attempts 才出 spec
13:22–15:37      plan 30m / coding 27m / review-i9 FAIL+i10 32m(review 亲手改码)/ ut 27m
15:37–16:57      testing-i12 77m(hylyre 本体仅 ≈150s/轮)→ 屏级 candidates ×3(collapsed
                 真缺陷 + expanded OCR 伪缺陷)→ backtrack 作废 coding/review/ut
16:57–18:54      第二轮全链 115m:coding-i13 修 collapsed 真缺陷(有效);testing-i16 →
                 唯一 candidate = expanded 伪缺陷(屏级身份随集合漂移,复发不可见)→ 又 backtrack
18:57–19:37      第三轮:coding-i17 26.7m 自证「产品源码零改动」→ review-i18 13m → 熔断
```

## 事实核实表(v6;v2 review「已通过」清单维持,标注处置去向)

| # | 断言(来源) | 核实 | 证据 / 处置 |
|---|------|------|------|
| 1 | visual_diff 候选零验真直通回退,review 候选须信任合取——不对称 | ✅ | repair-candidates.ts:599 恒 `'coding'`;:263-297 review 合取 |
| 2 | testing 复核结论在候选 summary 字段里,裁决代码不读 | ✅ | 60bcd1 testing summary;assess.ts:768-802 只读 category |
| 3 | visual 候选身份=屏级 defects 集合拼接 hash;单条稳定指纹已存在未被候选层用 | ✅ | goal-runner.ts:1875 `structural.sort().join('&')`;visual-diff-check.ts:652 computeDefectFingerprint |
| 4 | 第一次回退正当、第二次纯伪缺陷驱动——系统不可区分 | ✅ | i12 candidates=3(collapsed 真缺陷有效修复);i16=1(仅伪缺陷) |
| 5 | 整轮全等熔断防不了「集合缩小」;eligible-baseline 恒不成立 | ✅ | goal-runner:7774;visual-rounds-ledger:223-225 |
| 6 | repeated_round 声明了但 assess 零消费 | ✅ | assess.ts:83-86 |
| 7 | i17 零改动后 review 仍全量重启;dirty worktree 下 git diff 判据不可用;快照机制已存在 | ✅ | coding gap-notes 自证;product-source-snapshot.ts:16 |
| 8 | 同构建设备重复执行 ≥9 次;证据复用与信任链冲突 | ✅ | goal-runner:1742 起 collector 强制——**随 M3 另立 change** |
| 9 | 预算 480 生效 585 是按契约执行(派生下限),问题在不显式 | ✅ | goal-manifest.schema.yaml:51——**随 M4 移出另立** |
| 10 | tryCloseUpstreamPhase 只 finalize 已 passed;missing/failed→receipt_repair_with_verifier 是规范明文 | ✅ | upstream-closure.ts:68;spec:527——**支撑 M4 另立 change 的设计输入** |
| 11 | review 改 4 个产品源文件的授权依据=retry 提示模板「apply a minimal fix」 | ✅ | review/headless-assumptions.jsonl 实录 |
| 12 | 「PASS+可信候选仍回退」是锁死契约,只能收紧"可信" | ✅ | repair-candidates.unit v23 F1(:548) |
| 13 | 链结构无法表达修复环;回退=指针回拨+整段级联 | ✅ | goal-runner:7853/7855 |
| 14 | 「Incremental re-review」只是提示词不缩执行面 | ✅ | buildBacktrackReviewFocusBlock 注释 2618-2620——M3 域 |
| 15 | closure_wall_repeated 判 TERMINAL 是误分类;WAITING 须定义恢复输入 | ✅ | 60bcd1 实证——**随 M4 移出另立** |
| 16 | 时间 97.7% 在 agent 内部,非编译/测试慢 | ✅ | UT tsc 60ms/hylyre ≈150s/轮 |
| 17 | visual 候选生产在 goal-runner 收集侧,harness-runner 侧无输入可验 | ✅ | harness-runner:1585 vs goal-runner:6503→:6556——裁决管道落收集侧 |
| 18 | phase_backtrack_completed 在目标 phase 执行前发出 | ✅ | goal-runner.ts:7592——v3 处置=移到真正完成后+result 字段,不新增状态机 |
| 19 | **(v3 新增)** must_fix→defect 关联已存在,门禁强制引用 | ✅ | visual-diff-check.ts:113 `must_fix_refs`;visual-fidelity.unit:3619「must_fix 无 refs 引用应 BLOCKER」——直接复用,不新增关联结构 |
| 20 | **(v3 新增)** UI tree 与 OCR 编辑距离 1 只能证明两源冲突,不能证明 OCR 错 | ✅ | 逻辑审计(v2 review):自动判 refuted 无依据——冲突归 uncertain(producer)或 needs_human(裁决),v2 的 text_mismatch 自动 refuted 作废 |
| 21 | **(v4 新增)** crash/resume 契约要求 request-only 候选必须恢复执行——attempted 不得从 requested 直接累计 | ✅ | goal-runner:7822/:7854(request→started 后目标 phase 才真执行);repair-candidates.unit:692(request 后崩溃的候选 resume 须恢复) |
| 22 | **(v4 新增)** WARN 级注记不保证停等;candidates 现行契约=只承载 trusted actionable | ✅ | harness-runner:847(仅 FAIL 级进 violations);unified delta:5——uncertain 须走 halt 路径而非 WARN,needs_human 不得写入 candidates |

## v5→v6 的裁决变化(吸收五轮 review 1 P1 + 1 P2 + 2 非阻断)

1. **uncertain 判停时序冻结**(P1):v5 只保证「merge(:6503→:6556)前判停」,但现有控制流在 :6503 之前已完成 PASS 的 receipt 校验与 `finalizePhaseClosure`(:6307 读 summary→:6319/:6335 closure→:6393 visual_round 投影→:6503 收集)——「唯一 uncertain 其余全 PASS」会先关环再停等,成功态与停等并存。v6 冻结:读 fresh summary+script-report 后即读 `uncertain_signals[]` 形成 pending 标志 → **禁止 receipt validation/finalizePhaseClosure** → 保留既有 visual_round 事件投影与完整性处理 → halt,不进普通 verdict/merge;接线测试补两断言(uncertain-only 不得调用/完成 closure finalization;visual_round 投影不因提前停等丢失)。零新状态/事件/产物。
2. **plan 证据残留订正**(P2):「证据复用 visual-diff.json 的 uncertain 注记」与不回写裁决矛盾→改「复用 script-report.json 的 checks[].structured.uncertain_signals[] 与 defect-review 块」;载体落盘依据=report-generator.ts:134/:147(checks[].structured 随 script-report 既有落盘,无需新文件/IPC——五轮 review 证实)。
3. **非阻断**:proposal/design/tasks 正文的 plan 版本引用同步;「本 change 唯一 schema 增项」收窄为「`summary.json` 唯一 schema 增项」(payload 的 uncertain_signals 同为形状可选扩展,非 breaking 无迁移)。

## v4→v5 的裁决变化(吸收四轮 review 1 P1 + 1 P2)

1. **uncertain 生产链路冻结**(P1):v4 只规定了「producer uncertain → merge 前停等」的结果,未定义 uncertain 如何从 visual-diff-check 到达 goal-runner(`VisualDiffStructuredPayload` 现无 uncertain 字段(visual-diff-check.ts:791);goal-runner 只从 visual-diff.json 收集 must_fix/crash(:1568/:1807))。v5:载体=payload 新增**可选 `uncertain_signals[]`**(item_fingerprint+原因+证据引用),经既有 check report 通道传递(check :2630 构造→harness-runner:1071 consumeVisualRoundPayload 同源通道),goal-runner 在 collectActionableDefects→merge(:6503→:6556)链路前读取判停;**不回写 visual-diff.json、零新文件/账本/receipt/状态机**;补一条走真实载体的生产接线测试。
2. **proposal 残留清理**(P2):Impact 清单「repair-candidates.ts …adjudication 块类型」与「无 adjudication schema」裁决矛盾,已删;design/tasks 标题版本号同步。

## v3→v4 的裁决变化(吸收三轮 review 2 P1 + 2 非阻断)

1. **attempted 判定加执行门槛**(P1-1):v3 从 `phase_backtrack_requested.candidates[]` 直接累计,会把「只 request 未执行」误判已修——现有实现 request→started 之后目标 phase 才真执行(goal-runner:7822/:7854),且 crash/resume 契约要求 request-only 候选必须恢复执行(repair-candidates.unit:692);v3 语义下 request(A)→崩溃→resume 会误停 repair_not_converging。v4:身份从 request 取,**仅当同一回退窗口后出现目标 phase 的 `agent_process_settled` 或 `phase_verdict` 才计入 attempted**;补 crash 两场景回归。
2. **uncertain 收进停等路径,裁决移到 merge 前**(P1-2):v3 的「uncertain 不产候选只留 WARN」使其从两态掉出(WARN 不保证停等,harness-runner:847 只 FAIL 级进 violations)。v4:confirmed 才物化为 RepairCandidate(`repair_candidates[]` 维持 unified delta:5 的 trusted-actionable 契约);disputed/unreviewed/producer-uncertain → merge 前直接停 `repair_adjudication_pending`,不写入 candidates;**删 adjudication schema 与 MIGRATION 面**(summary 仅新增可选 identity_schema,非 breaking);补「唯一 uncertain 其余全 PASS 仍停等」Scenario。
3. **非阻断收口**:集成序列图 `t5`→`t3`;明确 `identity` 只是概念名、唯一存储字段仍是 `item_fingerprint`,累计/no-op 仅作用于 `identity_schema='signal@1'`(不触碰 check-domain candidates)。

## v2→v3 的裁决变化(吸收二轮 review 3 必修)

1. **M4 与 closure_wall_repeated 移出本 change**(必修 1):receipt runner-owned 字段机械化、closure_wall_repeated 恢复、预算派生显式化三项与候选裁决无数据依赖,继续捆绑=扩大文件面/规格面/验收链(重蹈「小需求跑十小时」)。另立 closure/预算小 change;v2 的 MODIFIED :527 delta 随之删除。本 change 核心=M1+M2。
2. **收敛公式改累计 one-shot,裁掉新事务状态机**(必修 2):v2 的 T=「上一次回退目标」存在 A/C 交替重获资格漏洞;v3 公式=`attempted(本 run 已实际执行自动修复的全部身份,events 累计派生)`,`eligible=current_open−attempted`,非空只回退 eligible、空即停——全部从既有 events 派生,零新账本。事件侧:保留既有三事件;目标 phase 执行与否复用 agent_process_settled/phase_verdict;:7592 的 completed 移到真正完成后;no-op=`phase_backtrack_completed.result='noop'`,零新事件类型;身份复用 computeDefectFingerprint+既有 must_fix_refs,零平行结构。
3. **删第二套代码 verifier 与新 receipt 体系**(必修 3):OCR/viewport/geometry 机械判定只在 producer 一处(裁决层重复实现=第二套规则第二真源);编辑距离 1 只证冲突不证谁错→自动 refuted 作废;裁决收敛为两态(同向 confirmed 可回退;反对或未复核 needs_human 停);人工恢复单一真源=既有 visual-confirm 人签通道(confirmed_by + isHumanVerified);「再允许修一次」=收敛 halt 后人工 --resume 本身(attempted 重新计入后须再次人工动作);不引入 manual driver/confirmation-receipts 作为本 change 恢复输入、不新增 defect-adjudication receipt。

## 交叠与依赖

- **`unified-responsible-phase-routing`(在飞)**:不推翻;其 delta 的 `item_fingerprint` 定义由本 change 细化为信号级(归档合并以本 change 为准);其「真机缺陷恒属 coding」由裁决两态修正(needs_human 时不再机械回退 coding)。
- **既有元门禁契约全部保留**:禁直调 classifyPhaseVerdict、phase_verdict 仅 boundary 发、halt_reason 注册表、supervisor 只读 run_disposition、IncidentFacts 不产授权、「宁缺毋滥」。
- **另立 change 清单**:①closure/预算小 change(M4 三项:receipt runner-owned 机械化——设计输入=事实 #10 的 owner 分类与预填+re-probe;closure_wall_repeated 恢复;预算派生显式化);②M3 增量修复环(pre/post 双 scope、部分 fresh 合成语义、设备证据复用 reused_from 血缘+fresh-directory 契约 delta);③fidelity 三门禁 SSOT 互斥、`--skip-assert-expected` 硬编码、hylyre 采集编排效率、spec 需求溯源纪律。
- **宿主侧当下处置(不等本 plan)**:60bcd1 不加预算续跑;按 codex 收口路径人工完成。

## 提交与集成顺序(codex 建议,已采纳;v3 注:M4 为另立小 change,序列位置保留)

```
Br_release_3.0.0
  OpenSpec delta 补齐并 strict validate
  → d11348a(既有前置)
  → a52b64f(既有前置)
  → M1 稳定身份/收敛/no-op        (本 change t1)
  → M4 receipt/closure-only/预算   (另立 closure/预算小 change)
  → M2 候选裁决                    (本 change t2)
  → M3 增量修复环                  (另立 change)
  → t3 全量回归与 OpenSpec 收口    (本 change t3 + 各 change 收口)
            │
            └──逐里程碑 cherry-pick -x──▶ main 集成分支
```

约束:每里程碑独立可 pick 的提交(串),不跨里程碑混改;`-x` 保留源追溯。

## 已定裁决(v6;除非用户反对,按此执行)

1. **本 change 范围=M1+M2**;实施顺序 t1→t2→t3 收口;M4(closure/预算小 change)与 M3 另立,集成序列位置保留。
2. **v23 F1 保留、可信收紧**:可信=harness 合成 adjudication(mechanical+agent 同向),agent 自报不生效。
3. **物化前两态裁决、fail-closed、恢复走既有通道**:confirmed 才物化进 `repair_candidates[]`(trusted-actionable 契约不变);agent 反对/未复核/producer uncertain=merge 前停等,不写入 candidates、无 adjudication schema;无自动 refuted;恢复单一真源=既有 visual-confirm 人签通道(confirmed_by + isHumanVerified);「再修一次」=人工 --resume 本身;不新增 receipt/ledger/账本。
4. **身份最小机制**:`sha256(computeDefectFingerprint)`+`identity_schema:'signal@1'`+复用 must_fix_refs;legacy 仅诊断。
5. **不建跨 build 静默缓存;producer 根治为主体**:冲突/口径缺口在源头归 uncertain,裁决层不重复检测算法。
