---
name: 3.0.0 效率优先 — 闭环仪式减法 / 判词给改法 / 派生与量测机器化 / verifier 一次化与核对化 / 回执与报告机器化 / 真机证据按输入复用 / 上下文减负
version: 3.0.0
# 窗口说明：Br_release_3.0.0 长期线（宿主体验优化），与 main 的 3.1.0 蓝图刻意不同号（记忆 branch-integration-strategy）。
# 原则来源：用户 2026-09-03 裁定——"防篡改永远不是高优先级；高优先级是平衡效率和准确性，又快又好，
# 不要求 100% 没问题；篡改不一定非要拦截，关键看效率；不做对比测试，开发完新版本直接去宿主跑，
# 有问题回来分析哪里能加速。"并要求"不要分 plan，合并到一个 plan 里，这一次把效率提升要做好"。
# 与 docs/overview.md §1.2.1 第一条"效率优先"（codex 2026-09-03 15:02 已落盘，四条总原则）同向；记忆 design-principle-efficiency-first。
# v2（2026-09-03，codex 一轮 review 后返修）：吸收 17 条意见——删僵尸 --correction-check；执行通道三态；
# scroll 保留不改 tap；身份断言由注入提供、UX 谓词断言保留；稳定性口径按完整执行键含失败轮；verifier
# 材料按 evidence manifest 读取面、状态改 completed_with_prior_review / current_material_not_reverified；
# 回执退出闭环输入并补时序；revalidate 只是检查执行器、UX 漂移按风险分级；执行键补设备/复位/版本；
# 报告整份机器生成、结论确定性派生、agent 观察进 notes.md；量测拆 geometry/content/style 三轴、不改
# ui-spec 真源、不解除 release block；phase executor 唯一 owner 并同步 goal-mode-skill spec；公共 CLI
# 只留 --revalidate / --force-device / --measure；OpenSpec 前置为 T0；验证按比例、整批收口再全量。
# v3（同日，codex 二轮 review）：P0 unsupported_gap 留在原始总分母并须机器证明；verifier subject 改为签发时
# 计算的 pre-verifier material view（完整 manifest 在 finalize 后才生成且含 verifier report，会成环）；机器报告
# 消费 visual-diff / visual-debt / 量测 / quality axes 并按轴出结论；T5/T6 依赖方向改正（T5 先、stability 可缺省）。
# 证据来源：宿主 bc-openCard-1 2026-09-02 会话（Claude /goal 手工推进六阶段，16h 全闭环）本机日志复算
# （记忆 bc-opencard1-0902-session-cost）：主代理 910 轮、缓存读 4.36 亿（≈48 万/轮）、verifier 34 次
# （单次 4–13 分钟、新鲜输入 5.66M）、真机 10 轮（后 7 轮派生计划字节相同、6 轮同 HAP）。
# 主代理上下文构成（字符）：代理自写命令与临时脚本 80 万（411 个脚本：209 个处理 trace/report/timing、
# 66 个量像素、39 个校验 JSON/YAML）、读文件返回 64 万（487 次：212 次读 framework TS 源码）、脚本输出 29 万、
# verifier 报告全文回传约 30 万、harness 输出 10 万、代理叙述 7 万。
overview: >
  这次回归慢，不是门禁抓错了什么，而是：闭环仪式把每一次改动放大成"harness → verifier → 回执 →
  check-receipt → 下游全部重闭"，verifier 报告绑在带时间戳的 prompt 字节上，重跑一次 harness 就作废；
  门禁只报症状不给改法，代理读了 212 次 framework 源码去反推；派生计划、像素量测、报告表格、稳定性
  比对全靠代理手写脚本，411 个临时脚本既慢又是错误来源；真机证据绑整份 test-plan.md 的 hash，改一个
  版本号就重跑真机；不可执行的测试输入要跑完真机才被判死。本 plan 一次做完：先立 OpenSpec change；
  删 correction 硬路径与僵尸命令；全部 BLOCKER 判词给到 TC/step/实际形状/期望形状/改法，执行通道改
  三态；P0 身份断言由 harness 在装载派生计划时注入、UX 谓词断言原样保留；闭环直接读 summary，回执退出
  闭环输入；测试报告整份机器生成、结论确定性派生；真机按完整执行键复用，稳定性按同键含失败轮统计；
  verifier 每份材料一次、材料按 evidence manifest 读取面、prompt 与输出瘦身、保留首轮 UX 语义检查、
  后续只核对；下游重闭一条检查命令、UX 源码漂移按风险分级复核；Claude /goal 主会话做薄 driver、每
  阶段一个 phase executor；视觉量测只出几何事实与三轴状态。每项写明放弃了哪部分准确性、可以不复审但
  必须诚实标注；不做 A/B，由用户拿新包去宿主跑，问题回灌再调。
todos:
  - id: t0-openspec-change-first
    content: T0 先立单一 OpenSpec change `efficiency-first-closure`，strict validation 通过后再动代码。delta 至少覆盖：harness-gates（执行通道三态、checkpoint 注入、执行键复用、报告机器生成、revalidate、漂移分级）、correction-routing（删除 touched_layers 对账与 Stop 拦截、feature correction 不再持久化状态、adhoc 保留 base_commit）、verifier / feature-artifact-layout（材料按 manifest 读取面、状态 completed_with_prior_review、输出短表、回执退出闭环输入、notes.md）、visual-diff / quality axes（量测三轴、release block 不由 geometry 解除、视觉缺口不阻止功能完成）、goal-mode-skill（native /goal 薄 driver + phase executor 与 GoalPhaseRuntime 互斥两入口，修改"attended 必须由 GoalPhaseRuntime 推进"）、agent-adapters（phase-executor 模板登记）、runtime-policy（completion_status 含 unsupported_gap / not_reverified 投影）。每条 REMOVED/MODIFIED 写清替代物。回归：`npm run openspec:validate` strict。
    status: completed
  - id: t1-correction-hardpath-delete
    content: T1（D4）删除 correction 硬路径：check-phase-completion.mjs:715-781 的 evaluateCorrectionGate/buildCorrectionBlockReason 与 :841-850 拦截分支整段删除（逃生阀不变）；harness-runner.ts 的 `--correction-check` 连同 correction-commands.ts 里的 touched_layers 对账、correction-layer-reconcile.ts 一并删除，不保留 exit 0 只打印的僵尸命令；feature correction 不再写 `.current-correction.json`（`--correction-init` 对 feature 路径删除，只保留 `--adhoc-correction` 依赖的 base_commit 状态，且该状态仅由 adhoc 入口读写）；correction-routing.ts 的 classifyCorrection/mapCategoryToChainPhase 保留供 assess/repair_candidates 责任路由。同步 AGENTS.md.template §4.0 修正三问段落（三问只用于归责与重验清单提示，不再有收口命令）、runtime-policy.ts / feature-track.ts 中对 correction-check 的引用。放弃：未声明层的改动不拦。回归：correction-c5-full / correction-check-fail-closed 删除或改为"命令不存在"断言，hook-stale-state 加"correction state 存在也放行 stop"，correction-routing.unit 不变，adhoc-correction 现有单测不变。
    status: completed
  - id: t2-diagnostics-frontload-and-channel-tristate
    content: T2（D8/D9）失败前移 + 判词给改法 + 通道三态 + 已知边界表。（a）派生期与静态 BLOCKER，detail 固定含 TC、step index、实际形状、期望形状、直接改法：① `STEP-P0-IDENTITY`——P0 checkpoint 的 required/forbidden id 缺少形状恰为 `{"wait_for"|"wait_gone":{"by_id":<id>,"timeout":N}}` 的身份断言时，由 T3 注入补齐；注入无法唯一定位时才报 BLOCKER（列出候选 step 与原因）；带 visible/enabled 等谓词的断言保留为 UX 断言，不算身份、也不报错；不维护任何约束键镜像表，身份断言以"精确形状"定义；② `STEP-BYTEXT-ORDER`——同 case 内映射到同一 canonical id 的 `by_text` 断言不得先于该 id 的身份断言（p0-semantic-gates.ts:360-388 取首个匹配），注入时自动放在其前，无法自动时报"把 step N 的 by_text 移到身份断言之后"；③ checkpoint action 绑定：p0-semantic-gates 对 `scroll`/`swipe` 这类 trace 里 selector 为 null 的动作，按步骤种类与顺序定位后用其后的身份断言（post-state）绑定，不再要求 selector；真实用户动作是 scroll 就保留 scroll，禁止为过门禁改成 touch；只有 `assert_visible` 这类非动作值在 check-spec 静态报"checkpoint.action 不是动作：指定触发动作或改为 required_element_ids 断言"；④ 执行通道三态——`executable`（正常执行）、`unsupported_gap`（需求保留、当前工具无执行能力；成立条件必须由机器证明：TC 的 gap_reason 引用 capability-resolution 中状态非 resolved 的 capability id，或引用固定枚举的无原语类别 system_settings / perf_sampling / memory_sampling / resource_variant / data_injection / external_precondition；凡 Hylyre 现有步骤原语能表达的 TC 写成 manual 一律判 invalid_test，防止难写的 P0 被随手改 manual；gap 不算 FAIL、不算 PASS、不阻止普通开发完成，但**留在原始总分母**：p0_coverage_integrity 输出 P0 total / verified_pass / unsupported_gap / failed / verified_coverage 五个数，completion_status 写 COMPLETE_WITH_P0_GAPS，报告结论与 summary 显著披露）、`invalid_test`（写法错误：composite 身份断言、顺序错、checkpoint 不可绑、未登记 provider id、可执行却写成 manual；跑机前必须修）；execution-channel-evidence.ts:29-37 的恒 fail-closed 改为按上述条件判三态；不再把"改 hylyre 或回 spec 出范围"当默认解法。（b）BLOCKER 判词全量审计：check-testing.ts 89 处、check-spec.ts 28 处、p0-semantic-gates.ts 6 处、upstream-verdict-gate.ts 2 处的 suggestion 逐条改写；本次命中的必改清单：p0_coverage_integrity（逐条未覆盖 AC → TC/step/原因）、hylyre_selector_runtime_gate（删"疑似回填冒充"，改"step N request.kind=composite，去掉 <键> 或补身份断言"）、visual_diff pending/warn-无-must_fix（明说 warn 必须填 must_fix 或判 pass 记 minor）、testing_channel_evidence_obligation（改三态话术）、report_trace_reconciliation（列失配格）、upstream_verdict_gate 与 evidence stale（changed_paths 翻译成"重跑 <phase>"）、derived-plan 新鲜度（"重新派生"）。测试不做"含路径/step/改为即合格"的形式断言，改为对上述每条门禁用真实 fixture 断言输出了正确根因与改法。（c）Hylyre 已知边界表写进 profiles/hmos-app/skills/device-testing/profile-addendum.md 与 hylyre-planned-step-fields.md，措辞按版本/能力限定、不写永久绝对事实：wait_for 超时=失败（0.5.x driver 观测语义）、scroll_to 只向下、assert_toast 在当前版本不可用改 wait_for by_text、driver/UI 树开销未校准不能直接当应用时延（本宿主实测 0.8–1.0s 仅作示例）、帧率/内存在当前 capability 下无采样通道、manual/provider 在当前版本无 PASS 载体、长参考图须按视口裁分屏、capture_completeness 是全局口径。放弃：无。回归：三态 fixture（含 gap 留分母的五数输出、可执行却写 manual → invalid_test）、scroll 绑定 fixture、注入失败报错 fixture、七条门禁根因断言、知识块断言更新。
    status: completed
  - id: t3-checkpoint-injection-internal
    content: T3（D9）P0 身份断言注入为 harness 装载派生计划时的内部步骤，不新增 agent 调用的 CLI：check-testing 把最新派生计划复制进 run 目录时，读 acceptance.yaml 每条 P0 criterion 的 checkpoint 与顶层 test-plan 的 TC↔AC 映射，在该 TC 中 checkpoint action 步之后、任何映射到同 id 的 by_text 断言之前，插入 `{"wait_for":{"by_id":<id>,"timeout":10}}`（required）与 `{"wait_gone":{"by_id":<id>,"timeout":10}}`（forbidden）；已有等价裸断言则不重复；只在 action 与插入位置唯一确定时注入，多个候选时输出 actionable gap（列候选 step）并作 invalid_test 处理，不猜；注入只承担身份绑定，原测试里的 visible/enabled/布局/内容谓词断言全部保留为独立 UX 断言，不得删除；不改 scroll 为 touch；run 目录里的派生计划带注入清单，源文件不改；T2 lint 对注入结果零违规。派生知识块改为"P0 身份断言由 harness 注入，派生只写导航、动作与 UX 断言"。放弃：无。回归：注入幂等与位置、与 by_text 顺序协同、多候选不猜、UX 断言未被删、源文件未改。
    status: completed
  - id: t4-closure-reads-summary-receipt-exits
    content: T4（D1）闭环直接读 summary、script verdict 与 verifier policy，回执退出闭环输入。时序冻结：① harness PASS → base summary 落盘 → 签发 verifier request（若 policy 要求）；② verifier 返回后由当前薄 driver（主会话或 phase executor）调一次轻量 finalize：只读 summary + 已发布的 verifier 报告 + policy，finalize 不重跑 harness、编译或设备；③ verifier 为 advisory/disabled 时 harness 在 ① 直接 closure；不得宣称"首次 harness 一次调用即 closed"当 verifier 尚未返回。回执：不再是闭环输入、不进 evidence manifest 的 freshness hash（phase-evidence-manifest.ts 的 receipt_sha256 与 receipt_changed 判定删除）；删除 agent 手填职责（agent_model、时间、commit、verifier 路径、testing 路径、反假设 checkbox：check-receipt.ts:17-22、:431-446、:985-1068）；如需兼容旧消费者，harness 生成只读兼容投影 phase-completion-receipt.md（机器字段）但不影响裁决；agent 备注移到独立 `<phase>/notes.md`，不进 closure/subject/freshness；Stop hook evaluateState 的 receipt 项改读 summary.closure_status；`--sync-closure` 改为调用同一 finalize。放弃：回执不再是代理自证。回归：16 份 check-receipt 相关单测按新语义改写或删除；时序 fixture（verifier pending 时不 closed、返回后 finalize 不重跑 harness、advisory 直接 closed）；hook-stale-state 更新。
    status: completed
  - id: t5-report-machine-generated
    content: T5（D5）测试报告整份由 harness 生成，agent 观察进 notes.md。新增 profiles/hmos-app/harness/test-report-writer.ts：从权威 run 的 trace.json + device-test-timing.json + build/install/run meta + gaps 台账（T2 三态）+ visual-diff.json + visual-debt.json + T10 的 measure-<screen>.json（存在时）+ 当前 quality axes + stability.json（T6 产出，可缺省）生成 test-report-template.md 全部章节——概览、run id、真机流水线耗时、逐 case 状态与耗时、稳定性（缺省时写"未生成"）、缺陷清单（失败 case 的 step、断言类型、期望/观测事实机器填；视觉 defects 逐屏列出）、缺陷统计、通过率（含 P0 total / verified_pass / unsupported_gap / failed / verified_coverage）、gaps、结论——结论按轴确定性派生：功能 PASS/FAIL、交互稳定性 PASS/FAIL/UNKNOWN、视觉几何 PASS/FAIL/UNKNOWN、视觉内容 CHECKED/UNKNOWN、视觉样式 CHECKED/UNKNOWN、已知缺口清单、总体 COMPLETE / COMPLETE_WITH_GAPS / FAILED；视觉不完整不阻止功能完成，但不得生成只看 trace 就宣称 UX 全部达标的报告；输出必须能被 testing-trace-gates.ts 既有解析器逐格读回；report-only 自动刷新；agent 的补充观察写 `testing/notes.md`，不进门禁、closure、subject、freshness。若为兼容必须保留同文件双区：generated 区是唯一门禁读取面，agent 区不参与裁决，marker 缺失时重新生成而非要求 agent 修。review-report 的「问题统计」表由 check-review 在检查时自动重算并回写（不新增 CLI）。引用与计数 lint 只作 WARN：路径存在性、行号范围、同表格行标识符在 ±3 行内出现；统计表 vs 问题行数 vs 正文"共 N 条"三方一致；新报告优先引用"文件 + symbol"，行号需要时由 renderer 生成。放弃：无。回归：生成→解析往返一致、结论派生规则 fixture（阈值/失败/gaps 三种）、视觉轴 fixture（trace 全过但 visual-diff FAIL → 总体不得为 COMPLETE）、stability 缺省渲染、notes.md 不影响 closure、report-only 刷新、统计自动回写、引用 lint 正反例。
    status: completed
  - id: t6-execution-key-and-stability
    content: T6（D6）真机按完整执行键复用 + 稳定性统计。① 执行键 = sha256(HAP 完整内容摘要 build-fingerprint.ts computeHapSha256Full, 派生计划内容摘要（含 T3 注入后的 run 副本）, 设备身份 + 当前可得的系统/显示环境, reset mode 与 cold-start/续会话区别, Hylyre/profile/provider/tool config 版本, 执行 flags 规范化串)；device-test-run.meta.json 写 `execution_key`；顶层 test_plan_sha256 保留为审计信息，不再决定是否操作设备（native-trace-binding.ts:214 退出判据，check-testing.ts:2078 派生计划 mtime 新鲜度改为 TC 表内容 sha 比较，标题/措辞/版本号不触发跑机）。② 复用规则（check-testing.ts:4945 shouldRunDevicePipeline）：用户明确要求 fresh 不复用；明确要求 N 轮稳定性必须真实执行 N 次；同键出现更晚失败时不得复用更早成功；取最近 eligible attempt 而非任意历史成功；trace/timing 必须完整；参考图或 ui-spec 改变但 HAP/交互计划未变时复用设备截图重新比较，不重跑交互；唯一显式逃生口 `--force-device`。summary 写 `device_run: reused_by_execution_key` + 被复用 run id。③ 稳定性生成为 testing 内部步骤（不公开 CLI），产出 stability.json 后调用 T5 writer 刷新报告（T5 先落地且 stability 可缺省，T6 依赖 T3 与 T5）：按完整执行键分组，同组包含成功与失败的全部 eligible attempt，含设备环境与复位方式，只在当前需求允许范围内计数；输出每 TC 的同键轮数、一致轮数、首个分歧 step；要求两轮时必须两次真实执行；执行策略为"一次全量 + 相关 TC 一次定向冷启动复验"，不再次全量。④ 与 T7 口径一致：testing 的 verifier 材料含机器生成报告，报告随 trace 变、不随 notes 变，故 report-only 不会凭空换 subject。放弃：同输入下 flaky 不再被"每次都真跑"顺带暴露。回归：同键复用 / 换 HAP / 换派生计划 / 更晚失败不复用 / fresh 与 N 轮真跑 / 截图复用重比 / 版本号不触发 / 稳定性含失败轮与分歧定位，九条 fixture。
    status: completed
  - id: t7-verifier-once-slim-crosscheck
    content: T7（D3/D12）verifier 每份材料一次 + 瘦身 + 首轮语义、后续核对。① subject 材料 = **pre-verifier material view**，在 harness Step 4 签发 request 时计算：phase 实际输入文件哈希（复用 phase-evidence-manifest.ts 的 spec-loader REQUIRED/OPTIONAL 文件解析表与哈希函数）+ phase 主产物哈希 + verifier 实际读取的源码/trace/visual-diff/机器报告哈希 + phase rule 哈希 + verifier prompt 模板哈希 + gate_fingerprint；明确排除 verifier report 自身、summary 的 closure/时间等运行字段、receipt、ai-prompt 生成时间、merged report；不复用 finalize 后才生成的完整 manifest aggregate（它含 summary 与 verifier report，会形成 subject→report→manifest→subject 环），也不新增落盘 manifest。这样 acceptance 变会影响 coding/testing、产品源码变会影响 review、trace/visual-diff/机器报告变会影响 testing；不再哈希 ai-prompt.md 字节（verifier-request.ts:61 与 harness-runner.ts:1955-1962 注释一并改写，删"时间戳换 subject 合法"）；材料未变 subject 不变，已发布的 verifier.report.<subject>.json 自动复用；verifier 复用判定为 finalize 内部逻辑，不新增 CLI；显式重审走既有 request 路径再投一次。② 材料变了但 driver 决定不重跑 verifier 时允许完成，summary 与 completion_status 写 `verifier: completed_with_prior_review` + `current_material_not_reverified`（列材料差异文件），不得写成"已 PASS"；BLOCKER 只保留"本阶段从未有过 PASS 报告且 policy=required"。③ report-generator.ts:220 assembleAIPrompt 瘦身：script_report 只嵌非 PASS 的 checks（PASS 项仅列 id）；下游阶段的 spec.md/plan.md 改路径 + 章节目录；源文件由"内联 30 个"改路径清单；同一 fixture 瘦身后 ≤ 原 50%。④ 检查项分层：首轮保留 UX 语义检查（需求到 acceptance 的覆盖、主交互与异常状态、视觉参考的结构语义、明显遗漏、引用与原文一致性），删除无不动点的可读性打磨项（overview_clarity/user_scenario_specificity/feature_description_actionable/ui_component_terminology/context_exploration_sufficiency 与四条 behavior_*，review/testing 同类）；后续轮只复核既有问题与直接变化，不扩新建议；证据不足允许 WARN/UNKNOWN 不阻断，不强迫二值。⑤ 输出瘦身：verify-*.md §七改为"终态块 + 条目表（check id / 判定 / 一行证据 / 改法）"，无散文章节；verifier.md 同步；record-verifier-report.mjs 只读终态块，不变。⑥ 各 phase SKILL 与 verifier.md 加"WARN/UNKNOWN 不在本轮修，记 notes.md；只有 BLOCKER FAIL 才修后重审"。放弃：材料变化后可不复审，但状态如实标注；可读性类问题不再由 verifier 提出。回归：verifier-evidence-identity / receipt-slim / e2e-spec-requirement-closure 中依赖"prompt 字节换 subject"的用例改写；新增"manifest 材料不变→复用""acceptance 变→coding/testing subject 变""not_reverified 状态投影""瘦身体积""输出格式解析"五条。
    status: completed
  - id: t8-revalidate-check-executor-and-drift-tiers
    content: T8（D2/D7）快速 revalidate 与 UX 漂移分级。① `--revalidate --feature <f> [--from <phase>]` 只是检查执行器，不拥有阶段推进、不建第二套状态机：用 upstream-verdict-gate.ts resolveUpstreamPhaseChain 取链，recomputePhaseEvidenceStaleness 找 stale 输入，按链序对每个 stale 阶段复用现有 checks 运行必要检查（不重新生成整套产物、不要求回执、不默认重跑 verifier），PASS 即调 T4 finalize，结果标 `script_revalidated` / `semantic_not_reverified`，FAIL 即停并打印该阶段 blocker 与改法；不宣称完整语义再审；`--sync-closure` 保留。② UX 源码变化按风险分级复核，写进 closure-attestation 的 detail 与 skill：文档/报告/备注变化不重审产品；测试代码变化只跑相关测试；导航/状态/业务交互变化做一次 scoped diff review；布局/字号/颜色/资源变化做一次真机截图或几何量测复核（T10）；多类同时变化做一次最终合并 diff review；不无条件重走 review→UT→testing。review_closure_attestation（closure-attestation.ts checkReviewClosureAttestationGate，check-testing.ts:5525）BLOCKER→按上述分级给出所需复核类型，未做时 WARN 并如实标注；ut_no_src_mutation 不再永久 BLOCKER：UT 阶段改了生产代码则重新归类为 coding change，进入对应编译/测试，不悄悄忽略。③ 已闭环 feature 的修正 = 直接改 SSOT/代码/测试输入 → 一次 `--revalidate` → 按分级补一次复核 → 完；不重新进入六阶段流程，不重写 spec/plan/review 全文，只在相应产物追加"修正记录"小节；不为修正另建 feature 或状态（AGENTS.md.template §4.0 + agents-entry-detail §5 同步）。放弃：上游改动后下游语义一致性默认不重审但如实标注；review 后小改动按分级而非全量重审。回归：链上 fixture（spec 改动→下游 stale）一条命令全绿并 closed 且带 semantic_not_reverified；FAIL 中断；五类漂移各对应一种复核要求；ut 改生产代码归类 coding。
    status: completed
  - id: t9-phase-executor-context-diet
    content: T9（D11）上下文减负，唯一执行 owner。① Claude 原生 /goal 路径：主会话是薄 driver，每个阶段最多一个 phase executor 子代理，executor 负责该阶段产出与自检（跑 harness、投 verifier、调 finalize），主会话只投递入口、收回 summary 路径与终态块；`--revalidate` 只是检查命令；Maison /goal-mode/GoalPhaseRuntime 是另一入口，不与该路径同时推进同一任务；T0 已同步修改 goal-mode-skill spec 中"attended 必须由 GoalPhaseRuntime 推进"的表述为两入口互斥。② 新增 agents/claude/templates/agents/phase-executor.md（tools 全量），adapter.yaml agents 列表登记；最小输入：原始需求路径、acceptance/ui-spec/参考图路径、当前改动文件、当前 blocker、已接受 gaps、上一阶段 summary 路径、对应 Skill 路径；不传历史，子代理按需读取；goal-condition.md 与 agents-entry-detail.md §4.1/§5、goal-mode SKILL 同步"同一会话内不连续执行两个阶段"。③ verifier/子代理纪律写进 agents-entry-detail 与 goal-mode SKILL：同步等待结果或只做与其结果无关的工作；禁止 sleep/轮询/后台等待器；verifier 未完成时不得修改其输入材料。④ harness 输出：`--failures-only` 已是默认（harness-runner.ts:1101），不重复做；在现有 summary 输出末尾加一行 `NEXT:` 动作指令（如"修 <check> 见 <path>，然后重跑本 harness"），不新增状态。⑤ agent-behavioral-principles.md 加"框架/vendor 事实以当前 addendum/契约为准，记忆只作搜索线索，不能作裁决依据"。放弃：无。回归：模板存在性与 adapter 登记单测、NEXT 行断言、文档 lint、goal-mode-skill spec 与 SKILL 措辞一致。
    status: completed
  - id: t10-visual-measure-facts-only
    content: T10（D10）视觉量测只承担测量，不承担完整视觉裁决。① `--measure --feature <f> [--screen <id>]`：复用 layout-oracle-check.ts 的布局树读取（layout-<screen_id>.json）、visual-diff-check.ts:2174 T8 几何不变量、visual-diff-capture 的截图与 readImageDimensions，对 ui-spec 声明元素输出可复算事实：bounds、卡片边界、间距/行距、重叠、参考图与设备的差值（px 与按行距反算的 vp）、颜色采样；结果写 device-screenshots/measure-<screen_id>.json 并打印人可读表；对 visual_reference_viewport 拆分后的每屏独立量测。② 输出拆三轴：geometry PASS/FAIL（阈值优先取 ui-spec 元素级 tolerance，缺省时 profile 默认只作 advisory，不用统一 12px）、content CHECKED/UNKNOWN、style CHECKED/UNKNOWN；visual-diff.json 的 defects[].note 由量测事实自动填充，verdict 仍由 agent 或 provider 给出，不自动覆盖；measurement 不自动等于 pixel_1to1 PASS；geometry PASS 不自动解除 visual/release block；不自动改写 ui-spec 真源，最多输出 bbox 建议供 spec 使用（不做 --ui-spec-measure-from-reference 写盘）。③ 无 provider 时：geometry 由机器得出结论；agent 实际看图后给普通视觉判断，不要求 region_attest/critic-receipt 类证明（对无 provider 场景改 SKIP）；content/style 未验证则披露 gap；功能完成不因视觉证据不完整被阻止（现有 quality-axes.ts:166-171 visual/asset 不阻断推进保持），release_readiness 如实保持。放弃：视觉判定降为几何事实 + 人眼；像素级色彩/字形差异不在量测范围。回归：扫描法在参考图与截图 fixture 上的带定位、差值填充、三轴状态、元素级 tolerance 优先、不改 ui-spec、不改 verdict、无 provider 场景 SKIP。
    status: completed
  - id: t11-docs-migration-final-verification
    content: T11 收口：保留 codex 2026-09-03 15:02 已落盘的四条总原则改动（AGENTS.md、docs/overview.md §1.2.1、agent-behavioral-principles.md、AGENTS.md.template §2.1），不重写；RELEASE-HIGHLIGHTS-v3.0.0.md 不属于本 plan，不改。同步 skills/reference 各 workflow-detail、AGENTS.md.template §4.0/§5.1 闭环判据、goal-mode-operations.md、MIGRATION.md（回执退出闭环、correction 命令删除、--revalidate / --force-device / --measure 三个新入口、执行通道三态、verifier 复用与状态、notes.md、phase executor）。整批收口验证：`cd harness && npm test` 全绿 + typecheck + `npm run openspec:validate` + `npm run release:check-plans` + LF/`git diff --check`。宿主验证不由实施代理发起：用户拿新包去宿主跑 bc-openCard-1 同类修正，回灌指标：主代理轮数、上下文构成（自写脚本/读源码/verifier 回传字符）、harness 次数、verifier 次数、真机轮数、挂钟，与记忆 bc-opencard1-0902-session-cost 基线比；对照第六节完成判据逐条核。
    status: completed
  - id: t12-host-repeat-checkpoint-fix
    content: 宿主回灌 TC-012：修复同一 checkpoint 重复触发的身份注入与 P0 证据消费；按动作顺序逐次绑定且不跨触发区间借断言，保留完整导航和 UX 断言、幂等复用裸 by_id，真正歧义仍诊断；同步现有 OpenSpec 和提示，不新增 CLI/schema/manifest/状态机。
    status: completed
  - id: t13-host-repeat-checkpoint-verification
    content: 重复触发回归覆盖完整进入→返回→再次进入、第二次失败、不跨区间、幂等与 UX/源文件不变、单次/无动作/真正歧义；typecheck 与目标测试后一次完整 harness npm test、OpenSpec strict、git diff --check，完成后停 review，不改宿主/跑真机/提交/打包。
    status: completed
---

# 3.0.0 效率优先：闭环仪式减法 / 判词给改法 / 派生与量测机器化 / verifier 一次化与核对化 / 回执与报告机器化 / 真机证据按输入复用 / 上下文减负（07a41ec6）

状态：**v3 待终审（2026-09-03，codex 二轮 review 3 处必修 + 1 处依赖修正已吸收；codex 声明修完即通过、不再扩面）。** 实施分工按既定：codex 实施，Claude 审 diff；用户拿新包去宿主实跑作为验收，不做 A/B。

关联资产：

- 成本基线：记忆 `bc-opencard1-0902-session-cost`（本机 Claude 日志复算，宿主文件未读）；总原则：记忆 `design-principle-efficiency-first`、docs/overview.md §1.2.1 四条总原则（codex 已落盘）
- 上游 plan：[Maison优化项_能力查表与视口口径与case复位_b3d7e5a1](./Maison优化项_能力查表与视口口径与case复位_b3d7e5a1.plan.md)（provider 计划期存在性查表已完成；视口前置门保留）
- 邻接 plan：[bc-openCard复盘根治_人签时点投影与closure死锁与视觉身份熔断_f3a8c6d2](./bc-openCard复盘根治_人签时点投影与closure死锁与视觉身份熔断_f3a8c6d2.plan.md)（goal-runner 路径；本 plan 不碰 goal-runner，只在 T0 把两入口写成互斥）

---

## 零、决策纪要

### D0 原则冻结（用户 2026-09-03，codex review 补第 4 条）

1. 效率优先，目标是更快、更好地完成开发，不要求 100% 无问题。
2. 产品功能和 UX 准确性优先于过程证据完整性。
3. 缺 verifier、receipt、签名、审计证明默认不阻止普通开发完成。
4. 可以不复审、可以复用、可以带已知缺口完成，但必须诚实标注；不能把"未重新检查"描述为"当前内容已 PASS"。
5. 不做 A/B、不造基准系统；新版本完成后由用户在真实宿主继续测试并回灌。
6. 一个 plan；减少重复 CLI、状态和执行 owner。公共入口只留 `--revalidate`、`--force-device`、`--measure`，其余机器化步骤全部内部化。

### 全局判断：既提效又提准的，都是"代理在手工做机器活"

主代理上下文里，代理自写命令与脚本 80 万字符、读文件返回 64 万字符，合计占六成；其中 212 次读 framework TS 源码反推门禁判词，209 个脚本处理 trace/report/timing，66 个量像素。它们既是 910 轮与 4.36 亿缓存回放的主体，也是错误来源：8 个派生值错误来自手写报告脚本，P0 覆盖 0/16 来自读源码得出的错误信念。T2/T3/T5/T10 收回这些活，是效率与准确率同时提升的项；T1/T6 是纯效率项；T7/T8 是拿一部分准确性换效率，第一节表中写明。

### D1 闭环读 summary，回执退出输入

本次 check-receipt 跑了 41 次；回执要代理抄模型、时间、commit、路径并勾反假设条款，抄错即 BLOCKER，闭环后补一句又让 manifest 判 stale。closure 只读 summary、script verdict 与 verifier policy；回执不再是输入、不进 freshness；如需兼容只生成只读投影；备注进 notes.md。时序：harness PASS → 签发 verifier request → verifier 返回 → 薄 driver 调一次轻量 finalize；advisory 时 harness 直接 closure。

### D2 下游重闭 = 检查执行器；修正即单元

correction-routing.ts 文件头写"重验≠重做：revalidate = 脚本门禁"，但闭环机制把它顶掉了。`--revalidate` 只运行 stale 输入对应的必要检查，不生成产物、不要回执、不默认重跑 verifier，结果标 `script_revalidated / semantic_not_reverified`；已闭环 feature 的修正就是改完跑一次它，再按分级补一次复核。

### D3 verifier 每份材料一次

harness-runner.ts:1955-1962 的"时间戳换 subject 合法"是本次 34 次 verifier 里至少 12 次的直接来源。subject 改为签发 request 时计算的 pre-verifier material view（输入文件 + 主产物 + verifier 实际读取面 + rule + 模板的内容哈希，排除 report、summary 运行字段、receipt、时间），材料不变自动复用；材料变了可不复审，但状态是 `completed_with_prior_review` + `current_material_not_reverified`，不是 PASS。首轮保留 UX 语义检查，后续只核对；无不动点的可读性打磨项删除；证据不足 WARN/UNKNOWN 不阻断。

### D4 correction 硬路径整个删除

三问表达不出 {coding, ut}，对账又把 ohosTest 归 ut，Stop hook 的 correction 拦截排在逃生阀之前。删对账、删拦截、删僵尸 `--correction-check`、feature correction 不再持久化状态；三问分类只留给责任路由；adhoc 路径保留自己的 base_commit 状态。

### D5 报告整份机器生成

11 个代理错误里 8 个是派生值失效。test-report.md 由 harness 从 trace/timing/meta/gaps/visual-diff/visual-debt/量测/quality axes/stability 整份生成，结论按功能、交互稳定性、视觉几何、视觉内容、视觉样式、已知缺口逐轴确定性派生，总体 COMPLETE / COMPLETE_WITH_GAPS / FAILED；功能 20/20 而画面仍错的情况不得被写成 UX 达标；agent 观察进 notes.md 不入门禁；review 统计由 checker 自动回写；引用与计数 lint 只作 WARN 提示。

### D6 真机执行键

执行键含 HAP 摘要、注入后派生计划、设备身份与显示环境、复位方式、工具链版本、flags；同键复用最近 eligible attempt，更晚失败不被更早成功覆盖；fresh 与 N 轮稳定性必须真跑；参考图/ui-spec 变但 HAP/计划不变时复用截图重比；文档措辞与版本号不触发跑机；唯一逃生口 `--force-device`。稳定性按同键含失败轮统计，"一次全量 + 相关 TC 定向冷启复验"。

### D7 UX 源码漂移按风险分级

3 个常量的布局修复让 review 重审两轮。文档不重审产品；测试代码只跑相关测试；导航/状态/业务交互一次 scoped diff review；布局/字号/颜色/资源一次真机截图或几何复核；多类一次合并 diff review。UT 改生产代码归类为 coding change 进编译/测试，不悄悄忽略。

### D8 判词给改法；通道三态；已知边界按版本限定

212 次读源码全是为了弄清 BLOCKER 要改什么。每条 BLOCKER 给 TC、step、实际形状、期望形状、改法，用真实 fixture 断言根因而不是形式检查。执行通道 executable / unsupported_gap / invalid_test：工具不支持的需求允许带缺口完成并披露，但 gap 留在原始总分母、不算 PASS，报告输出 verified_coverage；unsupported_gap 必须由 capability-resolution 或无原语枚举证明，Hylyre 能表达却写成 manual 的判 invalid_test；写法错误跑机前必修，不再一律"改 hylyre 或出范围"。Hylyre 边界按当前版本/能力写，不写永久事实。

### D9 身份断言由注入提供，UX 断言原样保留；scroll 不改 tap

`visible:true` 让 request.kind 变 composite、P0 覆盖归零，根因是把 UX 谓词断言当身份证据。身份由 harness 在装载派生计划时注入的精确形状裸 by_id 提供，不维护约束键镜像；代理写的谓词断言保留为 UX 断言。scroll/swipe 在 trace 里 selector 为 null，绑定改用 post-state，不为过门禁篡改交互。

### D10 视觉量测只出事实

66 个像素脚本、三源不一致、代理手填"VL"裁决。`--measure` 出几何事实与三轴状态，阈值取 ui-spec 元素级 tolerance；不改 ui-spec 真源、不覆盖 verdict、不等于 pixel_1to1 PASS、不解除 release block；无 provider 时机器给 geometry、人眼给普通判断、未验证的轴披露 gap，功能完成不被阻止。

### D11 上下文减负，唯一 owner

4.36 亿缓存回放 = 910 轮 × 约 48 万；后期失误明显多于前期。Claude /goal 主会话做薄 driver，每阶段一个 phase executor，与 GoalPhaseRuntime 入口互斥；禁止自建等待器；verifier 未完成不改其输入；框架事实以 addendum/契约为准。

## 一、放弃的准确性（逐项）

| 项 | 放弃什么 | 为什么可接受 |
|---|---|---|
| D1 | 回执不再是代理自证 | 自证字段全部可机器得出，手抄只制造 stale |
| D2 | 上游改动后下游语义一致性默认不重审 | 脚本门禁保留 AC 追溯与结构；状态如实标 semantic_not_reverified |
| D3 | 材料变化后可不复审 | 状态标 current_material_not_reverified，不冒充 PASS；可读性类改由 lint 提示 |
| D4 | 未声明层的改动不拦 | 该拦截结构上无法通过，零收益 |
| D6 | 同输入下 flaky 不被顺带暴露 | fresh 与 N 轮仍真跑，且稳定性含失败轮 |
| D7 | review 后改动按分级而非全量重审 | 每类漂移仍有一次对应复核 |
| D8 | 工具不支持的需求带缺口完成，含 P0 | gap 留在总分母、不算 PASS，verified_coverage 与 COMPLETE_WITH_P0_GAPS 显著披露；成立须机器证明 |
| D10 | 视觉判定降为几何事实 + 人眼 | 本次"VL 裁决"本来就是代理手填；量测数字更可信；缺口披露 |

D5/D9/D11 无放弃项。

## 二、预期效果（对本次基线）

| 指标 | 基线 | 预期 |
|---|---|---|
| verifier 调用 | 34 | 6–8（每份材料一次），单次 prompt 减半、回传缩到条目表 |
| check-receipt 调用 | 41 | 0 |
| harness 调用 | 143 | 30–50 |
| 真机全量轮 | 10 | 2–3 全量 + 定向复验 |
| 代理自写脚本 | 411 个 / 80 万字符 | 报告、量测、稳定性、断言注入四类归零 |
| 读 framework 源码 | 212 次 | 判词给改法后接近零 |
| 主代理轮数 | 910 | 大幅下降；阶段子代理化后单轮回放上下文亦大幅下降；具体由宿主回灌 |

## 三、非目标

- 不碰 goal-runner / GoalPhaseRuntime 的实现；只在 T0 把两入口写成互斥。
- 不做 A/B、不建错误种子回归、不做 verifier 增量 diff 语义。
- 不引入视觉 provider、VL 模型或 OCR 新通道；不自动改写 ui-spec；不用 geometry 解除 release block。
- 不新增 receipt、签名、人工确认、状态机、第二套阶段推进 owner；公共 CLI 只增三个。
- 不修改 codex 已落盘的四条总原则文本；不改 RELEASE-HIGHLIGHTS-v3.0.0.md。

## 四、提交边界（分段用，不是提交授权）

T0 → T1 → T2 → T3 → T4 → T5 → T6 → T7 → T8 → T9 → T10 → T11，每个 todo 一个提交。依赖：T3 依赖 T2（三态与注入失败语义）；T5 依赖 T2（gaps 台账）与 T3（注入后计划），stability 输入可缺省，T5 不依赖 T6；T6 依赖 T3 与 T5（产出执行键与 stability 后调用 T5 writer 刷新）；T7 依赖 T4（finalize 内的复用判定）；T8 依赖 T4 与 T7；T9 依赖 T4 与 T7（executor 调 finalize 与 verifier 纪律）；T10 独立，T5 在 measure 文件存在时消费它。

## 五、验证策略（按比例）

- 每个 todo 只跑对应单测；生产逻辑跨面修改时加 typecheck 与目标测试；不在每个提交前重复完整 `npm test`。
- 整批收口时：`cd harness && npm test`、typecheck、`npm run openspec:validate`、`npm run release:check-plans`、LF/`git diff --check`。
- 宿主验收不由实施代理做 A/B；新发布件完成后由用户在真实宿主运行需求，再按反馈调整效率/准确率边界。

## 六、完成判据

1. 产品码与 ohosTest 同轮修改不再导致 correction 永久拦 Stop。
2. 不可执行通道、composite 身份断言、by_text/by_id 顺序、checkpoint 绑定问题在设备操作前给出精确改法。
3. P0 身份断言由 harness 注入，UX 可见性/状态断言没有被删除。
4. 一次全量真机后，报告和回执无需 agent 手写脚本。
5. 相同执行键的报告/文档修订不重新跑机。
6. 用户要求的 fresh 和稳定性轮次仍真实执行，失败轮不会被过滤。
7. verifier 每份稳定材料最多一次，PASS 后不追非阻断 WARN。
8. 跳过后续 verifier 时如实记录"当前材料未复审"，但不阻止普通开发完成。
9. UX 源码改变只触发一次与风险对应的复核，不重走六阶段。
10. 几何量测不冒充完整视觉验真；视觉缺口不阻止功能完成。
11. Claude /goal 主会话保持薄上下文，不再自建等待器或累积全部阶段历史。
12. 没有新增平行真源、僵尸状态和重复 phase owner。
13. unsupported_gap 留在原始总分母，报告输出 verified_coverage 并显著披露；每个 gap 都有 capability 或无原语类别的机器证明，可执行却写成 manual 的判 invalid_test。
14. 机器测试报告按功能、交互稳定性、视觉几何/内容/样式逐轴出结论；视觉未验证不写成达标。

## 七、实施记录

### 2026-09-04：实施与 review 收口

- T0–T11 共 12 个 todo 均已 completed；各轮 review 必修已处理，用户确认「没有其他意见」。本 plan 的框架实施与 review 已收口；前文「v3 待终审」保留为原设计时点记录，不代表当前实施状态。
- 最终范围收敛：receipt 退出新闭环输入，磁盘 summary 为 Stop 闭环真源；summary 提交后同步 state、best-effort 生成 receipt，投影失败不改变闭环。3.0.0 只复用最新真实 attempt 的同键成功证据，不支持跨 key 回捞旧 run，不新增 selected-run pointer。
- TC 新鲜度覆盖行为字段，复用既有 derive-hint-from-plan.json 保存源 TC 基线；正常派生入口默认先写 canonical hint 再生成派生计划，不再依赖首次 harness 失败建立基线。known gap 留分母并诚实披露；active 无 per-TC producer 的 provider 在设备动作前判 invalid_test。报告由 harness 生成，agent 只读结果并补 notes.md。
- 验证记录：完整 npm test 执行一次，typecheck 通过，309 个 unit suite 中 3817/3818 通过；唯一失败为 receipt-path-reconcile 旧夹具缺当前 summary 字段，补齐后该套件定向 4/4，通过结果复用，不重复全量。随后单独执行 test:fixtures，46/46 通过。最后一次首次派生接线返修执行 efficiency-first-t2 定向测试，9/9 通过，typecheck 通过；OpenSpec strict 最近一次 31/31 通过；相关文件 LF 与 git diff --check 通过。
- 本次记录仅补充执行结果，不改原目标、设计或验收正文；记录后只做 plan 校验与 diff 检查，不为文档重复跑测试。
- 宿主实跑尚未执行：OpenSpec tasks.md 的 11.3 保持未完成，由用户自行打包、集成发布件到宿主并回归反馈；当前不归档 efficiency-first-closure，不将框架侧测试结果冒充宿主验收。

### 2026-09-04：宿主验证完成与最终归档

- 用户明确确认「宿主验证已经完成了。全部关闭就行了」；据此将 OpenSpec 11.3 标为完成。本条更新前述待宿主验证状态，未由实施代理代跑或推断宿主结果。
- T0–T13 共 14 个 frontmatter todo 全部 completed；TC-012 重复 checkpoint 回灌修复已提交为 d6e55321，保留连续导航与 UX 断言，按触发区间逐次注入及验真。最近一次完整框架验证为 typecheck 通过、309 个 unit suite 3820/3820、fixtures 46/46；定向回归 50/50。
- efficiency-first-closure 全部 26 项任务完成，8 组 delta 已同步至 openspec/specs；change 已归档至 openspec/changes/archive/2026-09-04-efficiency-first-closure/。
- 本 plan 的实施、review、宿主验证和规格归档均已闭环；原设计正文保持不变。本次归档只做 OpenSpec strict、plan 与 diff/LF 校验，不重复运行代码测试。
