---
name: 盲档视觉根治 — 负面裁决传播、盲档地板抬升（可执行组件+确定性度量）与多轴完成语义
version: 3.0.0
# 版本说明：窗口不 bump（用户控版本）。中型演进：治「盲宿主上视觉链路停摆、产出线框级 UI、
# 负面裁决被总括 PASS 吞掉、视觉债务被完成状态掩盖」。
# v2（2026-07-17）：吸收 codex 一轮审查——新增 P0-A verdict lattice、P0-B role-aware、
# P0-C 可执行 UI kit、P0-D 复用档位三态禁自动降级、P1-E JSON SSOT、P1-G 可复现验收。
# v3（2026-07-17）：吸收 codex 二轮 + cursor 二轮——P0-A 修 needs_fix/needs_human 状态投影冲突
# + 多轴 schema/适用性矩阵/旧 verdict 兼容投影；P0-C 定案路线 A + uitree 锚点事实；
# P0-B 补 role 交叉对账/占位按角色/crop 左移禁令/阈值两段式；P1-E 修 evidence 语义映射 +
# 复用 visual-rounds-ledger 与既有 nav BLOCKER（HEAD 已档位无关）+ 采集策略与档位解耦；
# P0-D 补人工视觉验收 receipt 债务清偿闭环 + 宿主交互成本预告。
# v4（2026-07-17，定稿）：codex 三轮「方案层面通过」+ cursor 三轮「可以开干」——压入 6 项契约
# 细节：schema 不变量+resolution 入轴+双投影分立/跨阶段 SSOT 链路防 TOCTOU+legacy 1.0 政策/
# crop 禁令收窄（禁执行自证不禁消费可信产物）/kit 目录解析算法+实例语义锚点+gallery fixture/
# rubric 阈值冻结 ≥4/5+结构化 screens/两类确定性信号分立+package digest 身份；
# 新增 P0-0 OpenSpec change 先行。
overview: >
  【事故样本（2026-07-13~16，宿主 claudecodeli + minimax 2.7 / receipts 记 GLM-5.1-Alpha-Auto，
  非多模态，逐阶段 CodeAgentCLI 驱动、非 goal preflight 路径；宿主所用 framework 发布版落后于
  HEAD——如 nav 门禁在宿主侧仍是 WARN，HEAD 已是档位无关 BLOCKER）】bc-openCard 第二轮宿主实测：
  14/14 真机 TC 全 PASS、四阶段全部闭环、test-report「达标可发布」——但 UI 成品是线框级：
  全程零可见图标（23 张 placeholder PNG，设备渲染不可见/疑似空白，待 jimp 像素统计固化为机器证据）、
  无卡片容器/分隔线/chevron/返回键、半模态无关闭钮、按钮呈禁用态淡蓝色。
  对比素材：D:\1.code\对比结果\1-bc-opencard（0-原始需求 8 屏 / 1-优化前 VL / 2-优化后 VL / 3-盲宿主成品 6 屏 + 完整工作目录）。
  【硬约束（用户拍板 2026-07-17）】宿主短期只有 minimax（盲模型），无法绑定/外挂其他模型——
  外置 VL（BYO-VL）路线不考虑。全部措施必须在纯盲档内成立：不可绕过的裁决状态机 +
  可执行 UI 组件 + 确定性像素度量（jimp/OCR/uitree）+ 人工收口。
  【根因链（全部实证；codex/cursor 两轮审查后修正）】
  ① 负面裁决不传播（第一根因）：review 终态「审查结论：不通过 + 3 BLOCKER」（review-report.md:120），
  但 conclusion_with_verdict 只校验「结论与统计一致」（check-review.ts:428），洞⑥
  conditional_pass_closure 只覆盖「有条件通过」分支（check-review.ts:894 结论=不通过 →
  本门禁不适用 → PASS）——review summary.json 落 verdict:PASS/closed 照样闭环推进 ut/testing。
  全库无任何机制消费「不通过」。报告合法性(report_validity)与产品裁决(product_verdict)混用一个 PASS。
  ② 视觉链路停摆的机制拆分（四点分立，勿过度概括）：(a) VL 专属检查合法 SKIP
  （visual_multimodal_parity SKIP，image_input=none）；(b) 确定性采集在场但未启动——宿主版
  visual_diff_capture 因缺 visual-diff-nav.json 拒采且仅 WARN（HEAD 已修为档位无关 BLOCKER，
  check-testing.ts:2194 t7）；(c) 非 pixel_1to1 档视觉检查降 WARN 不阻断 + 完备性/几何检查
  多处 pixel-only 早退（capture-completeness-check.ts:87/216/732「仅 1:1 下强制」）；
  (d) 负面/缺失证据不传播到完成状态（同①）。
  ③ fidelity 意图检测未覆盖本路径：goal-fakepass-hardening 档位三态（强 pixel 意图+盲 →
  DEFERRED_CAPABILITY_MISSING；含混+有参考图 → await_human_fidelity_tier；--fidelity 只升不降；
  spec.md:48，场景例即 bc-openCard）只在 goal preflight 生效；宿主逐阶段驱动漏检 →
  spec 缺 fidelity_target 缺省 semantic_layout（fidelity-shared.ts:59）→ pixel 硬门禁全体未激活。
  ④ 素材链断裂：22 项素材声明 acquisition:crop（盲模型不可能完成，违反盲档工作法），未验真；
  coding 物化 placeholder PNG 进 media 仅 WARN；设备测试真值=uitree 存在性（TC-002
  「底部5个小图标可见」，Image 节点在=可见）。
  ⑤ 视觉债务被完成状态掩盖：全埋 WARN/soft_advisories，test-report 结论零 caveat。
  【业界对照】不可绕过状态机与多轴 verdict（quality gate ≠ report lint）；component-kit-first
  （可实例化组件+tokens 一起交付）；确定性视觉回归（Storybook Visual Tests / Playwright Visual
  Comparisons）；设计交接=结构化 spec+导出资产（Figma Dev Mode/Zeplin）。
  【目标效果曲线】盲宿主上：P0 后 = 负面裁决必然阻断完成 + 平台标准组件质感 + 素材完整性硬门禁 +
  视觉未验真时完成状态如实（FUNCTIONALLY_COMPLETE_VISUAL_PENDING）；P1 后 = 确定性反馈回路
  收窄可判差距 + 素材问人闭环 + 人工验收 receipt 清偿债务 + 可复现宿主验收。
todos:
  - id: p00-openspec-change
    content: >
      P0-0 OpenSpec change 先行（codex 三轮终审要求：行为变化落规格，不直接只按 plan 改代码）。
      按 openspec/changes/ 既有惯例（先例：goal-fakepass-hardening）新建 change
      blind-visual-hardening：proposal + specs + tasks，规格至少覆盖六域：
      ①phase/product verdict propagation（P0-A 硬线+SSOT 链路）；②summary 1.1 与 completion
      projection（多轴/不变量/双投影/legacy 政策）；③blind asset integrity（crop 收窄禁令/
      role-aware/三态清偿）；④executable UI kit（目录解析算法/锚点算法/三段闭环/gallery）；
      ⑤deterministic visual feedback（两类信号/采集解耦/身份字段）；⑥human visual acceptance
      receipt（冻结阈值/结构化 screens/清偿边界）。specs 写 Scenario 时直接引用本 plan
      证据索引的事故 fixture。
    status: completed
  - id: p0a-verdict-lattice
    content: >
      P0-A verdict lattice 与负面裁决传播（第一优先）。
      【实施顺序收敛（cursor：面偏大，先最小闭环再铺面）】第一切片只做两条硬线：
      (1) review 结论=不通过 → phase BLOCKER FAIL（对齐洞⑥ conditional_pass_closure 纹理，
      补其漏掉的分支；testing 不达标同理）；(2) 跨阶段传播——SSOT 链路（codex 三轮②防 TOCTOU）：
      Markdown 报告只是**解析输入**，harness 解析后落 summary.json quality_axes 为阶段机器裁决，
      receipt/evidence-manifest/fingerprint 负责新鲜度绑定；下游 phase 只消费**新鲜 summary +
      receipt**，不重新解释上游自然语言报告（报告在检查后被改 → 新鲜度失效自然拦截）。
      第二切片再铺多轴字段与 completion 映射。
      【状态投影（codex 二轮 B1：严格复用现行 needs_fix/needs_human 语义，勿把 FAIL 伪装成等人）】
      多轴裁决每轴附 resolution {class: needs_fix|needs_human|external_dependency,
      owner: agent|human|toolchain|external, retry_phase: coding|review|testing|null}；
      映射固定（对齐 verify-feature-completion.ts:99 CleanPassIssueKind 与
      phase-transition-policy.ts:285 resolveGoalRunStatus，不新造状态机）：
      轴 FAIL / 证据 STALE/MISSING 且可重跑 → needs_fix → run PARTIAL（或 phase FAILED）+
      FEATURE_INCOMPLETE；必须人工视觉验收 → needs_human → AWAITING_HUMAN_REVIEW +
      VISUAL_PENDING；能力缺失且禁降级 → DEFERRED_CAPABILITY_MISSING（不可完成）；
      全部必需轴 PASS → CHAIN_SLICE_COMPLETED 才允许验证 FEATURE_COMPLETED。
      人工确认永远不能解除确定性 FAIL。
      【多轴 schema 契约（codex 二轮 B2 + 三轮①：先定契约再动代码，防 split-brain）】
      summary.json schema_version 升 1.1（additionalProperties 保持 false，新增顶层
      quality_axes 为 required；消费方按 1.0/1.1 双版本读取，见 summary.schema.json:27 const 现状）；
      每轴对象化：{applicable, required_for_release, verdict, blocking_class, source_checks[],
      resolution}——resolution 正式入 schema（与上文 class/owner/retry_phase 同一契约，勿两处各写）。
      schema 不变量（机器校验）：applicable=false ⇒ verdict=NOT_APPLICABLE ∧
      required_for_release=false ∧ blocking_class=null；verdict∈{PASS,NOT_APPLICABLE} ⇒
      resolution=null；verdict∈{FAIL,UNVERIFIED,STALE,MISSING} ⇒ resolution 必填。
      非 UI feature 的 visual/asset 轴 applicable:false；无素材 feature 同理。
      双投影分立（勿含混「必需轴」）：**顶层兼容 verdict 按 required_for_phase_advance 投影**
      （spec 阶段 functional=UNVERIFIED 不阻断进 coding），**feature completion 按
      required_for_release 投影**；两矩阵按 phase 定义落 phase-rules 附表。
      旧顶层 verdict 保留为**唯一解析器生成的兼容投影**（单一计算点）：
      推进必需轴 FAIL→FAIL；无 FAIL 但推进必需轴 UNVERIFIED→INCOMPLETE（枚举已存在）；全过→PASS。
      legacy 1.0 summary 政策（codex 三轮②）：可兼容读取/展示，但**不得**直接作为 1.1
      completion 的干净依据——须在当前 gate_fingerprint 下重跑，或保守投影 INCOMPLETE
      （防历史假 PASS 重入新状态机）。现有消费方（verify-feature-completion.ts:157
      verdict!=='PASS'→needs_fix 等）行为在投影语义下保持不变，实施时全量 rg verdict 消费点核对。
      【completion 投影纪律（cursor 三轮）】FUNCTIONALLY_COMPLETE_VISUAL_PENDING 只是
      completion 投影标签，不另起状态机绕过 verify-feature-completion——映射仍走
      needs_human → AWAITING_HUMAN_REVIEW 既有通道。
      verifier PASS 语义固定为「报告可信」，永不改写产品裁决（落成代码断言）。
      单测：本次事故 fixture（不通过+3 BLOCKER → phase FAIL）、洞⑥回归、needs_fix/needs_human
      投影分流、兼容投影三分支、非 UI feature 轴适用性、跨阶段传播、goal/交互两路径同源。
    status: completed
  - id: p0b-asset-integrity-role-aware
    content: >
      P0-B role-aware 素材完整性（源文件 sanity + 渲染可见性双层，治「空白图+假可见」）。
      ①crop 左移禁令（cursor 二轮显式句 + codex 三轮③收窄防误伤）：禁的是盲模型**执行或自证**
      crop，不禁消费已可信完成的 crop 产物。spec 门禁：effective_image_input=none ∧
      acquisition=crop ∧ 未同时满足{resolved_path 存在、文件 sanity PASS、crop provenance
      可验证、human_crop_confirmed 具可信身份/receipt} → BLOCKER（正确路径=placeholder:true+
      asset-manifest 或问人）；四条件齐备（用户/外部工具已裁好）→ 放行为消费态。
      ②role 与 criticality 反自报（codex 二轮 M2）：role（brand_logo|illustration|icon|mask|
      decoration|system_symbol）与 ui-spec icon.kind/ref-elements/must_have 交叉对账，失配 → FAIL；
      criticality 由 P0 屏归属+must_have+reference element 派生（机器算），不由 agent 自由填写；
      placeholder_allowed 语义=「开发期是否允许继续」，发布放行由 release policy 判
      （brand-critical 仍为占位 → release_readiness=BLOCKED，接 P0-A/P0-D）。
      ③源文件 sanity：物化进模块 media 的图片按 role 分档跑 jimp 检测（全透明/近纯色/内容占比
      异常低/尺寸解码异常）——brand-critical 命中 → BLOCKER 不分档位；阈值不平移
      asset-crop-validation.ts:69 的 crop 校准值，按 role 重校。
      ④占位按角色生成（勿一刀切 text_avatar）：brand_logo→文字头像（首字+确定性中性调色板
      圆角块）；system_symbol→HarmonyOS sys symbol；illustration→明确标注的中性插画占位框；
      decoration→中性块/可省略。manifest 记 placeholder_kind，真素材到位无缝替换；
      禁止空白/透明 PNG 充当占位。
      ⑤渲染可见性（设备侧）：uitree Image/自绘节点 bbox × 设备截图区域——判「与周边背景
      不可区分/无前景对比/无边缘结构信号」+ 实际渲染 bbox 与声明一致性。
      【两段式落地（cursor：判据强易误伤扁平合法 UI）】先以事故截图夹具+VL 宿主正常截图
      双向校准阈值、WARN 观察一轮，钉死后再升 BLOCKER。
      单测：crop 左移禁令、role 对账失配、criticality 派生、分角色占位确定性、
      设备区域可见性（事故夹具+扁平合法 UI 反例）。
    status: pending
  - id: p0c-blind-ui-kit
    content: >
      P0-C 可执行盲档 UI kit（component-kit-first 落到「可实例化」）。
      【交付形态定案（codex 二轮 M1：不留到实施首日）】路线 A：profile 内维护 ArkUI block 模板，
      由**确定性 scaffolder** 生成进宿主工程公共层；宿主不引入 framework runtime 依赖。
      弃 B（片段照抄）理由：依赖弱模型正确复制、难建 source/runtime 锚点、易复制残缺/改坏 token。
      【目标目录解析算法（codex 三轮④：勿硬编码 03-CommonBusiness——非所有消费者工程都有该层）】
      1) framework.config 显式 ui_kit_target_dir > 2) profile 推荐 shared/common presentation 层 >
      3) 按 architecture 配置（outer_layers）推导 > 4) 无法唯一推导 → halt 问用户确认。
      与既有 Common 组件冲突策略（前缀命名空间+冲突即停）、锚点命名契约（下述算法）首日核对
      并记实施记录。
      【锚点唯一性算法（codex 三轮④：固定组件名在重复行场景不唯一）】锚点由实例语义生成：
      maison:<feature>:<screen_id>:<semantic_node_id>:<instance_key>
      （例 maison:bc-openCard:bank-list:bank-row:icbc）——同屏唯一、ui-spec 可追溯、
      repeated rows 可区分、visual-diff-nav 可稳定定位。
      【kit 自身防退化】UI kit gallery fixture：每 block 编译+结构断言+维护者批准的基线截图，
      防 kit 演进引入全局视觉退化。
      ①blocks 清单：MaisonNavBar / MaisonListCard / MaisonListRow / MaisonBottomSheetScaffold /
      MaisonPrimaryButton / MaisonSelector / MaisonResultState / MaisonSmsCodeField /
      MaisonDetailSection。每 block 定义：必需子节点、默认尺寸间距、variant、token 来源
      （对齐 sys.color/sys.float）、可覆盖项、对应 ui-spec semantic node、素材缺失行为
      （接 P0-B 分角色占位）、source/runtime 可验证映射。
      【uitree 锚点事实（codex 二轮：自定义组件运行时可能被展开，不得假设组件名保留）】
      block 内注入稳定 .id()/测试锚点，运行时检查锚点 ID+结构关系，不依赖组件名出现在 uitree。
      ②ui-spec schema 增加 container/frame 语义节点（list_card_container、nav_bar、sheet_header
      等）与 block 一一映射；must_have 覆盖结构容器；check 侧三段闭环：声明 semantic node →
      源码命中 block 锚点 → 运行时 uitree 出现锚点 ID+结构关系（防「声明容器、代码散落 Text」）。
      ③HarmonyOS 设计基线文本（token 表+组件形态词典）作为 blocks 解释层随包交付；
      盲档 spec 工作法升级为「套 block 模板+OCR 填内容」。
      验收：bc-openCard 需求文本+OCR 盲档重跑 spec+coding，8 屏 ui-spec 全部产出容器/导航/
      关闭钮/状态色声明且源码+运行时双命中锚点。
    status: completed
  - id: p0d-fidelity-intent-and-visual-debt
    content: >
      P0-D fidelity 意图三态覆盖扩面 + 视觉债务多轴收口 + 人工验收清偿闭环。
      ①意图检测扩面（非新机制）：goal-fakepass-hardening 三态检测从 goal preflight 扩到
      逐阶段驱动路径（harness-runner spec 阶段前置钩子，同源实现勿 fork）。落盘 reference_intent
      {value: exact|layout|inspiration|unknown, source: user|project_config|human_receipt|inferred}
      + desired_fidelity + effective_fidelity（含 deferred）+ downgrade_receipt 引用；
      盲档只能产生 effective=deferred 或凭证降级，绝不自动改写 desired、绝不静默继续跑强意图需求。
      【宿主体验预告（cursor 二轮：诚实成本，勿开旁路）】短期 minimax 宿主下，每条「完全参考
      X.jpg」类需求都会走 DEFERRED/人工降级确认——这是设计内成本；对齐 vision.blind_tier
      告知动线（I1），一次需求一次确认，禁止任何静默放行旁路。
      ②视觉债务 SSOT：doc/features/<f>/visual-debt.json（机器真值：逐条 source_check_id/
      asset_key/screen_id/severity/status/resolution_class）+ visual-debt.md 人类投影；
      由 harness 从 asset-crop-validation、ui-spec verified 态、static_fidelity_score、
      blind-review-pending、P0-B 检出派生。
      ③债务→完成语义映射（接 P0-A）：未清偿视觉债务 → visual_verdict=UNVERIFIED（needs_human）
      或 FAIL（needs_fix，如空白素材）→ completion_status=FUNCTIONALLY_COMPLETE_VISUAL_PENDING、
      release_readiness=BLOCKED；test-report 结论区渲染结构化轴；headless 可继续执行但
      FEATURE_COMPLETED 不可达。
      ④人工视觉验收 receipt（codex 二轮 M5 + 三轮⑤：防 UNVERIFIED 死锁，且阈值不许目标后移）：
      结构化 receipt（复用 confirmation-registry + validateConfirmationReceiptFile 信任链）：
      {feature, build_fingerprint, rubric_version, policy_hash, gate_fingerprint,
      screens: [{screen_id, variant, reference_sha256, actual_sha256}]（结构化映射，
      杜绝两组 hash 数组顺序调换仍验签通过）, rubric{container,hierarchy,density,state_color},
      verdict, accepted_debt_ids[], signed_by, signed_at}。
      阈值**在验收前冻结、本 plan 即定死**（不做「宿主复验后校准」——先看结果再调线=目标后移）：
      每维默认 ≥4/5；任一维 3 分必须对应显式 accepted_debt_id；出现 1-2 分不得通过。
      允许显式接受残余债务（逐条留痕）；receipt 仅能清偿主观视觉项（UNVERIFIED→PASS +
      债务条目 closed），**不能**洗掉确定性 FAIL（空白素材/证据缺失/hash 失配——needs_fix
      类只能修复重跑清偿）；hash/policy_hash 绑定失配即 receipt stale。
      ⑤品牌色事实源纪律：用户素材/主题 > 项目既有 token > 官方品牌资源（人工确认引入）>
      标注临时的中性占位；模型世界知识猜色只用于占位中性调色，永不写成品牌真值 token。
      单测：三态扩面触发分支、债务派生与轴映射、receipt 校验（hash 绑定/阈值/不可清偿
      needs_fix 回归）、desired 不可被自动改写回归。
    status: completed
  - id: p1e-deterministic-feedback
    content: >
      P1-E 确定性视觉反馈回路（盲模型的「文本化眼睛」，JSON 为 SSOT）。
      产物：device-testing/visual-feedback.json（机器真值）+ visual-feedback.md（人类投影）。
      JSON 至少含：reference/actual 文件 hash、framework commit SHA、gate_fingerprint、
      build/device/viewport、screen_id+variant、OCR 文本差异、bbox/spacing/color 区域差异
      （OCR 锚定分区主色对比+行距节奏序列对比）、confidence、与上一轮 delta、收敛状态。
      【语义映射修正（codex 二轮 M3 + 三轮⑥两类信号分立）】hash 失配/文件缺失 → evidence
      STALE/MISSING；连续无改善 → 接 visual-rounds-ledger 既有 no-progress fuse（扩展
      evaluateVisualRound 输入，不建并行收敛状态机）；证据新鲜且差距缩小 → evidence PASS +
      convergence=converging。stalled/regressing 不进 evidence_verdict。
      确定性信号两类分立（防「色差 8→9 就 FAIL 整个 visual 轴」）：
      离散事实（必需节点缺失/文案错误/空白素材/状态相反）→ 可直接 visual FAIL（needs_fix）；
      连续指标（色差/间距/bbox 偏移）→ 默认 finding/advisory；仅高置信指标持续退化且超过
      **冻结阈值** → 升级 visual FAIL。
      deterministic_feedback 策略位由 harness 按「盲档+UI change」**机器派生**，
      不是 agent 可关闭的普通配置开关。
      【反馈身份（codex 三轮⑥：宿主为发布包环境，不必有 git commit）】记
      framework_version + framework_package_digest（RELEASE-MANIFEST.sha256 可作源）+
      gate_fingerprint + framework_commit_sha: null|...（可空）。
      【复用存量（codex 二轮 M3 下半）】nav 配置缺失=档位无关 BLOCKER 已是 HEAD 存量
      （check-testing.ts:2194 t7）——本项只加回归测试锁行为，不重复实现。
      【采集策略与档位解耦（codex 二轮 M4，实证 capture-completeness-check.ts:87/216/732
      pixel-only 早退）】新增 deterministic_feedback 策略位：需要盲档反馈时（effective 盲档 +
      UI 需求）强制 8/全 P0 屏采集 screenshot+layout dump+OCR+screen/state binding，
      不随 fidelity 档位早退——盲档正是最需要几何数据的时候。
      红线：不做整图直方图/单一全局相似度当质量结论；只产单项可判事实+advisory。
      单测：JSON schema、语义映射四分支、ledger 扩展回归、盲档采集不早退、nav 门禁回归。
    status: completed
  - id: p1f-asset-request-loop
    content: >
      P1-F 素材问人闭环（素材是输入不是推断）。spec 阶段 brand_logo/illustration 类素材
      盲档无法验真裁剪时：生成 doc/features/<f>/spec/asset-request.md（逐项：用途/建议尺寸/
      放置路径/当前占位形态），交互式走 registry 确认（提供素材/接受占位/逐项 defer）；
      headless 不阻塞执行，按 P0-B 分角色占位物化并计入视觉债务——completion/release 语义
      受 P0-A/P0-D 约束（brand-critical 仍为占位 → BLOCKED，不进 FEATURE_COMPLETED）。
      【三态清偿防假清偿（codex 三轮附加：文件放进去了 ≠ UI 引用了）】素材债务按
      asset_source_status（文件 sanity）/ asset_binding_status（源码/资源绑定检查）/
      asset_render_status（设备区域可见性）三态分立——用户补素材后重跑 spec harness 只置
      source=VERIFIED；三态全 VERIFIED 才关闭该素材债务条目（防「文件已放、UI 仍引用旧占位」）。
    status: completed
  - id: p1g-controlled-host-replay
    content: >
      P1-G 受控宿主重放验收（可复现）。盲线（minimax 2.7 / claudecodeli）重跑 bc-openCard，
      固定条件：8/8 screen_id+variant 全覆盖（收起/展开/全部银行/类型弹窗/选卡/验证码/结果/
      详情）；固定 fixture（银行/卡片/验证码/选中态同数据）、固定设备/分辨率/字体缩放/状态栏/
      键盘态；同状态同区域前后对比。
      机器验收：0 空白或未披露关键素材（P0-B）、0 缺失基准结构（P0-C 锚点断言：返回键/卡容器/
      sheet header/关闭钮/chevron）、0 未关闭 MAJOR 视觉缺陷、反馈产物 hash+gate_fingerprint
      新鲜、provider/model 身份由工具链记录（不信 agent 自报）、completion_status 如实
      （存在占位素材时必为 VISUAL_PENDING）。
      人工验收：四维 rubric 经 P0-D④ 人工视觉验收 receipt 签署留痕（绑定截图 hash+build），
      对照 D:\1.code\对比结果\1-bc-opencard；receipt 达冻结阈值方可清偿主观项债务。
      【前置硬条件（cursor 三轮）】复验必须使用**含本 plan 改动的新 framework 发布包**
      （记录 framework_package_digest），否则与「宿主旧包 nav 仍 WARN」类版本视图差异搅在一起，
      结论不可归因。
    status: pending
---

# 实施记录

## Review-fix 八轮（2026-07-19，cursor 深度 review 2P2+2P3 实锤修复 + 1P2 设计判定显式关闭）

cursor 复审指出"上一轮意见未闭合"——核实：该轮意见此前未进入本仓工作流（工作区当时在做二期
e9c4a7f3），本轮逐条对 ground-truth 核实后处置：

- **P2-①（violation-only 债务永不闭账）→ 实锤修复**。`asset_materialization_sanity` /
  `asset_placeholder_present` / `visual_parity_unverified_crop` 三个 check 均只在有违例时
  push 结果——clean 时 `deriveVisualDebt` 恒走"本轮缺席→单调保留"分支，修好后债务永不
  closed。修复：扫描已执行且零违例时落明确 PASS 结果（`coding-visual-parity-check.ts` 三处），
  债务闭账与 `annotateAssetTriState` 三态标注同时受益。ui-kit 单测断言从"无结果"改"零命中 PASS"。
- **P2-②（visual_parity 整包异常 → MINOR SKIP）→ 实锤修复**。`safeRun` 非程序员错误异常降
  MINOR SKIP + `CODING_CRITICAL_SKIP_IDS` 无 visual_parity——dispatch 崩掉时 coding 可 PASS
  收口。修复：`safeRun` 增 `failClosed` 选项（`visual_parity` 站点启用，异常恒 BLOCKER）+
  visual_parity 入 critical skip 集（profile 显式关闭的 MINOR SKIP 经 severity 过滤天然放行）。
- **P2-③（check-receipt 不消费 release_readiness）→ 设计判定显式关闭（不改码）**。双投影分立
  是 quality-axes 定案设计（文件头③）：check-receipt 校验 **phase-advance 投影**（顶层 verdict），
  release 投影（release_readiness/completion_status）在 `verify-feature-completion` 的 release
  判定点强制消费且有防篡改一致性校验（release_readiness=READY 与非 PASS 必需轴矛盾 → needs_fix）。
  阶段闭环 ≠ 可发布；FUNCTIONALLY_COMPLETE_VISUAL_PENDING 语义正依赖这一分层。
- **P3-④（披露门禁用 deprecated loadVisualDebt）→ 实锤修复**。`checkVisualDebtDisclosure`
  是消费面而非纯展示——损坏账本归 null → open+accepted=0 → PASS，绕过单调 ledger fail-closed。
  改 `loadVisualDebtEx` 三态，invalid → BLOCKER FAIL（`visual_debt_ledger_corrupt`）。全库
  deprecated `loadVisualDebt` 消费点清零。
- **P3-⑤（ui_kit 轴前缀漂移）→ 实锤修复**。`VISUAL_PREFIXES` 的 `ui_kit_conformance` 匹配不到
  实际 check id `ui_kit_source_conformance`/`ui_kit_runtime_conformance`（漂移归 functional 轴）
  ——放宽为族前缀 `ui_kit_`。
- **附带发现**：`visual-debt.ts` `screensMatrixHash` 的 canonical 分隔符是**字面 NUL 字符**，
  导致 ripgrep 判整文件为 binary、全库检索漏检本文件——改等价 `\u0000` 转义（运行时字符串不变）。

验收：typecheck 0 · unit 2249/2249 · fixtures 44/44 · git diff --check clean（与二期
review-fix 二轮同一验证窗口）。

## Review-fix 七轮（2026-07-18，codex 2P1+1P2——全部核实属实并修复；上轮 6 项修复获逐项确认）

**P1-①（注释/字符串 $r 伪引用）**：`scanResourceRefModules` 曾对源码生扫正则——B 模块仅
`// Image($r('app.media.x'))` 也被计入引用模块，其残留占位随之入债务、brand-critical 时误阻
发布。改为复用同文件既有 `collectResourceRefsInActiveCode`（跳注释/字符串，有单测）；
**同族收口**：`scanEtsFile`（scanFeatureSourceTree 的 resourceRefs 面）同样生扫——注释里的
$r 会让 asset-render 等 presence 消费者**假 PASS**（比误报债务更毒的方向），一并改 active-code
收集器。生产链测试三例齐备（B 仅注释引用不计入 / B 字符串含 $r 不计入 / B 真实调用命中，
走完整 checkVisualParity），另加 scanResourceRefModules 直测。

**P1-②（contracts 全模块只读扫描缺 containment）**：source-ref-scan/visual-parity-backstop
等共享扫描直接信任 `modules[].package_path`——`../outside` 会越出宿主根且外部文件可影响
门禁裁决。按 codex 建议在**加载边界统一根治**（spec-loader 新增
`normalizeContractsModulePaths`）：package_path 必填字符串 + `validateProjectRelativePath`
（拒绝绝对路径/盘符/`..`）+ canonical 化写回（反斜杠→正斜杠）后再交所有消费者；非法条目
剔除 + shape_issues 留痕 → 既有 `feature_spec_shape` 结构化 BLOCKER 消费（不 throw：loader
在 safeRun 外，炸=无 summary）。联动：第五轮"null 条目剔除"用例的 M1 补合法 package_path
（保持原意图），新增越界/绝对/缺失/反斜杠四形态专测。

**P2（占位 CLI 静默过滤缺 key）**：`!a?.key` 直接过滤后可 exit 0"计划干净"——缺失/空白/
非字符串 key 现全部列入 blocked（exit 1），测试锁定 `{acquisition: placeholder}` 无 key 形态。

**验收**：typecheck 0 · unit **2179/2179**（+3 净新增）· fixtures 44/44 · openspec 40/40 ·
git diff --check PASS。codex 已确认上轮 6 项全部正确落地；发布 plan 门禁红为 8 个 3.0.0
存量 plan 未完成（含本 plan 诚实悬置项），非本轮缺陷。

## Review-fix 六轮（2026-07-18，codex 1P0+3P1+1P2——全部核实属实并修复）

**P0（Windows 反斜杠 package_path → 占位覆盖真素材）**：contracts 里 `app\feature` 形态的
package_path 与 CLI 归一化后的 restrict 集比对失败 → findModuleMediaFile lookup 落空 →
"未物化"误判 → `--apply` 直接把真实 SVG 覆盖成占位。双层根治：①`canonicalPkgPath` 导出
（visual-parity-backstop），findModuleMediaFile 对 restrict 集与 mod.package_path **双侧**
canonical 化；CLI 的 --module 匹配同口径；②`generateRolePlaceholder` 增 no-clobber 契约
（不依赖前置 lookup 是否失效）：目标已存在同字节 → 幂等 skip（written:false），异字节 →
`conflict:true` 拒覆盖，CLI 把 conflict 计入 blocked（exit 1）。e2e 测试：反斜杠 package_path
+ 已存在真实 SVG → skip、字节不变、exit 0。

**P1 三修**：①非法 asset key 曾**先越界 fs 探测再"假跳过"退出 0**——重排为任何 fs 访问前
全量 `ASSET_KEY_RE` 预校验，非法 key 直接 blocked（exit 1）且从 planner 输入剔除（零探测/
零落盘，测试断言 media 目录不被创建）；②`resolveUiKitTargetDir` config 分支安全校验收进
resolver 本体（CLI 末端 containment 只挡写入，conformance 消费 resolver 读外部目录仍可能
假 PASS）——`paths.ui_kit_target_dir` 越界/绝对路径 → `halt`，source-conformance 同源继承
BLOCKER（`ui_kit_target_unresolved`），resolver 与消费面双测试；③coding 侧 sanity/占位扫描
按 `$r` **实际引用模块**限定（scanResourceRefModules + resourceKeyToRef，`findAllModuleMediaFiles`
增 restrictPkgPaths 形参、canonical 双侧）——未引用模块的同名残留占位/坏图不再入债务分母
误阻 release；无引用记录的 key 回退全模块兜底；两组测试（B 未引用不计入 / B 引用后命中）。

**P2 + 统一口径**：ui-spec.md acquisition 表补 `placeholder` 行（CLI 只为此声明生成的契约
写进 spec 文档）；`ASSET_KEY_RE` 三处同源——asset-integrity（SSOT）→ ui-spec-schema-validate
key 校验（路径穿越 key 在 spec 期即拒）→ JSON schema `pattern`。

**验收**：typecheck 0 · unit **2176/2176**（+7 净新增）· fixtures 44/44 · openspec 40/40。

## Review-fix 五轮（2026-07-18，codex 1P0+4P1——全部核实属实并修复）

**P0（CLI 宿主根边界）**：两个写入 CLI 接入 `validateProjectRelativePath`（拒绝绝对路径/盘符/
`..` 段）+ 终态 containment——scaffolder 的 --target 与 config 派生目标、placeholder CLI 的
package_path（模型生成值）与 asset key（资源名白名单 `[A-Za-z_][A-Za-z0-9_]*`）全部受约束，
越界项转 blocked/exit 2 绝不落盘；e2e 测试断言根外零残留。

**P1 四修**：②运行时段部分缺 dump 不再整体 PASS——逐屏登记 missingEvidence，任一声明缺
对应 dump → WARN `ui_kit_runtime_unverified`（8 屏交 1 个合格 dump 曾整体 PASS 且债务随之
闭账的洞关闭）；③已物化判定去跨模块 first-match——placeholder CLI 限定所选模块
（findModuleMediaFile restrictPkgPaths）、coding 侧 sanity/占位检测改 `findAllModuleMediaFiles`
逐模块全匹配（A 模块真素材不再豁免 B 模块占位/坏图）；④validateQualityAxes 补齐 schema
required 语义——六键在场性（整键省略拒）+ resolution 未知字段拒 + `blocking_class ≡
resolution.class` 一致性 + resolution=null ⇒ blocking_class=null；⑤ui-spec 契约统一
（治"单测过、真实 spec 阶段挂"假绿）：canonical validator（ui-spec-schema-validate）与
JSON schema 双同步——COMPONENT_TYPE_ENUM 收编 9 个语义容器、ACQUISITION_ENUM 加
`placeholder`、ASSET_ALLOWED_KEYS 加 crop_provenance/role/placeholder_kind、node 层
`block` 字段枚举校验；新增 canonical→downstream 集成测试（validateUiSpecSchema 零错 →
collectDeclaredBlockInstances/planPlaceholderGeneration 同源消费）。

**验收**：typecheck 0 · unit **2169/2169**（+3 净新增）· fixtures 44/44。
codex 状态判定确认：框架侧主体实现完成；P0-B enforce 与 P1-G 实机复验为发布验收项
（诚实 pending，非代码缺陷）——本轮后代码缺陷面清零，待宿主回放回灌。

## Review-fix 四轮（2026-07-18，codex 2P0+4P1 + cursor 1P2——全部核实属实并修复）

**P0 双修**：
1. 占位 CLI 洗白真素材缺失（P0-1）：三件套收口——①`planPlaceholderGeneration` 可测判定：
   **只为 `placeholder:true`/`acquisition:placeholder` 声明的资产生成**，非占位声明的真实素材
   缺失（crop/repo_assets/未声明）→ blocked + CLI 非零退出（exit 1），绝不代生成；
   ②生成物内嵌 provenance marker（`<!-- maison:placeholder:<kind>:<key> -->`，
   detectPlaceholderMarker 机器识别）；③新 `asset_placeholder_present` 检查：marker 命中
   → 逐素材 WARN 入视觉债务（needs_human，入三态家族），brand-critical 占位 release 经债务链
   保持 BLOCKED——"占位可见 ≠ 素材已供给"闭环成立。
2. 空轴对象绕过 validateQualityAxes（P0-2）：全字段严格化——applicable/required_for_release
   须 boolean、verdict/blocking_class/resolution.class/owner 枚举强制、source_checks 字符串
   数组、未知轴/未知字段拒绝；codex 实锤形态（四个 `{}` 空轴）负例测试锁定 ≥4 项拒绝。

**P1 四修 + cursor P2**：③scaffolder CLI 增 `--project-root`（cwd=framework/harness 时相对
target 曾解析进 framework 内——e2e 测试：假 harness cwd 执行断言落宿主模块且 framework 内
零残留）；门禁 suggestion 输出含真实宿主根的完整命令。④spec 侧声明门禁异常 MINOR SKIP →
BLOCKER FAIL framework_bug（cursor P2 同项：三段全链一致，异常不得绕过地板）。⑤运行时段
无 dump 不再真空通过：声明>0 而零运行时证据 → MAJOR WARN `ui_kit_runtime_unverified`
（needs_human 债务源，release 阻断、阶段可继续——诚实且符合盲档设计）。⑥多模块不再默取
mediaDirs[0]：须显式 `--module <package_path>`（校验存在性），单模块自动，多模块缺参 exit 2。

**验收**：typecheck 0 · unit **2166/2166**（+4 净新增）· fixtures 44/44。
codex 上轮 9 项关闭确认单全绿（其复核原文）；本轮修复后四个测试盲区
（非占位资产 CLI 代生成/空轴对象/harness cwd 落点/盲档缺 dump）全部有反例锁定。

## Review-fix 三轮（2026-07-18，codex 2P0+4P1+次要项——全部核实属实并修复；cursor 本轮通过）

**P0 双修**：
1. 损坏账本 fail-open（P0-1）：`loadVisualDebtEx` 三态（missing/valid/invalid）——invalid 由管线
   抛错进 fail-closed（BLOCKED + DEBT_PIPELINE_ERROR）且**不覆盖**原文件（保留取证现场）；
   writeVisualDebt 改原子写（tmp+rename，进程中断不留截断 JSON）；loadVisualDebt 降级为只读
   展示用途（@deprecated）。
2. 管线异常仍可干净完成（P0-2）：completion 消费面统一规则——任一 required_for_release 轴
   非 PASS 都拦（needs_human→等人；needs_fix/external/无 resolution→needs_fix）；新增
   `release_projection_consistent`（READY 与非 PASS 轴矛盾=篡改拦截）与
   `debt_pipeline_healthy`（DEBT_PIPELINE_ERROR 一律 needs_fix）两条独立校验。

**P1 四修**：③kit 门禁异常路径 MINOR SKIP → BLOCKER FAIL（coding+testing 双处，
failure_kind=framework_bug——地板门禁不得因异常绕过）；④`validateSummaryV11` 唯一权威
（四字段 presence+枚举+轴不变量）：writer 落盘前 fail-fast（宁崩不落半 1.1）+ completion/
upstream 两消费入口统一调用（**偏差声明**：未做 TS discriminated union——类型面改动波及全消费
面，运行时唯一权威+writer fail-fast 等效收口，编译期缺口由 fail-fast 兜）；⑤宿主执行入口：
harness npm scripts `ui-kit:scaffold`/`ui-kit:placeholders` + 新 asset-placeholder-cli.ts
（按 ui-spec.assets 自动推导 role/label/media 目标，dry-run 缺省）+ 门禁 suggestion 全部改
可复制真实命令；⑥workflow 解析降级显式化：`resolveUpstreamPhaseChain` 返回 degraded 态，
降级+当前 phase 不在回退链 → BLOCKER `workflow_chain_unresolved`（自定义阶段门禁不静默消失）。

**次要项双修**：三态标注只更新本轮观察到的维度（render 缺席不把历史 VERIFIED 打回
UNVERIFIED）；组件调用匹配改"去注释+去字符串"双基线（`stripArkTsCommentsAndStrings`——
`Text("MaisonNavBar(")` 字符串伪调用不作数，锚点匹配仍保字符串基线）。

**验收**：typecheck 0 · unit **2162/2162**（+5 净新增回归用例）· fixtures 44/44。
夹具随契约同步：completion 夹具补全 V11 四字段。
cursor 残余记录：声明强度为"每屏 ≥1 任意语义容器"（按页型强制组合留 P1-G 回灌后评估）；
其观察到的 preflight_blocks_on_env_injection 单测失败经复跑为环境性偶发，与本 change 无关。

## Review-fix 轮（2026-07-18，codex「不能判定完成」3P0+4P1 + cursor「有条件通过」1P1+3P2——全部核实属实并修复）

**P0 三连修**：
1. 视觉债务跨阶段蒸发（codex P0-1）：deriveVisualDebt 重构为**单调 ledger reducer**——check 本轮
   缺席→历史条目原样保留；明确 PASS 才 closed；结构化 findings 展开 scope 级子条目
   （render_visibility 逐屏 / asset_sanity 逐素材，`debt:<check>:<scope>`），单 scope 修复单独闭账；
   新增 coding→testing 序列测试（缺席不蒸发）+ scope 粒度测试。
2. fail-open 双洞（codex P0-2）：债务管线异常 → fail-closed（release=BLOCKED、completion=
   DEBT_PIPELINE_ERROR、visual 轴降 UNVERIFIED(needs_fix/toolchain)、债务文件保留 last-known-good）；
   投影 vs legacy 失配 → 顶层 verdict 取**更严一侧**（FAIL>INCOMPLETE>PASS），不再选择宽松侧。
3. 裸 1.1 summary 绕过（codex P0-3）：validateQualityAxes 接入两处生产消费面——
   verify-feature-completion（quality_axes 缺失/违反不变量 → needs_fix `quality_axes_valid`）+
   upstream-verdict-gate（1.1 缺 lattice → 机器裁决不可信 → 下游 FAIL）；
   `{"schema_version":"1.1","verdict":"PASS"}` 裸文件两个入口都被拒（测试锁定）。

**P1/P2 修复**：④kit 从 opt-in 变强制（cursor P1）：新 `ui_kit_declaration_required`（盲档 P0 屏
零 block 声明 → spec BLOCKER；全 P1 自报也不逃）接入 spec-ui-spec 链；源码段升级：kit 物化
强制门禁（未 scaffold/hash 漂移 → BLOCKER 附精确 --apply 命令；目录不可解析 → BLOCKER 要求显式
配置）+ stripArkTsComments 注释剥离（保留字符串——锚点真身在 .id() 字符串里）+ `\bBlock\s*\(`
正则匹配（注释伪注三连测试锁定）。**偏差声明**：codex 建议"coding preflight 自动 scaffold 落盘"
未采纳自动写入形态——harness 静默改产品源码违反 consumer-write-guard 红线；等效强制力由
BLOCKER+精确命令承担（弱模型不执行即无法过门禁），语义等同"必须执行的结构化 action"。
⑤jimp 三态（codex P1-5/cursor P2）：assessMaterializedFile 改 pass|fail|unverified，
unverified 绝不折叠 pass；brand-critical unverified → BLOCKER（注入降级分支测试）。
⑥上游链 SSOT（codex P1-6）：resolveUpstreamPhaseChain 消费 active workflow
（resolveWorkflowSpec+resolvePhaseChain(track) featureOrdered），解析失败回退保守缺省。
⑦report_validity 出产品轴（codex P2-7）：报告合法性 check 只喂 report_validity，
projectPhaseAdvanceVerdict 增 reportValidity 输入（阻断保留、责任归属分离，等价性维持）。
⑧external_tool provenance 收紧（cursor P2）：source_sha256 须命中 feature 参考图集真实字节哈希
（collectFeatureReferenceImageHashes，≤64 文件/32MB 上限；自填假 sha 测试锁定拒收）。
⑨criticality 去自报（cursor P2）：brand/ill + 有屏即 brand_critical（全 P1 不降级）。
⑩披露加固（cursor P3）：「视觉债务」须与计数数字同段。

**验收**：typecheck 0 · unit **2157/2157**（+8 净新增）· fixtures 44/44。
夹具随收紧同步：completion 夹具补最小合法 lattice；external_tool 夹具改真实参考图哈希。

## 主实施（2026-07-17，用户授权"开工做完为止"）

**完成面**：P0-0/P0-A（两切片）/P0-C/P0-D/P1-E/P1-F 全部完成；P0-B 完成 calibrate 段
（enforce 升级按 design §1.3 须"连续两轮真实 run 零误报"——观察期内诚实不标完成）；
P1-G framework 侧物料全部备齐（docs/operations/blind-host-replay-runbook.md：8 屏矩阵/
机器验收 M1-M10/rubric receipt 模板/回灌表），实机复验属外部依赖待用户宿主执行。

**验收**：typecheck 0 · unit **2149/2149**（基线 2068 → +81 新用例，7 个新套件：
negative-verdict-gate 16 / quality-axes 12 / blind-crop-prohibition 6 / visual-debt 13 /
profile ui-kit 11 / profile asset-integrity 15 / profile visual-feedback 8）· fixtures 44/44 ·
openspec validate 40/40（含新 change blind-visual-hardening）· release:verify 除 plan-version
门禁全 PASS（8 hit=7 存量 3.0.0 未完结 plan + 本 plan 自身诚实未完结，与 goal-fakepass 4.6 同性质）。

**phase-rules yaml 变更（gate_fingerprint 已变，存量回执 stale 属预期 breaking，MIGRATION 已记）**：
review/testing/coding/ut-rules 登记 negative_verdict_closure + upstream_verdict_gate；
spec-rules 登记 fidelity_capability_pregate + blind_crop_prohibition；testing-rules 登记
visual_debt_disclosure。

**计划外硬发现（比 plan 推导更深，已修）**：
1. `collectRequirementIntentText` 只读 goal-run manifest——逐阶段驱动路径**恒空串**，这就是
   "三态检测漏检"的代码实体；新增 `collectIntentTextWithPhaseFallback`（feature 根需求文档 +
   spec.md 回退，同源 dereference 勿 fork）。
2. image-toolkit bbox 语义 SSOT=归一化 [x,y,w,h]（worker 头注），uitree bounds=像素
   [x1,y1][x2,y2]——render-visibility 首版直传即错（round6 转置教训同族），已换算并注释锚定。
3. layout dump 真形状为 `{schema_version:'hylyre-hypium-ui-dump-v1', tree:...}` 包装——
   夹具须按真形状构造（parseHypiumDump 校验 schema_version）。

**偏差声明（对 plan/openspec 正文的收窄，均已在 tasks.md 逐条登记）**：
①必需轴矩阵落 ts 单点（quality-axes.ts ADVANCE_UNVERIFIED_BLOCKING）未落 phase-rules yaml
（避免二次 fingerprint churn；openspec 文字为规格 SSOT）；②P1-E visual_feedback 的 hard 信号
现为 WARN 观察产出（阻断由既有 OCR 门禁承载，防同一事实双 BLOCKER 抖动——P1-G 回灌后再评估
独立升级）；③收敛跟踪自 feedback 指纹集对比，未直改 evaluateVisualRound 输入面（stalled 事实
与既有 fuse 同源于 visual_diff 结构化轮次，不并行造状态机）；④capture-completeness pixel-only
早退未改（属 spec 期 OCR 完备性检查而非设备采集；设备采集本就档位无关）；⑤三态清偿的防假清偿
硬保障=各阶段检查各自债务条目，三态字段为可读 rollup；⑥gallery 编译+基线截图归 P1-G 实机段。

**悬置项（全部有判据与落点）**：P0-B enforce（判据=连续两轮真实 run calibrate 零误报，
runbook §4 回灌）；P1-G 实机复验（runbook 全套，用户执行）；P1-E hard 升级评估（同回灌）。

# Review 采纳记录·四轮（2026-07-17，codex「正式通过，可定稿」+ cursor「定稿可执行」——终审）

plan 正文冻结（codex：停止重写，按 P0-0 规格化）。以下 8 条实施备注**不改 todo，落 P0-0 的
OpenSpec design/tasks**，在此存档防丢：

1. （codex①）P0-A 两切片过渡实现：第一切片负面结论走**现有**顶层 verdict=FAIL+blocker、
   跨阶段先消费旧结构；第二切片 schema 1.1 落地后切换 quality_axes SSOT（或最小 axes writer
   提前入切片一）——切片一不得依赖未实现的切片二。
2. （codex②）report_validity 落盘位置二选一钉死：独立顶层字段，或 quality_axes.report 轴——
   只表达「报告可解析/可信」，不覆盖 product verdict。
3. （codex③）P0-B 渲染可见性拆两个验收节点：p0b-calibrate（冻结正/反样本数、可接受误报率、
   阈值版本、升级条件）→ p0b-enforce；WARN 观察期不得把 P0-B 标记完成，enforce 完成才算达成。
4. （codex④）人工接受的债务条目 status=accepted（含 accepted_by/acceptance_receipt），
   不改写成 closed——closed=已修复，accepted=仍存在但用户明确接受，审计语义分立；
   两者均不再阻断 release。
5. （codex⑤）P1-E 措辞收紧入规格：「产出单项可判事实与结构化 finding；硬不变量可阻断，
   连续指标默认 advisory，禁止用单一全局相似度直接裁决整体质量」——与离散事实 FAIL 规则不冲突。
6. （cursor①）crop 四条件中「provenance 可验证」在规格里钉死可接受来源枚举（外部裁剪工具 /
   人签 receipt / 已验真产物哈希）；裸 user_requirement 不作验真金牌（沿 P0-6 既有语义）。
7. （cursor②）锚点字符串补 ArkUI .id() 合法字符集/长度约束条款。
8. （cursor③）≥4/5 首跑预期管理：盲宿主第一轮多半走「显式接受债务」而非一次清完——设计内
   诚实成本，交互告知文案与之对齐。

实施顺序终确认（codex）：P0-0 → P0-A 切片一 → P0-A 切片二 → P0-B（calibrate→enforce）→
P0-C → P0-D → P1-E → P1-F → P1-G（新发布包）。

# Review 采纳记录·三轮（2026-07-17，codex「方案层面通过」+ cursor「可以开干」——定稿轮）

- codex 上轮问题复核表 11 项全数确认「已修复/已定案」；本轮 6 项契约细节全部采纳压入 todo：
  ①resolution 入 schema + 三条不变量 + 顶层 verdict 按 phase_advance / completion 按
  release 双投影分立（P0-A）；②跨阶段传播改「新鲜 summary+receipt」SSOT 链路防 TOCTOU +
  legacy 1.0 不作 1.1 completion 干净依据（P0-A）；③crop 禁令收窄——禁「执行/自证」不禁
  「消费可信产物」（四条件放行，P0-B①）；④kit 目标目录四级解析算法（勿硬编码 03-CommonBusiness）
  + 实例语义锚点 maison:<feature>:<screen>:<node>:<instance_key> + gallery fixture（P0-C）；
  ⑤rubric 阈值验收前冻结、本 plan 定死 ≥4/5（3 分须显式 accepted_debt_id，1-2 分不过；
  弃「宿主复验后校准」——目标后移风险）+ receipt 增 rubric_version/policy_hash +
  screens 结构化映射（P0-D④）；⑥两类确定性信号分立（离散事实可直接 FAIL，连续指标默认
  advisory、超冻结阈值才升级）+ deterministic_feedback 机器派生非 agent 开关 +
  framework_package_digest 身份（P1-E）。
- codex 附加「三态清偿」采纳（P1-F：source/binding/render 全 VERIFIED 才关债务，防假清偿）。
- codex 终审要求「先建 OpenSpec change 再编码」采纳 → 新增 P0-0（六域规格）。
- cursor 三注意采纳：completion 投影标签不绕过 verify-feature-completion（P0-A 补句）；
  P0-C 首日三项记实施记录（保留）；P1-G 必须用含本 plan 的新 framework 发布包（补前置硬条件）。

# Review 采纳记录·二轮（2026-07-17，codex 有条件通过 + cursor 通过，已逐条 ground-truth 核实）

- **codex B1（FAIL/UNVERIFIED 误投影 AWAITING_HUMAN_REVIEW）→ 采纳，P0-A 重写**。核实：
  verify-feature-completion.ts:99-107 已定义 needs_fix（FAIL/血缘 stale/attestation 失配 →
  FEATURE_INCOMPLETE，**不是**待人工）vs needs_human（→ AWAITING_HUMAN_REVIEW 封顶）；
  phase-transition-policy.ts:285-288 blockingFix→PARTIAL。原 P0-A④「任一轴 FAIL/UNVERIFIED
  封顶 AWAITING_HUMAN_REVIEW」确与现行语义冲突——已改为 resolution class + 固定映射表，
  复用现行两类语义不新造。
- **codex B2（多轴 schema/适用性/兼容投影未定义）→ 采纳，P0-A 补契约**。核实：
  summary.schema.json:6 additionalProperties:false、:27 schema_version const "1.0"、
  :33 verdict enum 含 INCOMPLETE（兼容投影正好复用）；verify-feature-completion.ts:157
  顶层 verdict 为承重消费点。已定：1.1 升版、quality_axes 对象化（applicable/
  required_for_release/verdict/blocking_class/source_checks）、旧 verdict=唯一解析器兼容投影、
  非 UI feature NOT_APPLICABLE、release 必需轴与 phase 推进必需轴分离。
- **codex M1（A/B 不留首日）→ 采纳定案 A**（scaffolder 生成、宿主无 framework runtime 依赖）；
  补 uitree 锚点事实（ArkUI 自定义组件运行时可能被展开）→ 稳定 .id() 锚点方案。
- **codex M2（role/criticality 反自报）→ 采纳**：交叉对账 + criticality 机器派生 +
  placeholder_allowed 只管开发期 + 分角色占位（插画不给文字头像）。
- **codex M3（收敛信号语义 + 存量复用）→ 采纳并核实两处存量**：check-testing.ts:2194
  nav 配置缺失已是**档位无关 BLOCKER**（t7，宿主 WARN 系旧发布版）→ P1-E 改「复用+回归测试」；
  visual-rounds-ledger.ts evaluateVisualRound/no-progress fuse 存在 → 扩展不并行。
  stalled/regressing 移出 evidence 轴。
- **codex M4（几何采集受档位影响）→ 采纳并实证**：capture-completeness-check.ts:87/216/732
  多处 pixel-only 早退——新增 deterministic_feedback 策略位与档位解耦。
- **codex M5（人工验收清偿闭环缺失）→ 采纳**：P0-D④ 结构化人工视觉验收 receipt
  （复用 confirmation-registry 信任链；只清偿主观项，不洗确定性 FAIL）。
- **cursor ①（P0-A 不治丑）**：如实——A 治假完成，地板靠 B/C/D，顺序不变。
- **cursor ②（P0-D 扩面交互成本）→ 采纳**：宿主体验预告入 P0-D①，禁旁路。
- **cursor ③（crop 左移禁令缺显式句）→ 采纳**：P0-B① image_input=none ⇒ acquisition:crop
  非法 BLOCKER。
- **cursor ④（P0-C 首日决策项）**：路线已定 A；层级/冲突策略/锚点命名仍列首日决策并记录。
- **cursor ⑤（不可区分判据易误伤）→ 采纳**：P0-B⑤ 两段式（夹具校准 WARN 观察→升 BLOCKER）。
- **cursor ⑥（P0-A 面偏大）→ 采纳**：第一切片=不通过→FAIL+跨阶段阻断，第二切片铺多轴。

# Review 采纳记录·一轮（2026-07-17，codex 全量审查，已逐条 ground-truth 核实）

- **B1 negative-verdict 传播缺失 → 采纳为 P0-A 并升第一优先**。核实实锤：
  `conditional_pass_closure`（洞⑥）只覆盖「有条件通过」，`不通过` 分支「本门禁不适用→PASS」
  （check-review.ts:894）；宿主 review 终态「不通过+3 BLOCKER」（review-report.md:120）
  vs summary.json `verdict:PASS/closed`。全库 extractDeclaredVerdict 消费点仅
  conclusion_with_verdict（一致性）、conditional_pass_closure（仅有条件通过）、
  check-testing 达标系（一致性语义）。
- **B2 自动降级破坏防伪闭环 → 采纳，P0-D 重写**。goal-fakepass-hardening spec:48 档位三态
  已是现行规范；原「authoritative_refs 非空→desired 自动 pixel_1to1」删除；真实缺口=检测
  只在 goal preflight 生效、宿主逐阶段路径漏检。
- **B3 复合措辞误导 → 采纳**。多轴 verdict + completion_status 枚举。
- **M1-M5 → 全部采纳**（可执行 kit / role-aware / 品牌色纪律 / JSON SSOT / 可复现验收）。
- **事实修正三处 → 全部采纳**：8 屏非 7 屏（facts.md「7 张」系宿主侧记录错误）；
  「空白 PNG」软化为「渲染不可见/疑似空白待 jimp 固化」；「增强只对有视觉路径生效」拆四点。

# 证据索引（review 用）

- 负面裁决不传播：`review\review-report.md:120` vs `review\reports\summary.json`；
  `harness\scripts\check-review.ts:428`、`:894`
- 状态投影现行语义：`harness\scripts\utils\verify-feature-completion.ts:99-107`
  （needs_fix/needs_human）、`harness\scripts\utils\phase-transition-policy.ts:285-288`
  （blockingFix→PARTIAL / pendingHumanReview→AWAITING_HUMAN_REVIEW）
- summary schema 现状：`harness\schemas\summary.schema.json:6,27,33`
  （additionalProperties:false / const 1.0 / verdict enum 含 INCOMPLETE）；
  顶层 verdict 承重消费：`verify-feature-completion.ts:116-124,157`
- nav 门禁 HEAD 已档位无关 BLOCKER：`harness\scripts\check-testing.ts:2194-2214`（t7）
- pixel-only 早退（几何/完备性检查）：`profiles\hmos-app\harness\capture-completeness-check.ts:87,216,732`
- 视觉轮次账本存量：`harness\scripts\utils\visual-rounds-ledger.ts:178+`（evaluateVisualRound）
- 档位三态现行规范：`openspec\changes\goal-fakepass-hardening\specs\goal-runner\spec.md:48-57`
- 缺省档位：`harness\scripts\utils\fidelity-shared.ts:59-66`
- 视觉层 VL SKIP：`coding\reports\summary.json:50-58`；素材链断裂：`spec\ui-spec.yaml:4-6,515+`、
  `coding\reports\script-report.json:232-253`；非 pixel 档实锤：`coding\reports\...\trace.json:106`、
  `review\review-report.md:68`；结论裸奔：`testing\test-report.md`；
  盲档工作法：`skills\feature\spec\reference\ui-spec.md:221-231`
