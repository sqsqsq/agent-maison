---
description: Plan 与 semver 版本窗口绑定；新建 plan 须打 version 标签
alwaysApply: true
---

# 版本演进（plan ↔ semver）

SSOT：根 [`AGENTS.md`](../../AGENTS.md)「版本演进策略」节。

## Agent 禁止擅自改版本（BLOCKER）

**未经用户明确通知，不得**改动任何版本号或版本窗口绑定，包括但不限于：

- 根 `package.json` 的 `version`
- `npm run release:version -- bump` 或等价 bump 操作
- `.cursor/plans/*.plan.md` 的 `version` / `deferred_to` / `deferred_from`
- `RELEASE-NOTES-v*.md` 所对应的发布版本叙事（勿擅自新建下一版说明）
- 在回复中「建议现在 bump 到 x.y.z」并自行执行

当前在研窗口以用户口头说明为准（例如「仍在 2.3.0 开发」）；**默认保持 `package.json.version` 不变**。

若 `release:check-plans` 因未完成 plan 失败，只报告原因，**不要**用顺延 `deferred_to` 或 bump 替用户做决定；由用户选择完成 plan、取消 todo、或手动顺延。

## 新建 plan（BLOCKER）

在 `.cursor/plans/*.plan.md` frontmatter 中**必须**包含：

```yaml
version: <读取 package.json 的 version 字段，如 2.1.0>
```

紧随 `name` / `overview` 之后。

## 顺延到未来窗口

本窗口未做完、不进当前发布时：

- 将 `version` 与 `deferred_to` **同时**设为未来目标版本（二者必须相等）。
- 不要用 `deferred_to` 记录来源；来源用可选 `deferred_from`。

## bump 速查

- 小修补 → `--patch`；中大型演进 → `--minor`；架构大改 → `--major`。
- 发布前：`release:check-plans`（当前窗口 plan 须全完成或已顺延）。
- 发布后：`release:version -- bump --<level>`。
