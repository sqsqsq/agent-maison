---
description: Plan 模式执行期：可更新 todo 状态，勿改计划正文
alwaysApply: true
---

# Plan 执行约定

当用户要求「按计划实施」且附带 `.cursor/plans/*.plan.md`（或等价计划文件）时：

## 允许

- 将 frontmatter / todo 列表中的 **`status` 标为 `completed` / `in_progress` / `cancelled`**，以反映实际进度。
- 在计划文末追加 **「实施记录」** 小节（日期、验收命令、偏离说明），若用户明确要求记录执行结果。

## 禁止（除非用户明确要求修改计划本身）

- 改写计划的 **目标、背景、设计、改造点列表、验收标准** 等正文（即用户已定稿的「要做什么」）。
- 用实施过程中的新发现 **替换** 原 plan 的 scope（应新开变更或与用户确认后再改 plan）。

## 与 Cursor Plan 模式的关系

系统提示中的「Do NOT edit the plan file」应理解为 **不要改计划内容**；**更新 todo 完成状态不算违规**。若与用户口头约定冲突，以用户最新说明为准。

## 仓内 SSOT

与根目录 [`AGENTS.md`](../../AGENTS.md) 中 **「Plan 执行（开发仓）」** 一节一致；两处冲突时以用户最新口头说明为准。
