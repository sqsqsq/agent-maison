## 1. Contract and topology freeze

- [x] 1.1 Run the active-change and production semantic inventory; classify quality pass keys, ordinary input provenance, machine evidence, convergence fuses, external authority, and legacy-only fields in `design.md`.
- [x] 1.2 Strict-validate and archive the completed canonical dependencies: responsible-phase routing, simplified visual trust, runner-owned facts, product selection, device policy, and agent containment.
- [x] 1.3 Create proposal/design/delta specs for `autonomous-recovery-without-human-gates` and strict-validate the change before production edits.
- [x] 1.4 Reconcile the contradictory active deltas in P0 skip, critic loop, layout oracle, and goal-host replay so future archive cannot reintroduce human quality authority; preserve unrelated pending host verification honestly.

## 2. Phase ownership and invocation write boundary

- [x] 2.1 Implement the shared phase write-boundary resolver from resolved workflow, phase-contract `produces`, artifact/evidence registry, coding scope/module paths, profile UT roots, and testing protected sources.
- [x] 2.2 Add unique/no-owner/multi-owner handling for new files and full/lite/custom phases without an owner manifest or hard-coded six-phase table.
- [x] 2.3 Snapshot normalized path/content hashes around the agent process only; exclude pre-existing dirty state and runner-owned event/summary/manifest/state writes, and deduplicate role=both paths.
- [x] 2.4 Enforce write discipline in all phase prompts/skills/rules, including removal of plan's permission to create/repair spec-owned acceptance artifacts.
- [x] 2.5 Record `phase_write_violation` path/owner/pre-post hashes, invalidate invocation plus owner/downstream closures, preserve bytes as untrusted, and route first stable violations through existing `backtrack_to_phase`.
- [x] 2.6 Add resolver/attribution tests for canonical and custom chains, coding scope, profile UT roots, testing source protection, pre-existing dirty files, new files, and repeated/unstable violations.

## 3. Total upstream-gap disposition and closure recovery

- [x] 3.1 Implement a pure total disposition for earlier `missing|failed|legacy_unverified|unclosed|pruned|stale` gaps using trusted candidates, workflow order, provenance, freshness, capability facts, and existing budgets.
- [x] 3.2 Normalize/remove dead executable uses of `run_phase:*`, `rerun_phase:*`, and `restore_inputs_and_rerun`; keep them display-compatible only and execute recovery solely through `backtrack_to_phase`, runner refresh, closure completion, capability defer, retry, or precise terminal/fuse outcomes.
- [x] 3.3 Generalize assess → goal-assess-driver → goal-runner across every earlier owner/current pair in full/lite/custom chains, including target-absent, downstream invalidation, grouped candidate handoff, crash/resume, budget, and fingerprint behavior.
- [x] 3.4 Make partial phase-closure publication recognizable before generic freshness rejection and idempotently resumable across staged summary, manifest, pointer, phase state, and canonical rename.
- [x] 3.5 Remove arbitrary closed-summary evidence rebinding; allow refresh only with explicit runner-owned equivalence proof and otherwise invalidate/backtrack the owner.
- [x] 3.6 Add gap-matrix and closure fault-injection tests for every publication cut, repeat invocation, stale/provenance class, and bc-openCard read-only/backtrack path.

## 4. Runtime fidelity machine evidence

- [x] 4.1 Add `runtime_step_telemetry` support/version handshake to the existing provider/profile capability resolver using the shared P0-device applicability predicate.
- [x] 4.2 Project unsupported or bounded-probe-unavailable telemetry before spawn to existing capability-missing/external defer with zero content retry.
- [x] 4.3 Extend existing Hylyre/device-test evidence with ordered per-checkpoint action, actual hit target/bounds, screen transition, required/forbidden observations, outcome, device/provider/run/attempt identity, and full flow/plan/HAP/source/trace hash binding.
- [x] 4.4 Verify complete P0 checkpoint coverage and reject missing, forged, stale, replayed, cross-identity, misordered, wrong-target, or provider-failed evidence as ordinary testing BLOCKER failures when support was declared.
- [x] 4.5 Connect verified runtime observations to testing evidence manifests and completion without adding a confirmation receipt, capability-report current-output input, new quality axis, or sidecar ledger.
- [x] 4.6 Add the full capability-versus-evidence test matrix, including unsupported→defer and supported-but-missing/invalid→FAIL/retry as distinct outcomes.

## 5. Retire human quality pass keys and waivers

- [x] 5.1 Remove `visual-confirm`, human visual acceptance, `confirmed_by`/`human_confirmed` gate effects, and human final-sign exemptions; retain only legacy parsing/provenance with new-writer suppression.
- [x] 5.2 Replace `repair_adjudication_pending` with deterministic/provider candidate materialization or required evidence FAIL/UNVERIFIED/capability defer; keep optional uncertainty advisory only.
- [x] 5.3 Keep `repair_not_converging` as a cumulative fuse while preventing same-run resume/human action from clearing attempted identities or fingerprints.
- [x] 5.4 Remove `--allow-blind-visual`/`allow_blind_visual` from new CLI/manifest writers and launch policy; tolerate legacy fields as ignored and derive strict/optional behavior from requirement plus capability facts.
- [x] 5.5 Remove fidelity, P0 skip, conditional review, behavior switch, and source-mutation waiver consumers; migrate flow truth to spec evidence and crop/bbox provenance to machine-verifiable source/hash/tool facts.
- [x] 5.6 Remove legacy receipt issuance/guidance/trust-key/schema/script consumers and delete the generic confirmation receipt module only after production zero-consumer proof; preserve narrowly classified genuine external authority.
- [x] 5.7 Update phase rules, schemas, package scripts, skills, templates, confirmation registry, runbook, and `MIGRATION.md` for compatibility-read/new-writer-stop semantics.

## 6. Completion, legacy migration, and correction semantics

- [x] 6.1 Make current quality-axis writers stop producing quality-derived `needs_human`/`AWAITING_HUMAN_REVIEW`; legacy readers reproject to repair, capability-missing, optional advisory, or legacy diagnostics.
- [x] 6.2 Keep phase advance on phase matrix + `projectPhaseAdvanceVerdict` and completion on `required_for_release` + `projectReleaseReadiness`/`verify-feature-completion`; add no persisted `required_for_phase_advance` field.
- [x] 6.3 Require fresh machine visual/runtime evidence, all applicable release-required axes PASS, all required phases closed, no open trusted BLOCKER/MAJOR/P0 gap, and committed recovery transactions before `FEATURE_COMPLETED`.
- [x] 6.4 Treat old receipt/confirmed/allow-blind artifacts as readable but inert without bulk consumer rewrites; ensure prior receipt-derived PASS is recomputed from current machine evidence.
- [x] 6.5 Route post-delivery user feedback into a correction/successor run at spec or the responsible implementation phase without changing predecessor completion evidence.

## 7. Diagnostics and observability

- [x] 7.1 Project stable recovery reasons, owner/target phase, gap kind, safe changed paths/hashes, disposition, and budget/fingerprint facts through progress, report, status, and run-state reducer.
- [x] 7.2 Keep capability defer, write violation, target absence, integrity/concurrency, budget/fingerprint fuse, genuine external authority, and framework defect distinct; remove generic “external wait/recovery condition” summaries for known cases.
- [x] 7.3 Add machine-assertable bc-openCard incident projection for plan write violation → spec owner → automatic backtrack/revalidation without first HALT or dead `rerun_phase:spec` guidance.

## 8. Verification and closeout

- [x] 8.1 Add/adjust focused unit and fixture suites for owner resolution, gap disposition, backtrack invalidation/resume, closure fault injection, human-key retirement, visual/provider routing, runtime evidence, completion projectors, and legacy compatibility.
- [x] 8.2 Run a production semantic zero-consumer scan with an explicit allowlist for legacy readers, external authority, fixtures, migrations, and negative tests; remove contradictory canonical specs/docs/prompts.
- [x] 8.3 Run `npm run openspec:validate`, `node scripts/check-plan-version.mjs`, TypeScript checks, and all affected focused tests.
- [x] 8.4 Run `cd harness && npm test` for publishable runtime changes and record the result.
- [x] 8.5 Run mandatory `npm run release:verify` once at batch closeout, review the final diff/LF state, and leave consumer-host smoke to a separately user-triggered host task.
