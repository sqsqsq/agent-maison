# Superseded

本 change 于 2026-08-14 被 `simplify-visual-trust` 取代，未完成的宿主回灌任务取消。

其中 `effective_policy=blind_safe`、artifact/policy ledgers、hash-chain、checkpoint/head/HWM、`vision_lineage` 等设计不再是运行时要求，不得从本归档规格恢复实现。仍有价值的当前调用 capability/reference receipt 与内容质量门禁，以 `simplify-visual-trust` 的差异规格为准。
