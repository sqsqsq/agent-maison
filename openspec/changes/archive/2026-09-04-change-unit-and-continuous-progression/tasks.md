## 1. Change Unit artifact and identity

- [x] 1.1 Add `change-unit@1` JSON schema for canonical identity, revision, priority, one blueprint owner, P1-shaped provenance, bounded-transformation fields, explicit dependencies/blockers, and forbidden authored Feature/ready/completion/closure state.
- [x] 1.2 Implement the deterministic `blueprint/component/<component_id>/change-units/<change_unit_id>.yaml` path/ref loader with safe-segment checks, raw-byte SHA-256 binding, stable component directory enumeration, and no arbitrary-path/Feature/legacy fallback.
- [x] 1.3 Implement CU schema/identity/semantic validation for non-empty purpose/provides/design refs/ownership touches/invariants/predicates/verification/safe intermediate state; derive the injective Feature id from component/CU identity, reject overrides or an existing Feature bound to another CU, allow only truly absent preconditions/requires/blockers to be empty, and reject empty horizontal shells or unknown authority fields.
- [x] 1.4 Add positive/negative unit cases for path/YAML/ref/provenance mismatch, second blueprint ownership, authored/duplicate Feature identity, self-reported hash/status/ready/done/closure, unsafe intermediate state, and deterministic enumeration.

## 2. Blueprint design refs and constructability

- [x] 2.1 Reuse the P1 canonical blueprint loader/resolver for every owner/design ref and require one component/blueprint/revision/source/artifact identity across blueprint/view/node/relation/flow/decision/contract targets.
- [x] 2.2 Implement current-delta closure derivation from target predicates, touches and design roots, including development owner, design-basis relation/decision/contract, scenario traversal, cross-view relations and conditional runtime-flow obligations.
- [x] 2.3 Implement the design-constructability verdict: current-closure unknown/open decision/blocker/unapproved contract blocks, a future owner/needed-by decision outside the closure does not, and blueprint-invalidating facts return reconciliation rather than permitting a Feature TBD.
- [x] 2.4 Add counterexamples for dangling/mixed-revision refs, omitted changed view/relation/scenario/flow addresses, unresolved current decisions, unrelated future decisions, and blueprint hash/schema drift.

## 3. Feature construction projection

- [x] 3.1 Extend the existing Feature contracts schema/types/loaders with `change_unit_ref` plus ID-only predicate/provide/design-ref mappings to implementation/symbol/test refs; reject copied/redefined CU content or unknown IDs and preserve compatibility for Features without that section.
- [x] 3.2 Update spec/plan Skill contracts and affected plan/coding/review checkers to derive the Feature id, inherit CU intent by reference, require complete concrete mappings for every canonical CU obligation, and route newly discovered blueprint conflicts to P1 without changing the CU definition.
- [x] 3.3 Keep `contracts.state_management` as the sole Feature runtime-construction authority and mechanically derive CU-bound `use-cases.yaml`/ephemeral DAG obligations from ordered steps, failure/recovery branches, shared-state consumers, lifecycle recovery and unit/both scope; update the existing template/type/loader/gates in one chain and reject authored opt-outs.
- [x] 3.4 Add projection counterexamples for stale CU hash, copied/redefined CU or blueprint content, missing/unknown predicate/provide/design mappings, a parallel runtime section, orphan publication/consumer, missing replay/cleanup or background recovery, and required-but-missing use-case/DAG; prove simple read-only/first-load paths need no fake mutation/subscription or unnecessary flow artifact.
- [x] 3.5 Enforce the first shared runtime chain and first approved host-evolution seam as vertical units with real owner/contract, first Provider, real Consumer and executable/contract verification; reject empty Store/EventBus/interface units.

## 4. Dependencies, blockers, ready set, and selection

- [x] 4.1 Implement exact same-blueprint `requires.from_change_unit_id + provide_id` matching; satisfy a require only from the provider CU's derived Feature with matching VALID completion and an all-targets-resolve-and-remain-admitted carry-forward verdict, never from priority, execution order, goal labels or capability seam dependencies.
- [x] 4.2 Implement structured blocker validation and activity derivation, requiring probes for machine-observable release conditions and authority evidence/revision for human-only resolution; reject `resolved: true` self-reporting.
- [x] 4.3 Implement the non-persistent `ABSENT|VALID|STALE|INVALID` completion adapter and disposable ready-set projection: derive Feature id, workflow, track and expected chain from existing SSOT, distinguish never-run/not-completed from damaged evidence, report per-unit reasons, and never consume file existence or self-reported done.
- [x] 4.4 Implement execution-precedence cycle/SCC detection and `silent_progress_stall` when unfinished predicates have neither ready CU, active run nor legal blocker; do not create a general relation registry or persist condensation state.
- [x] 4.5 Implement the static selector using ascending numeric priority then stable `change_unit_id`, returning at most one selected CU and preserving other candidates without writing dependency edges.
- [x] 4.6 Add dependency/ready/provider tests for missing or wrong provides, cross-namespace pseudo-matches, completion ABSENT/VALID/tampered/missing-original states, duplicate Feature binding, cycles, legal blockers, silent stalls, multiple ready candidates, stable tie-break and active-run suppression.

## 5. Thin Goal Mode progression and lifecycle

- [x] 5.1 Implement the stateless progression decision/loop (`resume_active | select_one | blocked | ready_for_component_closure`) with an injected existing Goal Mode caller and a fresh derivation before and after every unit.
- [x] 5.2 Build the deterministic Goal Mode handoff from canonical CU path/ref/hash, blueprint ref and derived Feature id; resolve expected track/chain through the existing workflow/track SSOT, keep manifest/events/reducer/receipt/evidence/recovery authoritative, and bind completion back to the exact Feature contracts/CU revision.
- [x] 5.3 Implement rolling invalidation for blueprint/CU revision or fingerprint changes: stale unimplemented CU design/ready/mapping results; preserve completed CU artifacts and Goal Mode history; carry forward only when every historical stable target still resolves and remains admitted, otherwise block dependents and return to P1; require a new revising/superseding CU id for corrections and build no semantic-diff/invalidates engine.
- [x] 5.4 Add the `change-unit-progression` Skill and register it through the existing `skills.index.yaml`/bundle chain; document standalone Feature compatibility, the single-main-agent concurrency assumption and the handoff to P3 without claiming closure.
- [x] 5.5 Add static provider-boundary enforcement and tests for decomposition (optional), relation/ready analysis (required) and candidate selection (required): decomposition emits only temporary candidates, consumer validation writes provenance-bearing canonical CUs, exit cleans candidates but not accepted artifacts, and missing/replacement/duplicate authority cannot mutate Goal Mode facts.

## 6. Fixtures and integration regression

- [x] 6.1 Add valid canonical fixtures for at least three dependent provenance-bearing vertical CUs over one P1 blueprint, their deterministically derived distinct Features, independent same-priority candidates and a current structured blocker.
- [x] 6.2 Add negative fixture bundles for artifact/ref/provenance/Feature-binding identity, design closure, ID-only construction mapping, runtime/use-case/DAG obligations, unsafe intermediate state, empty horizontal units, dependency cycles/stalls and P2/P3 authority boundaries; all mutations must enter the production validator and assert exact issue ids.
- [x] 6.3 Add a fake/injected Goal Mode integration that advances A→B→C one at a time only after workflow/track-derived authoritative completion validation, covers ABSENT/VALID/STALE/INVALID, and proves selection order is not written back as a dependency.
- [x] 6.4 Add failure/pause/awaiting-human/resume integration cases showing the outer loop stops on the same run, starts no second CU, and recovers by rereading existing facts without a P2 ledger/checkpoint.
- [x] 6.5 Add blueprint/CU-revision cases showing future mappings stale while completed history remains: all historical targets resolving and admitted permits carry-forward; missing/replaced/unknown/open/blocker targets return to P1 without semantic diff; add first-seam vertical-slice and later-Provider cases proving only decision refs plus exact requires/provide ids matter, never priority or Consumer prose.
- [x] 6.6 Register every P2 suite in the existing unit/fixture runners and add mutation checks for constructability, ID-only mapping, runtime/use-case/DAG derivation, exact dependency matching, four-state completion, carry-forward, cycle/stall, selector and provider-boundary sub-checkers.

## 7. Verification and handoff

- [x] 7.1 Run TypeScript typecheck and all focused P2 unit/fixture/Goal Mode integration suites; record exact pass counts and verify every negative fixture fails through the production entrypoint.
- [x] 7.2 Because P2 extends existing Feature contracts/types/phase and Goal Mode integration while preserving `contracts.state_management` authority, run `cd harness && npm test` and resolve all regressions without weakening standalone Feature behavior.
- [x] 7.3 Run `npm run openspec:validate`, `node scripts/check-plan-version.mjs` and `git diff --check`; if `openspec update` was used, first rerun `node scripts/patch-openspec-artifacts.mjs` as required.
- [x] 7.4 Confirm no breaking consumer migration was introduced; update `MIGRATION.md` only if implementation evidence proves otherwise, and keep P3 Component closure explicitly out of this change.
- [x] 7.5 Leave `node scripts/check-plan-version.mjs --release` and `npm run release:verify` unchecked until total-plan m5/MG executes the batch release gates; do not claim either passed before that run.

## 8. M5A evolution-workspace and identity correction (added 2026-08-21, plan e2a7c4b9)

> Supersedes the `blueprint/component/<component_id>/change-units/` path and the `(component_id, change_unit_id)` Feature identity encoding described by the completed tasks above; those tasks stay as implementation history. The retired root path was never released and has no real host data: hard cut, no compatibility layer, no dual-read, no migrator.

- [x] 8.1 Move the CU canonical artifact to `<features_dir>/<blueprint_id>/<change_unit_id>/change-unit.yaml` (fixed filename, directory name == `change_unit_id` == YAML root, `paths.features_dir` resolved, `change_unit_id != "blueprint"`); rewrite `change-unit-path.ts` (`changeUnitDirectory`/`changeUnitPath`/`loadCanonicalChangeUnit`) to address by `(blueprint_id, change_unit_id)`, enumerate only same-workspace subdirectories containing `change-unit.yaml` sorted by directory name, and delete every retired-root concatenation (no read, no fallback, no scan).
- [x] 8.2 Add required `blueprint_id` to the CU root and to `ChangeUnitRef` in `harness/schemas/change-unit.schema.json` and `change-unit-model.ts`/`change-unit-validator.ts`; tighten only the path-bearing `blueprint_id` pattern to a safe path segment; verify root `blueprint_id` == path segment == `component_blueprint_ref.blueprint_id` and root `component_id` == owner blueprint `component_id`.
- [x] 8.3 Re-encode the logical Feature identity as `cu-` + `base64url(blueprint_id + "\0" + change_unit_id)`; make invalid `cu-` payloads fail closed; route identity→physical path exclusively through the single Feature path SSOT defined by the `feature-artifact-layout` delta (no P2-local concatenation); rewrite `inspectDerivedFeatureBinding` to the `available | in_progress | matched | conflict | orphan(fail-closed)` state machine with artifact detection reused from `PHASE_SCOPED_ARTIFACTS`.
- [x] 8.4 Implement the `feature-artifact-layout` MODIFIED delta: one neutral pure function mapping logical feature identity to physical relative path (`<feature_id>` or `<blueprint_id>/<change_unit_id>`), consumed by `config.ts` `featureDirResolved` and the three `<feature>` template substitutions (receipt/report, including the `featuresDirAbs` override branch); collect every direct `<features_dir>/<feature>` concatenation in production code (goal-runner, goal-manifest, goal-progress, fidelity-shared, spec-loader, receipt-path-reconcile, context-facts, check-ut, backfill-context-exploration, correction-layer-reconcile, phase-closure-finalizer, phase-evidence-manifest, harness-runner — final set by grep) onto that SSOT; add a shared enumerator returning only `legacy | cu` Features with workspace-as-container, orphan and ambiguity fail-closed rules, and switch spec-loader, receipt-path-reconcile and check-catalog enumeration to it. `skill-contract.ts` (framework skills directory) is out of scope.
  The SSOT MUST be a dependency-free module importable by both the TypeScript harness and the plain-Node hooks shipped in the release; **independent production entries outside `harness/scripts` are in scope and MUST consume it, never a private decoder copy**: `agents/claude/templates/hooks/check-phase-completion.mjs` (`<feature>` substitution at ~205 and the report-path resolver at ~538), `agents/claude/templates/hooks/record-verifier-report.mjs` (~164), `harness/scripts/utils/testing-write-boundary.ts` (~30, declared writable/forbidden paths must equal the machine-checked paths), `harness/compat-messages.ts` (`{feature}` substitution ~46), `harness/scripts/check-spec.ts` (hardcoded `doc/features/<feature>/spec/assets` fallback ~343), and `scripts/smoke-consumer-lifecycle.mjs` (~526); any further non-TypeScript caller found by grep joins the same list.
- [x] 8.5 Limit `requires/provides` matching, carry-forward re-validation, ready/blocker derivation and the active-Goal-run check to a single `blueprint_id` workspace; prove that CUs of another workspace (including an earlier evolution of the same `component_id`) never satisfy a require or carry forward.
- [x] 8.6 Update `check-change-unit` CLI, `skills/project/change-unit-progression/SKILL.md`, provider candidate writing, progress-loop requirement binding and all internal signatures to `--blueprint <blueprint_id> --unit <change_unit_id>` addressing; `component_id` is verification output only.
- [x] 8.7 Migrate the change-unit fixture tree to the workspace layout with phase-scoped CU directories and the new encoding; add positive/negative cases: same `change_unit_id` in two workspaces without collision, `in_progress` binding without `contracts.yaml` is legal, orphan Feature inside a workspace fails closed, invalid `cu-` payload fails closed, retired root path is not consulted, custom `paths.features_dir` end-to-end, Goal Mode events/manifest/lock land under `<features_dir>/<blueprint_id>/<change_unit_id>/goal-runs/…` and `context/facts.md` under the CU physical directory regardless of patterns, receipts/reports land under the CU physical directory with the default patterns (and under the custom pattern's own structure with `<feature>` expanded when a custom pattern is configured), and no resolved path contains the encoded identity nor does a `<features_dir>/<encoded-featureId>` shadow directory exist, identity↔path round-trip identity across all enumerators; **shipped hooks**: run `check-phase-completion.mjs` and `record-verifier-report.mjs` against a CU Feature with (a) the default patterns, asserting the checked receipt and the written verifier report resolve under `<features_dir>/<blueprint_id>/<change_unit_id>/<phase>/…`, and (b) a custom pattern such as `requirements/features/<feature>/phases/<phase>`, asserting they resolve to `requirements/features/<blueprint_id>/<change_unit_id>/phases/<phase>/…` with the custom prefix and levels preserved (pattern relative to the project root, only `<feature>` expanded — never relocated under `<features_dir>`); both falsifiable: a hook that substitutes the encoded identity, or that forces the custom pattern under `<features_dir>`, must fail; **boundary text**: assert the testing write boundary declares the CU physical path and equals the machine-checked set.
- [x] 8.8 Prose and templates: state once in the shared skills reference glossary that `<features_dir>/<feature>/…` in framework docs denotes the physical Feature path (`<blueprint_id>/<change_unit_id>` for CU Features); update the "Feature 归档定位协议" line of each phase SKILL.md (spec/plan/coding/code-review/business-ut/device-testing/change-lite) and `agents/claude/templates/agents/verifier.md` to resolve the physical path via the framework instead of concatenating the logical identity; do not rewrite every individual path mention — the glossary definition governs them.
- [x] 8.9 Run `cd harness && npm test`, `npm run openspec:validate`, `node scripts/check-plan-version.mjs` (default mode) and record results; this section does not touch 7.5, `tests/fixtures/component-blueprint/release-semantics.json` or any release gate — those remain with total-plan m5 after M0 + M6 + MG regression are all complete.

> 迁移评估（2026-08-22，M5A t4 codex 二轮）：本 change 的 `feature-artifact-layout` MODIFIED delta
> 引入消费者可见默认行为变化——未显式配置 `receipt_dir_pattern` / `reports_dir_pattern` 时，默认
> 模式从 `paths.features_dir` 派生（`<features_dir>/<feature>/<phase>` 与
> `<features_dir>/<feature>/<phase>/reports`），不再使用字面量 `doc/features/...`；自定义 features_dir
> 边界按“磁盘上是否有显式 pattern”判定（缺失则 normalize 与 BACKFILL 均派生；显式 pattern 原样保留）。
> 自定义 features_dir 且磁盘上无显式 pattern 的宿主，receipt/report 落点随之迁移。显式配置的 pattern 原样保留、
> 默认 features_dir 宿主行为不变。已更新 `MIGRATION.md` §3.1.0。

## 9. M7 设计准备子流程、lite CU 绑定与单 CU 正向路径（2026-08-29 追加，plan f9e2c7b4）

> 本节不触碰 7.5、`tests/fixtures/component-blueprint/release-semantics.json` 或任何 release 门禁。

- [x] 9.1 暴露 P2「设计准备子流程」`harness/scripts/utils/change-unit-design-preparation.ts`：入口 = admitted blueprint（**0 CU 合法**）；complete 复用既有 CU decomposition Seam Card 的 provider 候选 → consumer validator 机制，不新造写入机制、状态或 CLI。
- [x] 9.2 consumer validator 批量接受：整批先全量校验（schema/identity/设计闭包/provenance/来源权威/批内重复/已存在），任一不过即整批拒绝且零字节落盘；通过后**原子**写出 1..N canonical `change-unit@1`，写入中途失败回滚本批。
- [x] 9.3 重复接受 fail-closed：目标 canonical 文件已存在即拒绝，不覆盖；修正已接受单元走新的修订/superseding CU。
- [x] 9.4 终点=design gate / readiness（`deriveDesignPreparationReadiness`），`entersConstruction` 恒 false——**停在 selector 与 Goal Mode 执行之前**，不触碰 P3 closure。
- [x] 9.5 推进决策新增 `design_preparation_required` action：admitted blueprint + 0 CU 与"有 CU 但无 ready"区分开，不再落进通用 blocked；`evaluateConstructionEntry` 与设计准备入口并列声明，施工段前提**不放宽**（仍要求 ≥1 canonical CU）。
- [x] 9.6 `skills/project/change-unit-progression/SKILL.md` 入口前提放宽到"admitted blueprint + 0 CU 可进入设计准备段"，同时写明 selector 与施工段仍要求 ≥1 canonical CU。
- [x] 9.7 CU-bound lite 复用既有 `contracts.yaml.change_unit` sidecar（D2）：`ProjectionPhase` 增加 `change`（lite 轨冻结施工契约的阶段，与 `plan` 同属 `CONTRACT_FREEZE_PHASES`），`check-change.ts` 接入 `checkChangeUnitFeatureProjection(ctx, 'change')`；不在 `change.md` 造降维映射协议。
- [x] 9.8 单 CU 正向路径：枚举 1 个 CU、单候选 ready set、正常推进、完成后交 P3；推进中发现第二个 CU 走正常蓝图调和追加单元，无升级动作、无迁移器、无第二套协议。
- [x] 9.9 正反 fixture：0-CU 入口、候选被 validator 拒绝（零落盘）、原子写出 1..N、重复接受 fail-closed、施工段仍要求 ≥1 CU、CU-bound lite sidecar 在 `change` 阶段被校验、普通 lite Feature 不受影响。
