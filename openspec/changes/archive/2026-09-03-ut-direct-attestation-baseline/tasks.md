## 1. Direct dispatch rebuild (plan f3a9d2c7 T1)

- [x] 1.1 Add a review formal-closure probe (`schema_version` 1.2 + `closure_status` closed + `closure_commit` 1.0) returning closed/open/unverifiable, with I/O and parse failures classified unverifiable
- [x] 1.2 Dispatch direct runs attestation-first: closed → attested adjudication, closed-without-attestation and unverifiable → fail closed, otherwise git fallback that never reads an orphan attestation
- [x] 1.3 Add the attested branch (clean PASS / drift BLOCKER `post_review_source_drift` under the existing `ut_no_src_mutation` id) without `classifySourceDrift` or any authorization channel
- [x] 1.4 Keep the goal branch byte-identical and keep using `hasGoalExecutionSignal()` / `resolveHarnessDiffBaseRef()` instead of restoring retired predicates or reading `HARNESS_DIFF_BASE_REF` directly
- [x] 1.5 (review P1) Treat partial closure evidence as unverifiable: an attestation on disk with a missing/open/legacy summary fails closed instead of downgrading to the commit-blind working diff; reserve the git fallback for features with no observable closure evidence
- [x] 1.6 (review round 2 P1) Replace `existsSync` probing with a three-state top-down path resolution so `EACCES`/`ENOTDIR` — and Windows reporting a mid-path file as `ENOENT` — classify as unverifiable instead of absent; read the summary only after a `present` probe
- [x] 1.8 (review round 3 P1) Walk from the filesystem root with `lstat`-before-follow so out-of-project report/receipt patterns and dangling links cannot reintroduce the `ENOENT` ambiguity; drop the in-project/out-of-project split entirely
- [x] 1.7 (review round 2 P2) Validate `inventory.roots`/`inventory.files` shape in `loadReviewClosureAttestation` so a structurally broken attestation returns an unavailable baseline instead of throwing inside reconciliation and surfacing as a generic framework fault

## 2. Wording and comment convergence (plan f3a9d2c7 T2)

- [x] 2.1 Rewrite business-ut constraint #11: attestation baseline first, `HARNESS_DIFF_BASE_REF=working` / `stale_diff_base` limited to the fallback domain, no instruction to commit coding artifacts
- [x] 2.2 Mark `git-diff.ts` as a fallback collector for this gate and restate `source-drift-facts.ts` as attestation-first for direct mode
- [x] 2.3 Add the baseline-degradation reason line to fallback failures
- [x] 2.4 (review P2) Make the drift remediation executable: restore reviewed content from local history/backup and verify against the attested sha256, since the attestation stores hashes only and coding artifacts may never have been committed

## 3. Tests and fixtures (plan f3a9d2c7 T3)

- [x] 3.1 Unit-cover the whole dispatch: attested clean PASS (uncommitted coding artifacts, and a non-git project), attested drift FAIL (failure_kind/affected_files/no authorization wording), closed-without-attestation fail-closed with no git fallback, orphan attestation not trusted, unverifiable closure fail-closed, and unchanged git behavior when review is not closed
- [x] 3.2 Add the commit-washing negative: a post-review edit stays FAIL after `git commit`
- [x] 3.3 Add profile fixtures `v2_2/ut_no_src_mutation_attested_pass` (host-shape regression) and `v2_2/ut_no_src_mutation_attested_drift_fail`, with attestations generated by the production writer
- [x] 3.4 Leave the existing `ut_no_src_mutation_fail` / `_approved_pass` EXPECTED files untouched and confirm the adjudication predicate scanner needs no allowlist change
- [x] 3.5 (review P1) Add the combination regression: closed review → edit source and commit → delete the review summary → still FAIL, with no git `baseRef` in the details
- [x] 3.6 (review round 2) Add regressions for a broken closure-artifact path (reports directory replaced by a file, asserting `existsSync` would have lied) and for a structurally invalid attestation under a closed summary
- [x] 3.7 (review round 3) Add probe-level regressions: out-of-project mid-path file, dangling junction, and the absent/present/directory baseline that keeps the probe from degenerating into always-unverifiable

## 4. Spec and documentation (plan f3a9d2c7 T4/T5)

- [x] 4.1 Publish this change (proposal + `harness-gates` delta + tasks) covering the direct branch that active `goal-host-replay-fixes` left open, without touching goal semantics
- [x] 4.2 Sweep repository wording for `ut_no_src_mutation` / `HARNESS_DIFF_BASE_REF` so the commit-based remedy is gone and the diff-base remedy is scoped to the fallback domain
- [x] 4.3 (review round 2 P2) Align every claim with what disk evidence supports: "partial closure evidence cannot downgrade" rather than "deleted evidence cannot downgrade", "no observable closure evidence" rather than "never closed", and an executable restore-then-verify remedy in the skill constraint

## 5. Verification

- [x] 5.1 Run `npm test` at the repository root (harness typecheck + unit suites + fixtures)
- [x] 5.2 Run `npm run openspec:validate`
- [x] 5.3 Run `node scripts/check-plan-version.mjs`
- [x] 5.4 Run mandatory `npm run release:verify`
  - Deferred: the release gate is a branch-integration step for `Br_release_3.0.0`, and this change is delivered to the working tree for human review before any commit.
  - 2026-09-03 执行：`npm run release:verify -- --skip-plan-release-gate --skip-typecheck` ALL PASS（in-zip manifest 1093 files、catalog/skills.index/stale-refs/zip 断言全过）；typecheck 由同批 `npm test` 覆盖；plan release gate 另跑 `check-plan-version --release`，仅剩 e7cecd22 未闭环（待用户拍板）。
- [x] 5.5 Host replay on SimulatedWalletForHmos (`bc-openCard-1`)
  - Completed 2026-08-28 (user-driven, framework `e21547c8`): UT summary PASS / 0 BLOCKER / closure_status=closed; `ut_no_src_mutation` PASS on the attestation baseline with the exact attested-clean wording (no git baseRef, no `post_review_source_drift`, no `review_closure_baseline_unavailable`); review-closure-attestation rebuilt at review closure (2026-08-28T13:55:37Z, 114 files / 6 roots, `contracts_sha256` byte-matches current contracts.yaml); zero commits required — host worktree kept 49 uncommitted changes throughout. Verified read-only against host reports on 2026-08-28.
