# unified-responsible-phase-routing tasks

- [x] t1 共享事实层：`repair-candidates.ts`（两层指纹/CorrectionCategory 归属：机器 check id 注册表优先+路径域兜底+宁缺/review 逐条验证信任合取+conditional receipt 抑制+负面两分支/plan·ut·coding 生产点）；summary 可选字段+validateSummaryV11 形状校验+**summary.schema.json 声明**；harness-runner 落盘前组装（report_validity 闸、**artifact resolver 读正式 review 路径**、**classification 优先取 failure_kind**）；verify-review 模板逐条全验+issue-verification 机器可读块（含 evidence 新鲜度绑定）
- [x] t2 通用 assess：`mapCategoryToChainPhase` 严格映射（null→backtrack_target_absent，删 chain[0] 静默兜底；correction 修正意见路由行为保留）；**assess 直读 phase summary 候选（唯一真源，reconcile 不再复制）**；candidates 推荐分支（最上游目标）；`backtrack_to_phase` action；driver earlier 泛化；goal-runner 观测接线+invalidatable 面推导+执行分支（共用预算池/整轮指纹熔断/target-absent halt）+mixed-owner 按阶段过滤注入+**crash/resume 候选恢复与非 repair 回退清空**；ut「缺陷→retry」单测按新语义翻案
- [x] t3 三类 driver：goal 复用既有事务（t2 执行分支）；manual `REPAIR_CANDIDATES` 确认菜单（读 assess 观测同一真源，不擅自跨阶段改文件）；batch 授权区间下界 `[start_phase, through_phase]`（`recommendationAuthorized`，缺下界 fail-closed，custom/lite 按实际 chain 序）
- [x] t4 物理收编（不与 generic route 并存）：testing 证据链验真器产物合并回 `repair_candidates`（写失败 `repair_candidates_unwritable` fail-closed halt）；**删除** goal-runner `backtrack_to_coding` 执行分支与 `ui_scope_violation` 专用 replan 调用点；补 UT product assertion→coding 信任合取生产点；testing→spec 本期不接（无机器判定点，文档不伪装自动路由）。`check-plan`/`device-testing` SKILL 文案经逐条核对**已符合新路由**（device-testing 已写「recommendation 由 assess@1 生成含回修起点」，check-plan 的 scope 失败只提示人工修 spec、未宣称自动路由），无需代码修改
- [x] t5 旧缺陷路由裁决链全删（codex 二轮①）：assess 的 `deterministic_defects → backtrack_to_coding` 分支、goal-assess-driver 同款 fallback、phase-transition-policy 的 testing 专用裁决删除（`deterministic_defects` 仅留诊断/指纹）；testing 有可信缺陷但 summary 路径不可用时同样 `repair_candidates_unwritable` fail-closed（不落回旧路）
- [x] t6 verifier 证据新鲜度双绑定（codex 二轮②）：`evidence` 须同时命中当前涉及文件与当前问题内容片段（只验文件挡不住「同 CR、同文件、问题已变」）；verify-review 契约同步；判别复现入负例
- [x] t7 生产接线共享实现（codex 二轮③）：`buildSummaryRepairCandidates`（harness-runner writer 与测试同调）、`restoreBacktrackCandidatesFromEvents`（goal-runner resume 与测试同调）；R8.3 改为磁盘真实 summary→`assessFeature` 自读→batch driver；源码正则断言全部删除
- [x] t8 验收与规格：生产接线级回归 + 组合级验收 + 端到端 goal 链（scope 越界回 plan、testing 缺陷回 coding 且 must_fix 原文进 prompt；桩 writer 改走生产组装函数）；本 OpenSpec change 的 proposal/delta/tasks 与 plan 记录同步为当前架构（codex 二轮④）
