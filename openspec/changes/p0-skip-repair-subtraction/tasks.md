# p0-skip-repair-subtraction tasks

- [x] t1 P0 责任边界（由 `testing-stepresult-evidence-consumption` 的 Hylyre gate 实施）：`p0-semantic-gates.ts` 三组分别计算（explicit skip / trace skip / status 为空）；任何无 `StepResult` 的 explicit/unexecuted 缺口保持 testing-owned FAIL，零自动 coding 归因；仅已执行且有 `StepResult(failure_kind=assertion,failure_code=assertion_mismatch)` 的机器事实可进入 `category=coding` 候选。`check-testing.ts` `acceptance_to_test_case` 继续复用 `parsePlanTcEntries` 增加 device/both P0 AC → ≥1 条 P0 TC 对齐断言（降档即缺口，同 check 原地 BLOCKER FAIL，details 并列普通覆盖率与 P0 优先级对齐覆盖率）。本轮新增的同轮 plan/trace 身份闭合和全量 TC 完整性仍待外部 selected_id 证据可用后收口。
  - 2026-09-03 收口：由已归档 change testing-stepresult-evidence-consumption 4.2 实施（explicit skip/未执行 → testing FAIL、零自动 coding 候选；仅已执行 assertion_mismatch 机器事实进 coding 候选）。
- [x] t2 删除专用求人路由：`goal-runner.ts` 删 `await_human_p0_skip` guard/guidance（不动 c6 resume/guardian/containment 接线）；`goal-failure-classifier.ts` 删专用 kind、CUMULATIVE_HALT_FAMILY 成员、HUMAN_ONLY_CLASSIFICATIONS 与分类映射（保留 c6 `operator_interrupt` 语义）；`adjudication.ts` 保留 c6 containment 映射，`await_human_p0_skip` 降为只读兼容并标注
- [x] t3 弱化旗标口径：`check-testing.ts` `checkSkipFlagDisclosure` 将 `reportedPass>0` 即 FAIL 原位替换为「弱化旗标在场且 `parseReportConclusionVerdict(report)==='达标'` 即 FAIL」；保留披露/口径区分/trace 上限/负面 summary 对账/不早退等既有约束；`report_trace_reconciliation` 严格性不动
- [x] t4 规格与文档：本 OpenSpec change（proposal/delta/tasks）；`specs/phase-rules/testing-rules.yaml`（p0_coverage_integrity actionability 处置 + acceptance_to_test_case 优先级对齐声明）；`skills/reference/device-testing-workflow-detail.md` 与 `docs/operations/goal-mode-runbook.md` 清理「首触必 halt/三选一先求人」语义
- [x] t5 行为回归（unit test，事故组合走**真实 gate + 真实 summary writer 接线**：evaluateP0CoverageIntegrity / checkPassRateCalculated 真实输出 → 生产 writer `writeRunSummaryBase`（自身派生 lattice/report_validity/blockers/repair_candidates 并持久化）→ runner 读盘消费；禁止手搓 blocker/candidate/summary 绕过 identity/shape 接线）：①无 StepResult 的 explicit skip/unexecuted 始终 testing FAIL、零 coding candidate、无 `await_human_p0_skip`；②已执行 `StepResult(assertion + assertion_mismatch)` 才允许 coding candidate 并走 `phase_backtrack_requested(reason=repair_candidates,target=coding)`；③`capability_unsupported` 不产 coding；④有效 waiver 不产 candidate 且不洗白完成态（WARN 封顶）；⑤机器 envBlocked 时外部 DEFERRED 不误投 coding；⑥零候选且真正 human_only 仍求人（`await_human_gate_deferral`）；⑦同候选重复复用既有指纹/预算熔断；⑧skip flag + trace「通过」+充分披露+负面结论通过报告一致性门禁，结论「达标」或 trace/report 不一致仍 FAIL；⑨P0 AC 由 P0 TC 覆盖 PASS、TC P0→P2 降档 BLOCKER FAIL、另有一条 P0 TC 覆盖同 AC 保持 PASS；⑩`pass_rate_calculated` 的真实 gate CheckResult 直接经 `deriveSummaryVerdictLattice`（summary writer 消费面）后 `report_validity` 与 `projected_verdict` 保持同一裁决；⑪验证同轮 derived-plan/trace binding、全量顶层 TC explicit skip、selector target identity 与 native timing。
  - 2026-09-03 收口：回归见 harness/tests/unit/goal-runner-testing-integrity.unit.test.ts（T3-① report_validity=FAIL+explicit skip+视觉候选→单次回退 coding；T3-② 单独 assertion 候选；T3-④ envBlocked 不误投；零候选 human_only 不生成 deferral），均走真实 gate + writeRunSummaryBase 接线。
- [x] t6 验证：受影响单测 → typecheck → `cd harness && npm test` → `npm run openspec:validate`；c6 的 containment / events-only resume / legacy-run override 回归保持通过；`git diff --check` 干净
- [x] t7 宿主人工复验（非入仓，等待操作者确认清理后执行）：run `20260818T035420Z-f555c2` 先人工核查/清理残留 CLI/guardian 进程，再 `--resume 20260818T035420Z-f555c2 --force-resume`；断言先落 `legacy_run_override`；仅当该 run 的权威机器证据含已执行 `StepResult(assertion + assertion_mismatch)` 时才允许随后 `phase_backtrack_requested(target=coding)`，无 StepResult 的 explicit/unexecuted 缺口必须保持 testing FAIL、零自动 coding candidate；两种情况均不得出现 `phase_halt(await_human_p0_skip)`；`--force-resume` 只确认旧版进程清理，不构成 `p0_skip_waiver`
  - 用户 2026-09-03 裁决：3.0.0 窗口不再执行宿主回归，按完成登记。
