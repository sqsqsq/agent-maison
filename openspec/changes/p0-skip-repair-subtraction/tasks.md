# p0-skip-repair-subtraction tasks

- [x] t1 P0 默认修复：`p0-semantic-gates.ts` 三组分别计算（explicit skip / trace skip / status 为空），unwaived 缺口全为 explicit 时复用 `failure_kind=code_regression` + `actionability=agent_fixable`，其余不产 coding 归因；`repair-candidates.ts` 增 testing 条件生产点（id + FAIL + code_regression 合取 → category=coding，不注册整个 check、不新增自报字段）并把 report_validity 抑制收窄到 review 候选；`check-testing.ts` `acceptance_to_test_case` 复用 `parsePlanTcEntries` 增 device/both P0 AC → ≥1 条 P0 TC 对齐断言（降档即缺口，同 check 原地 BLOCKER FAIL，details 并列普通覆盖率与 P0 优先级对齐覆盖率）
- [x] t2 删除专用求人路由：`goal-runner.ts` 删 `await_human_p0_skip` guard/guidance（不动 c6 resume/guardian/containment 接线）；`goal-failure-classifier.ts` 删专用 kind、CUMULATIVE_HALT_FAMILY 成员、HUMAN_ONLY_CLASSIFICATIONS 与分类映射（保留 c6 `operator_interrupt` 语义）；`adjudication.ts` 保留 c6 containment 映射，`await_human_p0_skip` 降为只读兼容并标注
- [x] t3 弱化旗标口径：`check-testing.ts` `checkSkipFlagDisclosure` 将 `reportedPass>0` 即 FAIL 原位替换为「弱化旗标在场且 `parseReportConclusionVerdict(report)==='达标'` 即 FAIL」；保留披露/口径区分/trace 上限/负面 summary 对账/不早退等既有约束；`report_trace_reconciliation` 严格性不动
- [x] t4 规格与文档：本 OpenSpec change（proposal/delta/tasks）；`specs/phase-rules/testing-rules.yaml`（p0_coverage_integrity actionability 处置 + acceptance_to_test_case 优先级对齐声明）；`skills/reference/device-testing-workflow-detail.md` 与 `docs/operations/goal-mode-runbook.md` 清理「首触必 halt/三选一先求人」语义
- [x] t5 行为回归（unit test，事故组合走**真实 gate + 真实 summary writer 接线**：evaluateP0CoverageIntegrity / checkPassRateCalculated 真实输出 → 生产 writer `writeRunSummaryBase`（自身派生 lattice/report_validity/blockers/repair_candidates 并持久化，report_validity=FAIL 由「结论=达标」报告经真实 pass_rate_calculated 产出）→ runner 读盘消费；禁止手搓 blocker/candidate/summary 绕过 identity/shape 接线）：①report_validity=FAIL 时 `p0_coverage_integrity FAIL+code_regression` 仍被 writer 持久化并回退 coding（review 自由文本候选仍被抑制）；②P0 candidate 单独（披露+结论不达标 → report_validity=PASS）或与视觉 candidate 混合均由 assess 回退 coding（`phase_backtrack_requested(reason=repair_candidates, target=coding)`），不落 WAITING/human、无 `phase_halt(await_human_p0_skip)`；③有效 waiver 不产 candidate 且不洗白完成态（WARN 封顶）；④机器 envBlocked 时外部 DEFERRED 不误投 coding（writer 仍持久化 p0 候选，runner 侧 envBlocked 前置才清空），报告散文自称 external 无机器信号不抑制 explicit-only 候选；⑤零候选且真正 human_only 仍求人（`await_human_gate_deferral`）；⑥同候选重复复用既有指纹/预算熔断；⑦skip flag + trace「通过」+充分披露+负面结论通过报告一致性门禁，结论「达标」或 trace/report 不一致仍 FAIL；⑧P0 AC 由 P0 TC 覆盖 PASS、TC P0→P2 降档 BLOCKER FAIL、另有一条 P0 TC 覆盖同 AC 保持 PASS；⑨`pass_rate_calculated` 的真实 gate CheckResult 直接经 `deriveSummaryVerdictLattice`（summary writer 消费面）后 `report_validity` 与 `projected_verdict` 保持同一裁决
- [x] t6 验证：受影响单测 → typecheck → `cd harness && npm test` → `npm run openspec:validate`；c6 的 containment / events-only resume / legacy-run override 回归保持通过；`git diff --check` 干净
- [ ] t7 宿主人工复验（非入仓，等待操作者确认清理后执行）：run `20260818T035420Z-f555c2` 先人工核查/清理残留 CLI/guardian 进程，再 `--resume 20260818T035420Z-f555c2 --force-resume`；断言先落 `legacy_run_override`，随后 `phase_backtrack_requested(target=coding)`，不得出现 `phase_halt(await_human_p0_skip)`；`--force-resume` 只确认旧版进程清理，不构成 `p0_skip_waiver`
