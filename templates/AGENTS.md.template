# {{AGENT_ENTRY_FILE}} — {{PROJECT_NAME}}（{{PROJECT_TYPE_LABEL}}）全局指令

> 本文件由 `framework/skills/00-framework-init` 基于 `framework/templates/AGENTS.md.template`
> 实例化生成。它是本工程所有 AI coding agent 的**全局系统指令**。
>
> 每次对话开始：先读完这里的全局约束，再按当前任务跳到对应 Skill 执行。
> **这里只列清单，不复述细节**——细节以链接的文档为准，避免上下文漂移。

---

## 一、基础信息

| 维度 | 取值 |
|------|------|
| 项目名 | `{{PROJECT_NAME}}` |
| 项目类型 | `{{PROJECT_TYPE}}` |
| 激活的 agent adapter | `{{AGENT_ADAPTER}}` |
| Framework 接入方式 | `framework/` 子目录（可能为 git submodule） |
| 架构摘要 | {{ARCHITECTURE_SUMMARY}} |

详细架构说明请阅读：[{{ARCHITECTURE_MD_PATH}}]({{ARCHITECTURE_MD_PATH}})。

---

## 二、SSOT（Single Source of Truth）

| 维度 | SSOT 文件 | 说明 |
|------|----------|------|
| 架构与模块划分 | [{{ARCHITECTURE_MD_PATH}}]({{ARCHITECTURE_MD_PATH}}) | 层级划分 + 依赖矩阵。**任何与之冲突的设计一律以本文为准**。 |
| 架构 DSL（机器可读） | [framework.config.json](framework.config.json) → `architecture` | 给 phase-rules / check-*.ts 读取的结构化定义。修改必过 `/framework-init`（UPDATE 模式） |
| 模块画像（Catalog） | [{{MODULE_CATALOG_PATH}}]({{MODULE_CATALOG_PATH}}) | 每个模块的职责 / `NOT_responsible_for` / `easily_confused_with`。**Scope 防错源头**。建模与修改走 Skill 0 · Phase A。 |
| 业务术语表（Glossary） | [{{GLOSSARY_PATH}}]({{GLOSSARY_PATH}}) | 自然语言业务名词 ↔ 权威模块映射。PRD 阶段术语消歧必读。建表走 Skill 0 · Phase B。 |
| 通用编码规范 | [framework/skills/3-coding/templates/coding-standards.md](framework/skills/3-coding/templates/coding-standards.md) | ArkTS 命名、目录、import、资源等编码规则 |
| ArkTS 易错点 | [framework/skills/3-coding/reference/arkts-pitfalls.md](framework/skills/3-coding/reference/arkts-pitfalls.md) | **弱模型必读**：常见错例 vs 正例 |
| 阶段规则（机器可读） | [framework/specs/phase-rules/](framework/specs/phase-rules/) | prd / design / coding / review / ut / testing / catalog / glossary 八阶段 YAML 规则 |
| 自动校验脚本 | [framework/harness/scripts/](framework/harness/scripts/) | 对应 `check-*.ts`，用于 BLOCKER 级门禁 |

---

## 三、核心全局约束（无论在哪个阶段都必须遵守）

### 3.1 架构守门（BLOCKER）

1. **层间依赖方向严格按 `framework.config.json` → `architecture.outer_layers` 声明**，任何反向依赖一律拒绝。
2. **模块内依赖方向按 `architecture.module_inner_layers` + `inner_dependency_direction` 声明**，禁止反向依赖。
3. **跨模块访问必须通过 `architecture.cross_module_exports_file` 声明的出口文件**（默认 `Index.ets`），禁止深路径 import。

### 3.2 术语守门（BLOCKER）

1. PRD 必须以 **`## 0. 术语映射表`** 章节起始，列出原始术语 → 权威模块的映射（见 Skill 1 Step 1.5）。
2. **所有映射必须逐条人工确认**（用户把 `[ ]` 改成 `[x]`），不启用 auto-approve。即便置信度 `high` 也必须确认。
3. 每一条映射的 `权威模块` 必须存在于 [{{MODULE_CATALOG_PATH}}]({{MODULE_CATALOG_PATH}})；否则 `terminology_mapping_table` BLOCKER 阻塞。
4. 术语命中其他模块的 `easily_confused_with` 时，**必须显式亮给用户看**，不允许静默忽略。
5. 用户批准的新术语 / 修正后的映射必须回写到 [{{GLOSSARY_PATH}}]({{GLOSSARY_PATH}})，作为下一次复用种子。

### 3.3 Scope 守门（BLOCKER）

1. PRD 必须声明 `in_scope_modules` / `out_of_scope_modules` / `rationale`（见 Skill 1）。
2. `in_scope_modules` / `out_of_scope_modules` 的每个模块名都**必须在 `{{MODULE_CATALOG_PATH}}` 中存在**（`scope_matches_catalog` BLOCKER），禁止自造模块名。
3. design.md 必须**继承** PRD 的 scope；如需扩展，**停下来**向用户发起「Scope 扩展提议」，获得用户明确批准后写入 `expansions_with_user_approval` 才可继续（见 Skill 2 Step 2.5）。
4. 编码阶段 `git diff` 涉及的所有文件必须落在 design 的 `in_scope_modules` 内（`doc/`、`specs/`、`framework/` 等框架/实例基础设施目录除外）。
5. **严禁静默扩展**：任何"顺手改一下"都必须回到 design 阶段走扩展提议流程。

### 3.4 ArkTS 正确性守门（BLOCKER）

1. 写 `.ets` 文件**前**先扫一眼 [arkts-pitfalls.md](framework/skills/3-coding/reference/arkts-pitfalls.md) 相关条目。
2. **逐文件闭环**：写一个文件 → 立刻 `ReadLints` → 零 error 才能写下一个。**严禁批量生成多文件后再统一 lint**。
3. 不允许出现 `any`、硬编码字符串、未定义资源 key、未导出的 `Index.ets` 符号。

### 3.5 文档与代码同步

- design.md 的 `contracts.yaml`（文件路径 / 接口签名 / 数据模型 / 组件 Props / 资源 key）是编码阶段的强契约，实现必须与之一致。
- 需求交付完成后，如模块边界有变更，必须同步更新 [{{ARCHITECTURE_MD_PATH}}]({{ARCHITECTURE_MD_PATH}}) 与 [{{MODULE_CATALOG_PATH}}]({{MODULE_CATALOG_PATH}})。

---

## 四、工作流与 Skill 路由

每个阶段都有对应 Skill 文档。具体触发方式由当前 adapter（`{{AGENT_ADAPTER}}`）决定——
有的 agent 用 slash（Claude Code 的 `/xxx`），有的 agent 用跳板文件（Cursor 的 `.cursor/skills/`），
也可以直接让 agent 读对应 `framework/skills/<n>/SKILL.md` 进入。

| 阶段 | Skill 路径 |
|------|-----------|
| 0. Framework 初始化 / 升级 | [framework/skills/00-framework-init/SKILL.md](framework/skills/00-framework-init/SKILL.md) |
| 0. 模块画像自举（Phase A） | [framework/skills/0-catalog-bootstrap/SKILL.md](framework/skills/0-catalog-bootstrap/SKILL.md) |
| 0. 术语表自举（Phase B） | [framework/skills/0-catalog-bootstrap/SKILL.md](framework/skills/0-catalog-bootstrap/SKILL.md) |
| 1. PRD 撰写 | [framework/skills/1-prd-design/SKILL.md](framework/skills/1-prd-design/SKILL.md) |
| 2. 技术设计 | [framework/skills/2-requirement-design/SKILL.md](framework/skills/2-requirement-design/SKILL.md) |
| 3. 编码落地 | [framework/skills/3-coding/SKILL.md](framework/skills/3-coding/SKILL.md) |
| 4. 代码审查 | [framework/skills/4-code-review/SKILL.md](framework/skills/4-code-review/SKILL.md) |
| 5. 业务级 UT | [framework/skills/5-business-ut/SKILL.md](framework/skills/5-business-ut/SKILL.md) |
| 6. 真机测试 | [framework/skills/6-device-testing/SKILL.md](framework/skills/6-device-testing/SKILL.md) |

> **Skill 0（catalog + glossary）是所有其它 Skill 的前置**：只有 `{{MODULE_CATALOG_PATH}}` + `{{GLOSSARY_PATH}}`
> 先建好，后续 PRD 阶段的术语消歧与 Scope 守门才有可校验的基准。
> 真实工程启动时，先按 Skill 0 逐模块自举；后续日常需求迭代，只有新增模块 / 术语才回 Skill 0。

**执行规则**：
- 进入某阶段前，**必须完整读一遍对应 SKILL.md**。
- Skill 中引用到的 template / reference / checklist 也是强制阅读（弱模型尤其不要跳读）。
- 每个阶段产物**必须通过** `framework/harness/harness-runner.ts --phase <phase> --feature <name>`（结构 + 规则级）
  以及 `framework/harness/prompts/verify-*.md`（语义级，由独立 verifier 子 agent 执行）。
  具体命令与路径以各 `SKILL.md` 为准。

---

## 五、交付凭证与 Trace

每次完成某个阶段任务，**必须**在以下路径产出一份 `trace.json`：

```
framework/harness/reports/<feature>/<timestamp>/<model>-<phase>/trace.json
```

字段见 [framework/harness/trace/trace.schema.json](framework/harness/trace/trace.schema.json)；
痛点回填结构见 [framework/harness/trace/gap-notes.template.md](framework/harness/trace/gap-notes.template.md)。
**这是弱模型回传问题的唯一渠道**，不要省略。

---

## 六、与用户交互的硬性规则

1. **最小改动原则**：任何不在用户原始诉求内的修改，**先问再做**。
2. **遇到 scope 越界、架构违规、lint 持续失败**：停下来报告，不要"硬着头皮继续"。
3. **不确定 → 用工具验证**：不要凭记忆写 import / 资源 key / 模块路径，用 `Read` / `Grep` 主动查。
4. **产物即契约**：写完 PRD/design 即被后续阶段当作 SSOT 使用，不允许"先写个草稿，后面再改"心态。

---

## 七、快速索引

- Framework 使用说明：[framework/README.md](framework/README.md)
- 全链路验证说明：[framework/harness/harness-runner.ts](framework/harness/harness-runner.ts) 及各 `framework/skills/<n>/SKILL.md`
- Skill 索引：[framework/skills/README.md](framework/skills/README.md)
- 功能需求产物目录：[{{FEATURE_DOCS_DIR}}]({{FEATURE_DOCS_DIR}})
- 功能契约目录：[{{FEATURE_SPECS_DIR}}]({{FEATURE_SPECS_DIR}})
- Skill 0 全局阶段 harness 命令（**无 `--feature`**）：

 ```bash
 cd framework/harness && npx ts-node harness-runner.ts --phase catalog
 cd framework/harness && npx ts-node harness-runner.ts --phase glossary
 ```
