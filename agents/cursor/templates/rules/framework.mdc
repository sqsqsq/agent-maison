---
description: Framework 全局提示——任何阶段都必须遵守的核心约束索引
alwaysApply: true
---

# Framework 全局规则（Cursor）

本规则是 Cursor 会话级的强制提示，任何阶段都必须遵守。**详细内容**参见 `AGENTS.md` 与 `framework/specs/phase-rules/`——本文件只做重点索引，不复述。

## 永恒原则

1. **前置阅读**：每次对话开始，先读完 `AGENTS.md`（agent 入口指令）。
2. **单点真相**：
   - 架构与模块划分 → `framework.config.json` 的 `architecture` 段 + `doc/architecture.md`
   - 模块画像 → `doc/module-catalog.yaml`
   - 业务术语 → `doc/glossary.yaml`
   - 阶段规则 → `framework/specs/phase-rules/<phase>-rules.yaml`
3. **Skill 路由**：
   - 进入某阶段前必须完整读对应 `framework/skills/<n>/SKILL.md`，不要凭跳板摘要动手。
   - 本仓库 `.cursor/skills/<skill>/SKILL.md` **只是跳板**，真实逻辑在 `framework/skills/<n>/SKILL.md`；其中 **由 `render-agents-md` 依据 `doc/extensions/skills/` 生成的跳板**同样只保留一条链到正文（`doc/extensions/...`），禁止扩写业务条款。
   - **禁止向跳板追加**业务条款、选型表或次级文档链接（会形成与正文 SKILL 的双源分叉，agent 易只读跳板漏掉正文闸门）。这些内容写在 `framework/skills/<n>/` 正文及其 `prompts/`、`templates/`、`reference/` 下；跳板**仅保留** frontmatter + **一条**跳转到正文 `SKILL.md` 的路径。如需改跳板默认形态：**先改** `framework/agents/cursor/templates/skills/**`，再跑一次 `/framework-init`（或等价 render）下发到 `.cursor/skills/`；不要只在实例跳板里手工扩写。
4. **BLOCKER 级守门**：
   - 架构依赖方向违规 → 停。
   - PRD 的术语映射表 / Scope 模块不在 catalog → 停。
   - design.md 的 Scope 超出 PRD → 停并发起扩展提议。
   - design.md 缺少「架构影响声明 (architecture_impact)」或 impact 取值非法 → 停。
   - 宿主 profile 声明的 lint / compile BLOCKER 有 error → 停并逐文件修复。
5. **架构文档门禁**：`doc/architecture.md` 只承载**架构级契约**（分层 / 模块集合 / 依赖边 / 出口约定）。feature 级变更（既有模块内新增页面 / 接口 / 数据模型 / 修复）**一律不写入**该文件；仅当 design 的 `architecture_impact != none` 时按 Skill 2 Step 12 分支更新，变更历史由 git 与 `doc/features/<feature>/` 承担。
6. **产物即契约**：写完 PRD / design 即被后续阶段当 SSOT 用，不允许"先写个草稿，后面再改"心态。
7. **最小改动**：任何不在原始诉求内的修改先问再做。
8. **Harness 校验**：每个阶段完成后必须跑 `framework/harness/harness-runner.ts --phase <phase> --feature <name>` 并由独立 verifier 子 agent 审一轮。**重试与闭环一律由 agent 自跑**；禁止让用户手动执行 harness/安装命令（见 `framework-agent-execution.mdc`，BLOCKER）。

## Framework 优先修改原则

当用户反馈 workflow / skill / harness / verifier / init / adapter / hooks / 规则文档等框架行为问题时，**第一优先级是修改 `framework/` 下的源头资产**，而不是直接修改由 framework 生成或分发到工程根的衍生物。

- 优先改：`framework/skills/**`、`framework/harness/**`、`framework/specs/**`、`framework/templates/**`、`framework/agents/**`、`framework/docs/**`。
- 谨慎改：根目录 `CLAUDE.md`、`.cursor/rules/*.mdc`、`.cursor/skills/**`、`.claude/**`、实例根 `framework.config.json` 等生成物 / 实例配置。
- 若衍生物内容不对，先定位对应 framework 模板或生成脚本，修改源头后再通过 framework-init / render / harness 路径验证生成效果。
- 只有在需要让当前实例立即具备新默认值或用于验证 framework 生成效果时，才同步修改实例侧衍生物；同步修改必须说明其源头文件，并避免只改衍生物。
- 目标是让真实工程移植 framework 后自然获得修复，而不是在单个实例里手补一次。

## 冲突处理

若本文件与 `AGENTS.md` / `framework/specs/phase-rules/*.yaml` 冲突，**一律以后者为准**，并立即反馈让维护者更新本 rules 文件。
