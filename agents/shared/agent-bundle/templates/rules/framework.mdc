---
description: Framework 全局提示——任何阶段都必须遵守的核心约束索引
alwaysApply: true
---

# Framework 全局规则

本规则在支持 rules 机制的 agent 中作为会话级强制提示。**详细内容**参见 `AGENTS.md` 与 `framework/specs/phase-rules/`——本文件只做重点索引，不复述。

## 永恒原则

1. **前置阅读**：每次对话开始，先读完 `AGENTS.md`（agent 入口指令）。
2. **单点真相**：
   - 架构与模块划分 → `framework.config.json` 的 `architecture` 段 + `doc/architecture.md`
   - 模块画像 → `doc/module-catalog.yaml`
   - 业务术语 → `doc/glossary.yaml`
   - 阶段规则 → `framework/specs/phase-rules/<phase>-rules.yaml`
   - Agent 行为规约 → `framework/skills/reference/agent-behavioral-principles.md`（Research Sub-Phase 强制前读）
3. **Skill 路由**：
   - 进入某阶段前必须完整读对应 `framework/skills/<scope>/<skill-id>/SKILL.md`，不要凭 bundle 内摘要动手。
   - **bridge 薄跳板**（Cursor `.cursor/skills/`、generic `.agents/skills/` 等）：bundle 内 `SKILL.md` 仅为跳板，真实逻辑在 `framework/skills/<scope>/<skill-id>/SKILL.md`。`inline` 全量物化模式已彻底废弃，所有 bundle skill 一律为 bridge 薄跳板。
   - **禁止向跳板追加**业务条款、选型表或次级文档链接。扩写写在 `framework/skills/<scope>/<skill-id>/` 正文及其 `prompts/`、`templates/`、`reference/`；跳板形态改 `framework/agents/shared/agent-bundle/templates/`，经 init/render 下发。
4. **BLOCKER 级守门**：架构依赖、术语映射、Scope、plan impact（architecture_impact）、宿主 lint/compile、Context Exploration 量化门禁（schema 1.1.0）、Agent 行为规约、Harness + verifier——见 `AGENTS.md` §3.6–§3.7。
5. **架构文档门禁**：`doc/architecture.md` 只承载架构级契约；feature 级变更由 git 与 `<features_dir>/<feature>/` 承担。
6. **产物即契约**：写完 spec / plan 即被后续阶段当 SSOT 用。
7. **最小改动**：任何不在原始诉求内的修改先问再做。
8. **Harness 校验**：每阶段完成后跑 `framework/harness/harness-runner.ts` 并由独立 verifier 审一轮。
9. **用户确认 UX**：凡 BLOCKER / HARD STOP 须 [user-confirmation-ux.md](../../framework/skills/reference/user-confirmation-ux.md) 渐进增强（widget + portable 编号）；须遵循 adapter interaction-renderer；禁止仅要求用户打字。

## Framework 优先修改原则

- **agent-maison 维护仓**：优先改 `framework/` 源头资产，而非只在实例 bundle 手补。
- **消费者实例工程**（submodule/vendor）：**禁止**为过关 UT/harness 修改 `framework/` 已跟踪文件；见 `framework/skills/reference/consumer-framework-boundary.md`。须改 framework 行为时，在 **framework 源仓库** 实现后发版升级。
- 维护跳板模板：`framework/agents/shared/agent-bundle/templates/`；经 framework-init / render 下发到实例。

## 冲突处理

若本文件与 `AGENTS.md` / `framework/specs/phase-rules/*.yaml` 冲突，**一律以后者为准**。
