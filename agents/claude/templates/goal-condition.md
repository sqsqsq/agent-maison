All feature phases from {{START_PHASE}} through {{END_PHASE}} for feature {{FEATURE}} have harness verdict PASS, or deferrable INCOMPLETE recorded as DEFERRED in goal-report. Stop after {{MAX_TURNS}} turns or when budget exhausted. Print harness summary paths in transcript. Each phase is executed by at most one phase-executor subagent dispatched by the thin driver session; the same session context never executes two phases back-to-back, and `--revalidate` is only a check command, never phase progression.
