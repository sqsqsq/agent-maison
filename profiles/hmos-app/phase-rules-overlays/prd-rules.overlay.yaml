# ----------------------------------------------------------------------------
# hmos-app · PRD phase-rules 叠加（ArkUI 等宿主语义）
# ----------------------------------------------------------------------------

phase: prd
version: "1.0"
applies_to: "doc/features/{module}/PRD.md"

semantic_checks:
  ui_component_terminology:
    description: >
      界面描述中引用的 UI 组件应优先使用 ArkUI 术语
      （Column、Row、List、Tabs、Navigation、Swiper 等）
    severity: MINOR
    ai_prompt_hint: >
      检查界面描述中的组件类型是否使用了 ArkUI 组件名称
