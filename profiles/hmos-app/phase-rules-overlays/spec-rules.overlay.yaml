# ----------------------------------------------------------------------------
# hmos-app · spec phase-rules 叠加（ArkUI 等宿主语义）
# ----------------------------------------------------------------------------

phase: spec
version: "1.0"
applies_to: "<features_dir>/{module}/spec/spec.md"

# plan 07a41ec6 T7：ui_component_terminology（ArkUI 术语可读性项）已随 verifier 可读性检查一并裁撤
semantic_checks: {}
