import { describe, it, expect } from '@ohos/hypium';

export default function demoMockMissingFixture(): void {
  describe('demo_v23_mock_plan_missing', () => {
    it('placeholder', 0, () => {
      expect(true).assertTrue();
    });
  });
}
