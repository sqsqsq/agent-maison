schema_version: "1.0"
feature: demo

spies:
  - target_class: DemoRepository
    target_file: 02-Feature/Demo/src/main/ets/data/repository/DemoRepository.ets
    base_strategy: subclass
    methods:
      - name: fetchData
        params: []
        return_type: { text: "Promise<string>" }
        presets:
          - id: ok_fetch
            returns:
              ts_expr: "'ok' as string"
      - name: notRegisteredMethodForUtFixture
        params: []
        return_type: { text: "Promise<string>" }
        presets:
          - id: orphan_only
            returns:
              ts_expr: "'bad' as string"
