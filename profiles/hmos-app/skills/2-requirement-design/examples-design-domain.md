# design.md 示例片段（中性域）

> 历史示例已从旧版专用领域样例迁移并中性化；供核对 contracts / Scope 行文格式。

- `contracts.yaml` 行表示例：`| F1 | 草稿列表刷新 | TaskDemo | 02-Feature | … |`
- `contracts` 包示意：`name: TaskDemo`，`package_path: "02-Feature/TaskDemo"`
- Scope 扩展示意：若新增跨模块接口，先走 `expansions_with_user_approval` 流程并在设计文档登记原因。
