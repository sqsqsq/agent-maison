# `generic` · Skill `code-graph` profile addendum

## 能力边界（占位）

本 profile **不提供** `GraphExtractor` 实现：

- **`module-graph` 门禁**：可运行（读已有 `code-graph.yaml` 做 schema + drift）。
- **生成/刷新 derived**：`bootstrap:code-graph` 会报错「未提供 GraphExtractor」；请使用 hmos-app 等已实现 profile，或待后续 provider。

## 资产

占位模板与 prompt 仅供 `profile-skill-asset:` 解析通过；内容声明 drift-only 边界。
