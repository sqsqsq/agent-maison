# 真机自动化派生计划（generic profile 未启用）

当前 `project_profile=generic`。Hylyre / Hypium 派生计划模板仅在声明 `device_test.run` 为 BLOCKER 的端侧宿主 profile（例如 `hmos-app`）启用。

请阅读 `framework/profiles/<目标 profile>/skills/6-device-testing/templates/test-plan-hylyre-template.md`。
