# `generic` · Skill `2-requirement-design` profile addendum

技术设计以 **中立 `design-rules`** 与实例 `doc/architecture.md` 为准；**不**预设 ArkTS 文件扩展名或 hvigor 任务链。

`architecture_impact` 与 `contracts.yaml` 仍必须自洽；若宿主无 `.ets`，审查与编码阶段规则以实际 profile 的 overlay / addendum 为准。

## Context Exploration Gate（profile 补充）

无 ArkTS/HAP 树时，探索摘要侧重文档与实例 DSL；若工程内确有源码目录，在 `context-exploration.md` 中按实际路径记录已扫范围即可。
