# ArkTS 规范（未启用）

当前 `project_profile=generic`。ArkTS 专用规则、`ReadLints`、`.ets` 细则仅在端侧宿主 profile（例如 `hmos-app`）启用。

请阅读 `framework/profiles/<目标 profile>/skills/3-coding/reference/arkts-pitfalls.md`。
