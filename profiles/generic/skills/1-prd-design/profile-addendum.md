# `generic` · Skill `1-prd-design` profile addendum

`generic` 为**文档/流程型** profile：PRD 仍以 `module-catalog` / `glossary` / `phase-rules` 为守门基准，**不**假定鸿蒙 UI 或 ArkUI 专有条目。

若 `project_profile.phases_disabled` 覆盖了部分阶段，以 `framework/harness` 对 profile 的跳过策略为准；勿将 hmos-app 示例工程的产品名词当作当前实例的 Scope。

## Context Exploration Gate（profile 补充）

不强制额外宿主源码路径；除实例 `framework.config.json > paths` 所列 SSOT 外，若需求引用仓内既有文档/设计稿，在 `context-exploration.md` 的「已检视文件」中如实列出即可。
