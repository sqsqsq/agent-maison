# `generic` · Skill `0-catalog-bootstrap` profile addendum

目录与模块信号以 **通用仓库树 + 文档**为主；无 `oh-package.json5` / `Index.ets` 时，依赖 **README、包清单、目录命名** 等中立线索推断模块边界。

仍须通过 `harness-runner --phase catalog` / `--phase glossary`；**禁止**虚构模块以「凑满」catalog。
