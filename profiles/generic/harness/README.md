# `generic` / harness

`generic` **profile** 不附带 hvigor / hdc / ArkTS AST 等与端侧宿主绑定的 runner 实现。

**Harness 契约 fixture**：见 [tests/fixtures/](tests/fixtures)（示例：`profile_generic/coding_phase_disabled`），由 `framework/harness/tests/run-tests.ts` 与其它 profile fixture 根合并扫描。
