# Stale CLAUDE.md (fixture)

This file is intentionally divergent from the AGENTS.md.template rendering
to verify check-init.ts catches it as POPULATED with a non-empty diff_summary.

If you ever see check-init describing this file as "已与模板一致", the
init-diff Hallucination Ban regression has reappeared — fix it.
