# Ledger requirements

## REQ-LEDGER-REFRESH

After a committed ledger mutation, every affected consumer must refresh from the authoritative repository state and remain recoverable after process recreation.
